{
  super.init();
  getWidget().connector=this;
  registerRpc(ComboBoxClientRpc.class,new ComboBoxClientRpc(){
    @Override public void setSelectedItem(    String selectedKey,    String selectedCaption){
      getDataReceivedHandler().updateSelectionFromServer(selectedKey,selectedCaption);
    }
  }
);
}
