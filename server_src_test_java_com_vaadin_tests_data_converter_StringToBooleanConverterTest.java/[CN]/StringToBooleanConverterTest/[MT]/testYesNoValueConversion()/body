{
  assertResult(true,yesNoConverter.convertToModel("yes",null));
  assertResult(false,yesNoConverter.convertToModel("no",null));
  Assert.assertEquals("yes",yesNoConverter.convertToPresentation(true,null));
  Assert.assertEquals("no",yesNoConverter.convertToPresentation(false,null));
}
