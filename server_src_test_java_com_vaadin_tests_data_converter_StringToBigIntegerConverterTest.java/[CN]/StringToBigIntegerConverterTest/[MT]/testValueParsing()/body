{
  String bigInt="1180591620717411303424";
  Result<BigInteger> converted=getConverter().convertToModel(bigInt,null);
  BigInteger expected=new BigInteger(bigInt);
  assertResult("Value bigger than max long was converted incorrectly",expected,converted);
}
