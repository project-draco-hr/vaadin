{
  getLogger().info("Server returned " + event.getResponse().getStatusCode() + " for xhr request");
  getLogger().warning("xhrInvalidStatusCode");
  handleTemporaryError(Type.MESSAGE,event.getPayload());
}
