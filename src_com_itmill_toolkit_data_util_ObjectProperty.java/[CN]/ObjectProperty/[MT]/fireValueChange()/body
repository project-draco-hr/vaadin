{
  if (valueChangeListeners != null) {
    Object[] l=valueChangeListeners.toArray();
    Property.ValueChangeEvent event=new ObjectProperty.ValueChangeEvent(this);
    for (int i=0; i < l.length; i++)     ((Property.ValueChangeListener)l[i]).valueChange(event);
  }
}
