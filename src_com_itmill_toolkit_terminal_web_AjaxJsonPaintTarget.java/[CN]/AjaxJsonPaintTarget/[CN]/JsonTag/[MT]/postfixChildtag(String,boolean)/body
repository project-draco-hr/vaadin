{
  TagCounter i=(TagCounter)childTagCounters.get(tagName);
  if (i == null) {
    i=new TagCounter();
    childTagCounters.put(tagName,i);
  }
  tagName=i.postfix(tagName);
  if (!start)   i.increment();
  return tagName;
}
