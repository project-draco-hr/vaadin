{
  String pres=name + ":{name:\"" + name+ "\",id:\""+ code+ "\",type:\"array\", value : [";
  for (int i=0; i < value.length; ) {
    pres+=value[i];
    i++;
    if (i < value.length)     pres+=",";
  }
  pres+="]}";
  return pres;
}
