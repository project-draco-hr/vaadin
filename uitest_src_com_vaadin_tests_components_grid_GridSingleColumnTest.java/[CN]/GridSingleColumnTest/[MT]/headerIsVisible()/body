{
  openTestURL();
  WebElement header=getDriver().findElement(By.className("v-grid-header"));
  WebElement cell=header.findElement(By.className("v-grid-cell"));
  assertThat(cell.getText(),is("Header"));
}
