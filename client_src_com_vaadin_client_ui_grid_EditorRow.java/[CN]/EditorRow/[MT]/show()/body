{
  if (state == State.ACTIVATING) {
    handler.bind(new EditorRowRequest<T>(grid,rowIndex,new RequestCallback<T>(){
      @Override public void onResponse(      EditorRowRequest<T> request){
        if (state == State.ACTIVATING) {
          state=State.ACTIVE;
          showOverlay(grid.getEscalator().getBody().getRowElement(request.getRowIndex()));
        }
      }
    }
));
    grid.getEscalator().setScrollLocked(Direction.VERTICAL,true);
  }
}
