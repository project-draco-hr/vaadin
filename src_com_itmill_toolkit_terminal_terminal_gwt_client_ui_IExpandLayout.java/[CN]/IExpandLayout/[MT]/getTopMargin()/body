{
  if (topMargin < 0) {
    topMargin=DOM.getElementPropertyInt(childContainer,"offsetTop") - DOM.getElementPropertyInt(getElement(),"offsetTop");
  }
  if (topMargin < 0) {
    return 0;
  }
 else {
    return topMargin;
  }
}
