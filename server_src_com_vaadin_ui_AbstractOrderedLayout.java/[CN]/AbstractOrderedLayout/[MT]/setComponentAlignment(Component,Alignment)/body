{
  ChildComponentData childData=getState().getChildData().get(childComponent);
  if (childData != null) {
    childData.setAlignmentBitmask(alignment.getBitMask());
  }
 else {
    throw new IllegalArgumentException("Component must be added to layout before using setComponentAlignment()");
  }
}
