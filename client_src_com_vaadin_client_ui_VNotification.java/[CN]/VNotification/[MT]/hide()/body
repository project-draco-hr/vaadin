{
  DOM.removeEventPreview(this);
  if (cssTransitionDelay >= 0) {
    AnimationUtil.setAnimationDelay(getElement(),cssTransitionDelay + "ms");
  }
  super.hide();
  notifications.remove(this);
  fireEvent(new HideEvent(this));
}
