{
  if (root == mainWindow) {
    return "";
  }
  for (  Entry<String,Root> entry : legacyRootNames.entrySet()) {
    if (entry.getValue() == root) {
      return entry.getKey();
    }
  }
  return addWindow(root);
}
