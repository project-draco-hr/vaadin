{
  PendingRootRequest pendingRootRequest=pendingRoots.get(getRootId(request));
  if (pendingRootRequest == null) {
    return null;
  }
 else {
    return pendingRootRequest.getCombinedRequest(request);
  }
}
