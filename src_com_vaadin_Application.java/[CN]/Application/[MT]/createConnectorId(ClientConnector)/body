{
  String connectorId=String.valueOf(connectorIdSequence++);
  Connector oldReference=connectorIdToConnector.put(connectorId,connector);
  if (oldReference != null) {
    throw new RuntimeException("An error occured while generating connector ids. A connector with id " + connectorId + " was already found!");
  }
  return connectorId;
}
