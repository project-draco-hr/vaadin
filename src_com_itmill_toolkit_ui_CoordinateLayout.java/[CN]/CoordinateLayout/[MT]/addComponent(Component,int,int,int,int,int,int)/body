{
  if (c == null) {
    throw new IllegalArgumentException();
  }
  Coordinates newCoords=new Coordinates(left,top,width,height,right,bottom);
  componentToCoord.put(c,newCoords);
  addComponent(c);
  return newCoords;
}
