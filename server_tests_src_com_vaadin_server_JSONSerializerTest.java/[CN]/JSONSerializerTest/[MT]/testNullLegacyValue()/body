{
  JSONArray inputArray=new JSONArray(Arrays.asList("n",JSONObject.NULL));
  UidlValue decodedObject=(UidlValue)JsonCodec.decodeInternalType(UidlValue.class,true,inputArray,null);
  assertNull(decodedObject.getValue());
}
