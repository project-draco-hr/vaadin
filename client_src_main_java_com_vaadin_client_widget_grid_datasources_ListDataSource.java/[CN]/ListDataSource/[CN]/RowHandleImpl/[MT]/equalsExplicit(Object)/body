{
  if (obj instanceof ListDataSource.RowHandleImpl) {
    @SuppressWarnings("unchecked") RowHandleImpl rhi=(RowHandleImpl)obj;
    return SharedUtil.equals(row,rhi.row);
  }
 else {
    return false;
  }
}
