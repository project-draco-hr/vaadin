{
  if (newContent == null || newContent.getMinimizedValueAsHTML() == null || newContent.getPopupComponent() == null) {
    throw new IllegalArgumentException("Content object is or contains null");
  }
  content=newContent;
  requestRepaint();
}
