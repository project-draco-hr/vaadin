{
  BeanItemContainer<Person> bc=new BeanItemContainer<Person>(Person.class);
  for (int i=0; i < names.length; i++) {
    Person p=new Person();
    p.setName(names[i]);
    p.setAge(ages[i]);
    bc.addBean(p);
  }
  return bc;
}
