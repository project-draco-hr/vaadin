{
  ThreeColumnLayout layout=new ThreeColumnLayout(createLabelWithUndefinedSize("first component"),createLabelWithUndefinedSize("second component"),createLabelWithUndefinedSize("third component"));
  addComponent(layout);
  setWidth("600px");
  if (UI.getCurrent().getPage().getWebBrowser().isChrome()) {
    getPage().getStyles().add("body { zoom: 1.10; }");
  }
}
