{
  Map<String,StreamVariable> nameToStreamVar=pidToNameToStreamVariable.get(owner.getConnectorId());
  nameToStreamVar.remove(name);
  if (nameToStreamVar.isEmpty()) {
    pidToNameToStreamVariable.remove(owner.getConnectorId());
  }
}
