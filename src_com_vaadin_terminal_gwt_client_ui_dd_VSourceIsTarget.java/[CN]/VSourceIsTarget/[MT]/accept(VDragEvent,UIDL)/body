{
  VPaintableWidget dragSource=drag.getTransferable().getDragSource();
  VPaintableWidget paintable=VDragAndDropManager.get().getCurrentDropHandler().getPaintable();
  return paintable == dragSource;
}
