{
  selectMenuPath(SET_GENERATOR);
  selectMenuPath(TOGGLE_DETAILS_FOR_ROW_1);
  TestBenchElement details=getGridElement().getDetails(1);
  assertTrue("Unexpected details content",details.getText().startsWith("Row: 1."));
}
