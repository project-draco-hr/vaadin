{
  selectMenuPath(SET_EMPTY_GENERATOR);
  selectMenuPath(TOGGLE_DETAILS_FOR_ROW_1);
  assertEquals("empty generator did not produce an empty details row","",getGridElement().getDetails(1).getText());
}
