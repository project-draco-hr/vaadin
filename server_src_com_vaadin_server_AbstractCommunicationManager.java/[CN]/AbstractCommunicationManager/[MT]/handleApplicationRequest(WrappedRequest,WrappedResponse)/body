{
  for (  RequestHandler handler : new ArrayList<RequestHandler>(application.getRequestHandlers())) {
    if (handler.handleRequest(application,request,response)) {
      return true;
    }
  }
  return false;
}
