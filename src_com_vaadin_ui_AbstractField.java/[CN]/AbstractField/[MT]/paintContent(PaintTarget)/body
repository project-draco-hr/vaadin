{
  if (getTabIndex() != 0) {
    target.addAttribute("tabindex",getTabIndex());
  }
  if (isModified()) {
    target.addAttribute("modified",true);
  }
  if (!isReadOnly() && isRequired()) {
    target.addAttribute(VAbstractPaintableWidget.ATTRIBUTE_REQUIRED,true);
  }
  if (shouldHideErrors()) {
    target.addAttribute(VAbstractPaintableWidget.ATTRIBUTE_HIDEERRORS,true);
  }
}
