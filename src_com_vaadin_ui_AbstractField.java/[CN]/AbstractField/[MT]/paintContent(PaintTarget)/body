{
  if (getTabIndex() != 0) {
    target.addAttribute("tabindex",getTabIndex());
  }
  if (isModified()) {
    target.addAttribute("modified",true);
  }
  if (!isReadOnly() && isRequired()) {
    target.addAttribute(AbstractComponentConnector.ATTRIBUTE_REQUIRED,true);
  }
  if (shouldHideErrors()) {
    target.addAttribute(AbstractComponentConnector.ATTRIBUTE_HIDEERRORS,true);
  }
}
