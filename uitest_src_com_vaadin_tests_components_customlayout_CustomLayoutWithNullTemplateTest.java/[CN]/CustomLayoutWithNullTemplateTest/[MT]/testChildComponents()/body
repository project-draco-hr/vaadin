{
  openTestURL();
  ElementQuery<CustomLayoutElement> customLayout=$(CustomLayoutElement.class);
  assertTrue("Button was not rendered.",customLayout.$(ButtonElement.class).exists());
  assertTrue("Label was not rendered.",customLayout.$(LabelElement.class).exists());
}
