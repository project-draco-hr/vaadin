{
  final String filename=resource.getFilename();
  if (filename == null) {
    return ApplicationConstants.APP_PROTOCOL_PREFIX + ApplicationConstants.APP_REQUEST_PATH + mapKey+ "/";
  }
 else {
    String encodedFileName=urlEncode(filename).replace("%2F","/").replace("%5C","\\");
    return ApplicationConstants.APP_PROTOCOL_PREFIX + ApplicationConstants.APP_REQUEST_PATH + mapKey+ "/"+ encodedFileName;
  }
}
