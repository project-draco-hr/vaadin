{
  assertTrue("Didn't accept valid value",minMaxValidator.isValid((short)15));
  assertTrue("Didn't accept valid value",minMaxValidator.isValid((short)99));
  assertFalse("Accepted too small value",minMaxValidator.isValid((short)9));
  assertFalse("Accepted too large value",minMaxValidator.isValid((short)110));
}
