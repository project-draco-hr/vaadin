{
  blur(e);
  if (BrowserInfo.get().isOpera()) {
    Scheduler.get().scheduleDeferred(new Command(){
      @Override public void execute(){
        focus(e);
      }
    }
);
  }
 else {
    focus(e);
  }
}
