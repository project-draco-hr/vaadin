{
  MeasuredSize measuredSize=VPaintableMap.get(client).getPaintable(this).getMeasuredSize();
  int extraSpace=getPopUpOpenerWidth() + measuredSize.getPaddingWidth() + measuredSize.getBorderWidth();
  return extraSpace;
}
