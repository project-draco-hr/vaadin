{
  focused=true;
  if (prompting && !readonly) {
    setPromptingOff("");
  }
  addStyleDependentName("focus");
  if (client.hasEventListeners(this,FOCUS_EVENT_IDENTIFIER)) {
    client.updateVariable(paintableId,FOCUS_EVENT_IDENTIFIER,"",true);
  }
}
