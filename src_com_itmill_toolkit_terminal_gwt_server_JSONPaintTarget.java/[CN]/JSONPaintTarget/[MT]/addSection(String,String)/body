{
  Tag t=new Tag(sectionTagName);
  t.data=sectionData;
  if (tag.data != null || tag.xml != null)   throw new IllegalStateException("Children can not be combined with XML or chardata");
  tag.children.add(t);
}
