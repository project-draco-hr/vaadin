{
  String className=resolveWidgetTypeName(uidl);
  if ("com.itmill.toolkit.demo.colorpicker.gwt.client.ui.IColorPicker".equals(className)) {
    return new IColorPicker();
  }
  return super.createWidget(uidl);
}
