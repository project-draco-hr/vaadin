{
  if (board != null)   layout.removeComponent(board);
  Game game=(Game)event.getProperty().getValue();
  if (game != null) {
    board=new Board(game,game.getBlackPlayer() == getUser());
    layout.addComponent(board,"board");
  }
}
