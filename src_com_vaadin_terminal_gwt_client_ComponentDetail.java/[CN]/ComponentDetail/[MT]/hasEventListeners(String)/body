{
  if (eventListeners != null) {
    int l=eventListeners.length();
    for (int i=0; i < l; i++) {
      if (eventListeners.get(i).equals(eventIdentifier)) {
        return true;
      }
    }
  }
  return false;
}
