{
  super.updateState();
  getState().getChildCss().clear();
  for (  Component child : this) {
    String componentCssString=getCss(child);
    if (componentCssString != null) {
      getState().getChildCss().put(child.getConnectorId(),componentCssString);
    }
  }
}
