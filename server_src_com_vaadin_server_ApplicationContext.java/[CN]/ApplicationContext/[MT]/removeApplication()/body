{
  if (application == null) {
    return;
  }
  try {
    application.close();
  }
 catch (  Exception e) {
    getLogger().log(Level.SEVERE,"Could not close application, leaking memory.",e);
  }
 finally {
    application=null;
    communicationManager=null;
  }
}
