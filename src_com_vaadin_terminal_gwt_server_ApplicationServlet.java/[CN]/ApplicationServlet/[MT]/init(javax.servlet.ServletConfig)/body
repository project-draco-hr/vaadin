{
  super.init(servletConfig);
  try {
    applicationClass=ServletPortletHelper.getApplicationClass(getDeploymentConfiguration());
  }
 catch (  ApplicationClassException e) {
    throw new ServletException(e);
  }
}
