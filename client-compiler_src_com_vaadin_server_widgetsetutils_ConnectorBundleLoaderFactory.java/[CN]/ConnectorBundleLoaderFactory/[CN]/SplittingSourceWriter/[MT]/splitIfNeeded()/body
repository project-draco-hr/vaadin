{
  if (approximateChars > splitSize) {
    String newMethod=baseName + wrapCount++;
    println("%s();",newMethod);
    outdent();
    println("}");
    println("private void %s() {",newMethod);
    indent();
    approximateChars=0;
  }
}
