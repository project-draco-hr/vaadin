{
  Map<JClassType,Set<JMethod>> needsNoLayout=bundle.getNeedsNoLayoutRpcMethods();
  for (  Entry<JClassType,Set<JMethod>> entry : needsNoLayout.entrySet()) {
    JClassType type=entry.getKey();
    for (    JMethod method : entry.getValue()) {
      w.println("store.setNoLayoutRpcMethod(%s, \"%s\");",getClassLiteralString(type),method.getName());
    }
    w.splitIfNeeded();
  }
}
