{
  assertNull("Found view name for null view string in an empty view provider",provider.getViewName((String)null));
  provider.addView("test",TestView.class);
  assertNull("Found view name for null view string",provider.getViewName((String)null));
}
