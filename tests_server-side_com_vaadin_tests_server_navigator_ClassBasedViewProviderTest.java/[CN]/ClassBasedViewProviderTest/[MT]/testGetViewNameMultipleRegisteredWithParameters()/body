{
  provider.addView("test",TestView.class);
  provider.addView("test2",TestView2.class);
  assertEquals("Incorrect view name found for view string","test",provider.getViewName("test/test2/params"));
}
