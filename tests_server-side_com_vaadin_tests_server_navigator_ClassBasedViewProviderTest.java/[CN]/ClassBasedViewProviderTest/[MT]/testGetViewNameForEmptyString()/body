{
  assertNull("Found view name for empty view string in an empty provider",provider.getViewName(""));
  provider.addView("test",TestView.class);
  assertNull("Found view name for empty view string",provider.getViewName(""));
}
