{
  assertNull("View class found for empty view provider",provider.getViewClass("test"));
  provider.addView("test",TestView.class);
  assertEquals("View class not found",TestView.class,provider.getViewClass("test"));
  assertNull("View class found for unregistered view name",provider.getViewClass("test2"));
}
