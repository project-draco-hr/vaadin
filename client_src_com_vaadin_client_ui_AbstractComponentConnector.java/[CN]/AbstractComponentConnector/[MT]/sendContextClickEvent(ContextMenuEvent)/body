{
  event.preventDefault();
  event.stopPropagation();
  getRpcProxy(ContextClickRpc.class).contextClick(MouseEventDetailsBuilder.buildMouseEventDetails(event.getNativeEvent(),getWidget().getElement()));
}
