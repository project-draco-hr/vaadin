{
  if (widget == null) {
    Profiler.enter("AbstractComponentConnector.createWidget for " + WidgetUtil.getSimpleName(this));
    widget=createWidget();
    Profiler.leave("AbstractComponentConnector.createWidget for " + WidgetUtil.getSimpleName(this));
  }
  return widget;
}
