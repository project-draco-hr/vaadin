{
  super.setHeight(height);
  activeLayoutSize.setHeight(getOffsetHeight() - activeMargins.getVertical());
  if (!isRendering) {
    if (recalculateLayoutAndComponentSizes()) {
      client.runDescendentsLayout(this);
    }
  }
}
