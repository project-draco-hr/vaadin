{
  super.setHeight(height);
  if (ORIENTATION_HORIZONTAL == orientationMode) {
    return;
  }
  if (height == null || "".equals(height)) {
    if (fixedCellSize) {
      removeFixedSizes();
    }
  }
 else {
    fixedCellSize=true;
  }
}
