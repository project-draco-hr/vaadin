{
  final Element wrapper=((WidgetWrapper)widgetToWrapper.get(w)).getElement();
  final boolean removed=super.remove(w);
  if (removed) {
    DOM.removeChild(childContainer,wrapper);
    widgetToWrapper.remove(w);
    return true;
  }
  return false;
}
