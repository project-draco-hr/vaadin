{
  Element wrapper=DOM.getParent(w.getElement());
  boolean removed=super.remove(w);
  if (removed) {
    if (!(w instanceof Caption)) {
      DOM.removeChild(childContainer,orientationMode == ORIENTATION_HORIZONTAL ? wrapper : DOM.getParent(wrapper));
    }
    return true;
  }
  return false;
}
