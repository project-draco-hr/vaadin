{
  super.setWidth(width);
  if (width != null && !width.equals("")) {
    activeLayoutSize.setWidth(getOffsetWidth() - activeMargins.getHorizontal());
  }
  if (!isRendering) {
    boolean sameSize=recalculateLayoutAndComponentSizes();
    if (!sameSize) {
      client.runDescendentsLayout(this);
    }
  }
}
