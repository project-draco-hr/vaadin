{
  super.setWidth(width);
  if (ORIENTATION_VERTICAL == orientationMode) {
    return;
  }
  if (width == null || "".equals(width)) {
    if (fixedCellSize) {
      removeFixedSizes();
    }
  }
 else {
    fixedCellSize=true;
  }
}
