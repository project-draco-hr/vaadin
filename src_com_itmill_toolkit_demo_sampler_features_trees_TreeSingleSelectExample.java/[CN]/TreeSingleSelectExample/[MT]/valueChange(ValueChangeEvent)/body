{
  if (event.getProperty().getValue() != null) {
    editor.setValue(tree.getItem(event.getProperty().getValue()).getItemProperty(ExampleUtil.hw_PROPERTY_NAME));
    editor.requestRepaint();
    editBar.setEnabled(true);
  }
 else {
    editor.setValue("");
    editBar.setEnabled(false);
  }
}
