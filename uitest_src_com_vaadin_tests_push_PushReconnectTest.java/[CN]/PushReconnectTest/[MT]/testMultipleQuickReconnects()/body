{
  setDebug(true);
  openTestURL();
  startTimer();
  waitUntilServerCounterChanges();
  for (int i=0; i < 50; i++) {
    disconnectProxy();
    Thread.sleep(100);
    connectProxy();
    Thread.sleep(100);
  }
  waitUntilServerCounterChanges();
  waitUntilServerCounterChanges();
}
