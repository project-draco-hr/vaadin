{
  setDebug(true);
  openTestURL();
  startTimer();
  waitUntilServerCounterChanges();
  for (int i=0; i < 50; i++) {
    disconnectProxy();
    Thread.sleep(50);
    connectProxy();
    Thread.sleep(50);
  }
  waitUntilServerCounterChanges();
  waitUntilServerCounterChanges();
}
