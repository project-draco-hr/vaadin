{
  if (indexToEnsureInView != -1) {
    int newPage=(indexToEnsureInView + (needNullSelectOption ? 1 : 0)) / getPageLength();
    page=newPage;
  }
  if (page * getPageLength() > size) {
    page=(size + (needNullSelectOption ? 1 : 0)) / getPageLength();
  }
  return page;
}
