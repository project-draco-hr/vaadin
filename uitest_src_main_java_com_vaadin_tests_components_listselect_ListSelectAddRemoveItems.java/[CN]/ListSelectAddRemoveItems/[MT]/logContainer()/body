{
  StringBuilder b=new StringBuilder();
  for (int i=0; i < container.size(); i++) {
    Object id=container.getIdByIndex(i);
    if (i != 0) {
      b.append(", ");
    }
    b.append(id);
  }
  log(b.toString());
}
