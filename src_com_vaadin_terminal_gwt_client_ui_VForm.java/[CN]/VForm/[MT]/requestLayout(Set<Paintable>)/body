{
  if (height != null && !"".equals(height) && width != null && !"".equals(width)) {
    return true;
  }
  if (renderInformation.updateSize(getElement())) {
    return false;
  }
 else {
    return true;
  }
}
