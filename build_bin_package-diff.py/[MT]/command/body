def command(cmd, dryrun=0):
    if (not dryrun):
        if os.system(cmd):
            print ("Command '%s' failed, exiting." % cmd)
            sys.exit(1)
    else:
        print 'Dry run - not executing.'
