{
  if (closing && tooltipOwner == owner) {
    closeTimer.cancel();
    closing=false;
    return;
  }
  updatePosition(event);
  if (opening) {
    showTimer.cancel();
  }
  tooltipOwner=owner;
  showTimer.schedule(1000);
  opening=true;
}
