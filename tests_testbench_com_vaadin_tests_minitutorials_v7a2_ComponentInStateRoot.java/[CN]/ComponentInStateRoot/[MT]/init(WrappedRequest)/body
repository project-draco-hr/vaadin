{
  ComponentInStateComponent component=new ComponentInStateComponent();
  component.setOtherComponent(this);
  addComponent(component);
  addComponent(new Label("Server-side type of other component: " + component.getOtherComponent().getClass().getName()));
}
