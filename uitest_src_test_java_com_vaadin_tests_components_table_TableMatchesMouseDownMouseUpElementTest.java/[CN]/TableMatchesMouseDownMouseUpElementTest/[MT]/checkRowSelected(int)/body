{
  assertEquals("contents of the selected row don't match contents of the row #" + rowIndex,table.getCell(rowIndex,0).getText(),getSelectedRows().get(0).findElement(By.className("v-table-cell-wrapper")).getText());
}
