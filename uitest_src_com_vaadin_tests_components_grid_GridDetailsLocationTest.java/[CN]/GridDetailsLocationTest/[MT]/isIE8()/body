{
  DesiredCapabilities desiredCapabilities=getDesiredCapabilities();
  DesiredCapabilities ie8Capabilities=Browser.IE8.getDesiredCapabilities();
  return desiredCapabilities.getBrowserName().equals(ie8Capabilities.getBrowserName()) && desiredCapabilities.getVersion().equals(ie8Capabilities.getVersion());
}
