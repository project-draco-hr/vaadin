{
  HorizontalLayout layout=new HorizontalLayout();
  addComponent(layout);
  Label gap=new Label();
  gap.setWidth(250,Unit.PIXELS);
  layout.addComponent(gap);
  LegacyPopupDateField field=new LegacyPopupDateField();
  layout.addComponent(field);
}
