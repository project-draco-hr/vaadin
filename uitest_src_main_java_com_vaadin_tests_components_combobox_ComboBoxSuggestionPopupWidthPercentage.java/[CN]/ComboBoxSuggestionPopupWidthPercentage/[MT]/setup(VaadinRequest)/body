{
  ComboBox percentage=new ComboBox("200px wide ComboBox with 200% wide suggestion popup",items);
  percentage.addStyleName("percentage");
  percentage.setWidth("200px");
  percentage.setPopupWidth("200%");
  addComponent(percentage);
}
