{
  Panel p=new Panel();
  p.setSizeFull();
  Button b=new Button("toggle Values",new Button.ClickListener(){
    public void buttonClick(    ClickEvent event){
      toggleStyleName();
    }
  }
);
  p.addComponent(b);
  return p;
}
