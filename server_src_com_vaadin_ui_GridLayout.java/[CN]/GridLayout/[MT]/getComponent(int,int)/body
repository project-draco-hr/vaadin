{
  for (final Iterator<Area> iterator=areas.iterator(); iterator.hasNext(); ) {
    final Area area=iterator.next();
    if (area.getColumn1() <= x && x <= area.getColumn2() && area.getRow1() <= y && y <= area.getRow2()) {
      return area.getComponent();
    }
  }
  return null;
}
