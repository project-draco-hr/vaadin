{
  LexicalUnitImpl operand4cm=LexicalUnitImpl.createCM(0,0,null,4);
  LexicalUnitImpl operatorDivide=LexicalUnitImpl.createSlash(0,0,operand4cm);
  LexicalUnitImpl operand2=LexicalUnitImpl.createInteger(0,0,operatorDivide,2);
  LexicalUnitImpl result=evaluator.evaluate(operand4cm);
  Assert.assertEquals(2,result.getIntegerValue());
  Assert.assertEquals(LexicalUnit.SAC_CENTIMETER,result.getLexicalUnitType());
}
