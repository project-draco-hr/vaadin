{
  selectMenuPath(EDIT_ITEM_5);
  new Actions(getDriver()).click(getGridElement().getCell(4,0)).sendKeys(Keys.ENTER).perform();
  assertEquals("Editor should still edit row 5","(5, 0)",getEditorWidgets().get(0).getAttribute("value"));
}
