{
  if (!c.isVisible()) {
    return;
  }
  markDirty(c);
  if (c instanceof HasComponents) {
    HasComponents container=(HasComponents)c;
    for (    Component child : AbstractCommunicationManager.getChildComponents(container)) {
      markComponentsDirtyRecursively(child);
    }
  }
}
