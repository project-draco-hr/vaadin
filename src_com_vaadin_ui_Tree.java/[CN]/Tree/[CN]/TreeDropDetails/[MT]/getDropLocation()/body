{
  String s=(String)vars.get("detail");
  if ("Top".equals(s)) {
    return Location.TOP;
  }
 else   if ("Bottom".equals(s)) {
    return Location.BOTTOM;
  }
 else {
    return Location.MIDDLE;
  }
}
