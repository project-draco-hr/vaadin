{
  count++;
  if (stressLayout != null)   main.removeComponent(stressLayout);
  stressLayout=randomComponents.getRandomComponentContainer("Component container " + count);
  randomComponents.fillLayout(stressLayout,20);
  main.addComponent(stressLayout);
  System.out.println("Created " + count + " times.");
}
