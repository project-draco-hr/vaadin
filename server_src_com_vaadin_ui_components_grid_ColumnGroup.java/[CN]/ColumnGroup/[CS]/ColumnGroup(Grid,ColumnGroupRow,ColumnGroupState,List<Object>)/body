{
  if (propertyIds == null) {
    throw new IllegalArgumentException("propertyIds cannot be null. Use empty list instead.");
  }
  this.state=state;
  this.row=row;
  columns=Collections.unmodifiableList(new ArrayList<Object>(propertyIds));
  this.grid=grid;
}
