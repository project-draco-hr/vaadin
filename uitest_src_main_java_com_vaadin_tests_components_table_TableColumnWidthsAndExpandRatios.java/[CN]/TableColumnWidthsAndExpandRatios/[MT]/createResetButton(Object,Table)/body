{
  return new NativeButton("Reset " + property + " width",new Button.ClickListener(){
    @Override public void buttonClick(    ClickEvent event){
      table.setColumnWidth(property,-1);
    }
  }
);
}
