{
  int firstTop=DOM.getAbsoluteTop(DOM.getFirstChild(childContainer));
  int lastTop=DOM.getAbsoluteTop(DOM.getChild(childContainer,(DOM.getChildCount(childContainer) - 1)));
  return firstTop != lastTop;
}
