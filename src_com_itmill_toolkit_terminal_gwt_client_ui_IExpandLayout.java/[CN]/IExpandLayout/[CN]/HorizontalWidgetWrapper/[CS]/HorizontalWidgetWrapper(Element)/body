{
  if (DOM.getElementProperty(element,"nodeName").equals("TD")) {
    td=element;
    setElement(DOM.getParent(DOM.getParent(DOM.getParent(DOM.getParent(td)))));
  }
 else {
    setElement(element);
  }
}
