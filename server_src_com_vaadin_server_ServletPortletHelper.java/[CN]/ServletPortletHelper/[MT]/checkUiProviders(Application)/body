{
  if (newApplication.getUIProviders().isEmpty()) {
    throw new ApplicationClassException("No UIProvider has been added to the application and there is no \"" + Application.UI_PARAMETER + "\" init parameter.");
  }
}
