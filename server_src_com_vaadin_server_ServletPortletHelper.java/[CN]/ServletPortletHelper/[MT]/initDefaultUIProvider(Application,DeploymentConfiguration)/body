{
  String uiProperty=deploymentConfiguration.getApplicationConfiguration().getInitParameters().getProperty(Application.UI_PARAMETER);
  if (uiProperty != null) {
    verifyUIClass(uiProperty,deploymentConfiguration.getClassLoader());
    application.addUIProvider(new DefaultUIProvider());
  }
}
