{
  String className=this.getClass().getName() + "Example";
  String javaFileName=className.replace(".","/");
  InputStream is=getClass().getClassLoader().getResourceAsStream(javaFileName);
  return new InputStreamReader(is);
}
