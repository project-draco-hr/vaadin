{
  if (terminal.getJavaScriptVersion().supports(this.requiredVersion))   return true;
  Log.info("Requirement: " + this.requiredVersion + " is not met by "+ terminal.getJavaScriptVersion());
  return false;
}
