{
  List list=new LinkedList();
  if (!this.supports(terminal))   return list;
  for (Iterator i=this.files.iterator(); i.hasNext(); ) {
    File f=(File)i.next();
    if (f instanceof Fileset) {
      list.addAll(((Fileset)f).getFileNames(terminal));
    }
 else {
      list.add(f.getName());
    }
  }
  return list;
}
