{
  if (terminal.getBrowserApplication().indexOf(this.agentSubstring) > 0)   return true;
  Log.info("Requirement: '" + this.agentSubstring + "' is not met by "+ terminal.getBrowserApplication());
  return false;
}
