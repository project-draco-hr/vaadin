{
  Assert.assertEquals(getMonthEvent(0).getText(),"12:00 AM <b>Hello</b> <u>world</u>!");
  click(htmlAllowed);
  Assert.assertEquals("1. HTML in event caption: true",getLogRow(0));
  Assert.assertEquals(getMonthEvent(0).getText(),"12:00 AM Hello world!");
}
