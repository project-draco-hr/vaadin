{
  if (getRootComponent() == abstractComponent && abstractComponent.getClass().isAnnotationPresent(DesignRoot.class)) {
    return (T)getDefaultInstance((Class<? extends Component>)abstractComponent.getClass().getSuperclass());
  }
  return (T)getDefaultInstance(abstractComponent.getClass());
}
