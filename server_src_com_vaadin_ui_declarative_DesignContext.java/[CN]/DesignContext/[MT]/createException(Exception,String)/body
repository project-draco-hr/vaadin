{
  String message=String.format("Couldn't instantiate a component for %s.",qualifiedClassName);
  if (e != null) {
    return new ComponentInstantiationException(message,e);
  }
 else {
    return new ComponentInstantiationException(message);
  }
}
