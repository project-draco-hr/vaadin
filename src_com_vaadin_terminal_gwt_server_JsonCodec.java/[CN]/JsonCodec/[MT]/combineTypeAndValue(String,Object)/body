{
  if (type == null) {
    throw new RuntimeException("Type for value " + value + " cannot be null!");
  }
  JSONArray outerArray=new JSONArray();
  outerArray.put(type);
  outerArray.put(value);
  return outerArray;
}
