{
  if (null == value) {
    return JsonEncoder.VTYPE_NULL;
  }
  String transportType=typeToTransportType.get(value.getClass());
  if (null != transportType) {
    return transportType;
  }
  throw new JSONException("Unknown object type " + value.getClass().getName());
}
