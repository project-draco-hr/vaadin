{
  String val="";
  if (n.getChildNodes().getLength() > 0) {
    for (int i=0; i < n.getChildNodes().getLength(); i++)     if (n.getChildNodes().item(i).getNodeType() == Node.TEXT_NODE)     val+=n.getChildNodes().item(i).getNodeValue();
  }
  return val;
}
