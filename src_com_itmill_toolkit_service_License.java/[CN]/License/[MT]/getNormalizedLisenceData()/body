{
  if (licenseXML == null)   throw new LicenseFileCanNotBeRead("File was not found in standard location and it has not been explicitely read during Application.init().");
  CharArrayWriter sink=new CharArrayWriter();
  try {
    serialize(licenseXML,sink);
  }
 catch (  IOException e) {
    throw new InvalidLicenseFile("Can not serialize the license file.");
  }
  return new String(sink.toCharArray());
}
