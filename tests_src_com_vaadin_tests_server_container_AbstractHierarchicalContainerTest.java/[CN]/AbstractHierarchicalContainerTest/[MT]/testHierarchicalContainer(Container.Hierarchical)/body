{
  initializeContainer(container);
  int packages=21;
  int expectedSize=sampleData.length + packages;
  validateHierarchicalContainer(container,"com","com.vaadin.util.SerializerHelper","com.vaadin.terminal.ApplicationResource","blah",expectedSize,1,true);
}
