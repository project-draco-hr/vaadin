{
  GlobalResourceHandler globalResourceHandler=uI.getApplication().getGlobalResourceHandler(false);
  if (globalResourceHandler != null) {
    globalResourceHandler.unregisterConnector(connector);
  }
}
