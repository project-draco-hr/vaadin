{
  super("Panels","Panels","Panel is a simple container that supports scrolling.<br/>It's internal layout (by default VerticalLayout) can be configured or exchanged to get desired results. Components that are added to the Panel will in effect be added to the layout.",new Feature[]{new PanelBasic(),new PanelLight()});
}
