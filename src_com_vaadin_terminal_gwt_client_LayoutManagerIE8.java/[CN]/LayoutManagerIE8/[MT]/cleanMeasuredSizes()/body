{
  Iterator<Element> i=measuredSizes.keySet().iterator();
  while (i.hasNext()) {
    Element e=i.next();
    if (e.getOwnerDocument() != RootPanel.get().getElement().getOwnerDocument()) {
      i.remove();
    }
  }
}
