{
  return "On window resize undefined " + "columns (by server or user (dragged)) columns " + "must consume the excess space. Space is divided "+ "by default according to natural widths of columns."+ " Lastname should get 1/3 of excess spsace, the las column 2/3.";
}
