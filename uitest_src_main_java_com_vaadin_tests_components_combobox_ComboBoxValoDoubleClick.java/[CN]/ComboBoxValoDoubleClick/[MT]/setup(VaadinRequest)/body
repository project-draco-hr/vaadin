{
  ComboBox<String> cb=new ComboBox<>("Double-click Me");
  List<String> items=new ArrayList<String>();
  for (int i=0; i < 100; i++) {
    items.add("Item-" + i);
  }
  cb.setItems(items);
  addComponent(cb);
}
