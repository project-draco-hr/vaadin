{
  int i=0;
  for (Iterator it=getChildren().iterator(); it.hasNext(); ) {
    Widget w=(Widget)it.next();
    if (w == child) {
      return i;
    }
 else     if (!(w instanceof Caption)) {
      i++;
    }
  }
  return -1;
}
