{
  if (handler instanceof BodyKeyPressHandler || handler instanceof HeaderKeyPressHandler || handler instanceof FooterKeyPressHandler) {
    return addHandler(handler,keyPress.getAssociatedType());
  }
  throw new IllegalArgumentException("Handler not a valid extension of GridKeyPressHandler");
}
