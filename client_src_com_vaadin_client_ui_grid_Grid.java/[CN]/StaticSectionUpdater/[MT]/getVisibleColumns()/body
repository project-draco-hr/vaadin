{
  List<GridColumn<?,T>> visible=new ArrayList<GridColumn<?,T>>();
  for (  GridColumn<?,T> column : getColumns()) {
    if (column.isVisible()) {
      visible.add(column);
    }
  }
  return visible;
}
