{
  if (editorRow.getState() != State.INACTIVE) {
    if (event.getTypeInt() == Event.ONKEYDOWN && event.getKeyCode() == EditorRow.KEYCODE_HIDE) {
      editorRow.cancel();
    }
    return true;
  }
  if (editorRow.isEnabled()) {
    if (event.getTypeInt() == Event.ONDBLCLICK) {
      if (container == escalator.getBody() && cell != null) {
        editorRow.editRow(cell.getRow());
        return true;
      }
    }
 else     if (event.getTypeInt() == Event.ONKEYDOWN && event.getKeyCode() == EditorRow.KEYCODE_SHOW) {
      editorRow.editRow(activeCellHandler.activeRow);
      return true;
    }
  }
  return false;
}
