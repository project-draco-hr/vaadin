{
  if (isExpanded(itemId))   return true;
  if (!areChildrenAllowed(itemId))   return false;
  expanded.add(itemId);
  expandedItemId=itemId;
  if (initialPaint)   requestRepaint();
 else   requestPartialRepaint();
  fireExpandEvent(itemId);
  return true;
}
