{
  final Window w=new Window();
  w.addComponent(new Label("Open this link: <a href='" + getURL().toExternalForm() + "'>"+ getURL().toExternalForm()+ "</a> in another browser-window.",Label.CONTENT_XHTML));
  return w;
}
