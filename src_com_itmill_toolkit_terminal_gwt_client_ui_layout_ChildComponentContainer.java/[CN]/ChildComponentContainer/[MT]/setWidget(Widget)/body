{
  if (w == widget) {
    return;
  }
  if (w != null) {
    w.removeFromParent();
  }
  if (widget != null) {
    remove(widget);
  }
  widget=w;
  if (w != null) {
    DOM.appendChild(widgetDIV,widget.getElement());
    adopt(w);
  }
}
