{
  setMarginTop(containerMarginTop);
  if (caption != null) {
    DOM.setStyleAttribute(caption.getElement(),"marginLeft",alignmentLeftOffsetForCaption + "px");
  }
  DOM.setStyleAttribute(widgetDIV,"marginLeft",alignmentLeftOffsetForWidget + "px");
}
