{
  String logoutUrl=application.getLogoutURL();
  if (logoutUrl == null)   logoutUrl=application.getURL().toString();
  AjaxApplicationContext context=(AjaxApplicationContext)application.getContext();
  if (context != null)   context.removeApplication(application);
  response.sendRedirect(response.encodeRedirectURL(logoutUrl));
}
