{
  BeanItemContainer<TableBean> container=new BeanItemContainer<TableBean>(TableBean.class);
  container.addBean(new TableBean("first"));
  container.addBean(new TableBean("second"));
  container.addBean(new TableBean("third"));
  return container;
}
