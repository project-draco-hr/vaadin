{
  Navigator navigator=new Navigator(new NullFragmentManager(),new NullDisplay());
  try {
    navigator.addView("test",(View)null);
    fail("addView() accepted null view instance");
  }
 catch (  IllegalArgumentException e) {
  }
}
