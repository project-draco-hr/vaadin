{
  Assert.assertNotNull("Selected value is null",select.getValue());
  Assert.assertTrue("Selected value is not a collection",select.getValue() instanceof Collection);
  Assert.assertEquals("Wrong number of selected items",((Collection<?>)select.getValue()).size(),count);
}
