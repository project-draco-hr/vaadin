{
  String uniqueBrowserIdentifier;
  if (versionOverride == null) {
    uniqueBrowserIdentifier=BrowserUtil.getUniqueIdentifier(getDesiredCapabilities());
  }
 else {
    uniqueBrowserIdentifier=BrowserUtil.getUniqueIdentifier(getDesiredCapabilities(),"" + versionOverride);
  }
  return getScreenshotReferenceDirectory() + File.separator + getScreenshotBaseName()+ "_"+ uniqueBrowserIdentifier+ "_"+ identifier+ ".png";
}
