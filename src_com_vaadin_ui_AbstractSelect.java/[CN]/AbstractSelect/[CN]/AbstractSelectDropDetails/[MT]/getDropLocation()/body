{
  String s=(String)getData("detail");
  if ("TOP".equals(s)) {
    return Location.TOP;
  }
 else   if ("BOTTOM".equals(s)) {
    return Location.BOTTOM;
  }
 else {
    return Location.MIDDLE;
  }
}
