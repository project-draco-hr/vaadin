{
  RpcManager manager=target.getRpcManager();
  if (manager != null) {
    manager.applyInvocation(invocation);
  }
 else {
    throw new RuntimeException("RPC call to a target without an RPC manager.");
  }
}
