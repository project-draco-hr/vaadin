{
  for (  LocatorStrategy strategy : locatorStrategies) {
    if (strategy.validatePath(path)) {
      com.google.gwt.user.client.Element element=strategy.getElementByPath(path);
      if (null != element) {
        return element;
      }
    }
  }
  return null;
}
