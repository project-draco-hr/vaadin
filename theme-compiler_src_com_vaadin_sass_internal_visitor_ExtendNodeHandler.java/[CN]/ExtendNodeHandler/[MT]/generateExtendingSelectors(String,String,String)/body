{
  String result=StringUtil.replaceSubString(selectorString,extendedSelector,extendingSelector);
  if (result.startsWith(".")) {
    result=StringUtil.removeDuplicatedSubString(result,".");
  }
  return result;
}
