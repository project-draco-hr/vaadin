{
  openTestURL();
  GridElement grid=getGridElement();
  assertTrue("Body cell 0, 0 is not active on init.",cellIsActive(grid,0,0));
  grid.getHeaderCell(0,3).click();
  assertTrue("Body cell 0, 0 is not active after click on header.",cellIsActive(grid,0,0));
}
