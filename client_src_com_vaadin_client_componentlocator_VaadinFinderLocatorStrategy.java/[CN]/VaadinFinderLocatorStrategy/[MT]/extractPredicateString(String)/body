{
  int ixOpenBracket=indexOfIgnoringQuotes(pathFragment,'[');
  if (ixOpenBracket >= 0) {
    int ixCloseBracket=indexOfIgnoringQuotes(pathFragment,']',ixOpenBracket);
    return pathFragment.substring(ixOpenBracket + 1,ixCloseBracket);
  }
  return "";
}
