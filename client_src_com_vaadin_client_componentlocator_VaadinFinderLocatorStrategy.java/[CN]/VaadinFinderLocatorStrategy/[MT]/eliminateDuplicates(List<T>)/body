{
  int l=list.size();
  for (int j=0; j < l; ++j) {
    T ref=list.get(j);
    for (int i=j + 1; i < l; ++i) {
      if (list.get(i) == ref) {
        list.remove(i);
        --i;
        --l;
      }
    }
  }
  return list;
}
