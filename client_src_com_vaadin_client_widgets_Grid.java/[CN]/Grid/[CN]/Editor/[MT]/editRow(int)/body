{
  Cell focusedCell=grid.cellFocusHandler.getFocusedCell();
  if (focusedCell != null && grid.getElement().isOrHasChild(WidgetUtil.getFocusedElement())) {
    editRow(rowIndex,focusedCell.getColumn());
  }
 else {
    editRow(rowIndex,-1);
  }
}
