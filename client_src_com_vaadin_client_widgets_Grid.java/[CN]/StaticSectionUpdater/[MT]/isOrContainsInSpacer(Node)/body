{
  Node n=node;
  while (n != null && n != getElement()) {
    if (Element.is(n) && Element.as(n).getClassName().equals(getStylePrimaryName() + "-spacer")) {
      return true;
    }
    n=n.getParentNode();
  }
  return false;
}
