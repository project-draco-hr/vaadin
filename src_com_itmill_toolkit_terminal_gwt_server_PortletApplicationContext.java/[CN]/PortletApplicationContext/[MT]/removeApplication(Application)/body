{
  portletListeners.remove(application);
  for (Iterator it=portletToApplication.keySet().iterator(); it.hasNext(); ) {
    Object key=it.next();
    if (key == application) {
      portletToApplication.remove(key);
    }
  }
  super.removeApplication(application);
}
