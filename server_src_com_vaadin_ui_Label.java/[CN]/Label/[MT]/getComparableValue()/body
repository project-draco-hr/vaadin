{
  String stringValue=getValue();
  if (stringValue == null) {
    stringValue="";
  }
  if (getContentMode() == ContentMode.XHTML || getContentMode() == ContentMode.XML || getContentMode() == ContentMode.HTML) {
    return stripTags(stringValue);
  }
 else {
    return stringValue;
  }
}
