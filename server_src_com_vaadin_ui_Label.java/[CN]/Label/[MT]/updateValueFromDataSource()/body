{
  String newConvertedValue=getDataSourceValue();
  if (!SharedUtil.equals(newConvertedValue,((LabelState)getState(false)).text)) {
    getState().text=newConvertedValue;
    fireValueChange();
  }
}
