{
  logger.warning("You are using Label.toString() to get the value for a " + getClass().getSimpleName() + ".  This will not be supported starting from Vaadin 7.1 (your debugger might call toString() and cause this message to appear).");
  return getValue();
}
