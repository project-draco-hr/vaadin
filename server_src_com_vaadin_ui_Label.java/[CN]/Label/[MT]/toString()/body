{
  logger.warning("You are using Label.toString() to get the value for a " + getClass().getSimpleName() + ". This is not recommended and will not be supported in future versions.");
  return getValue();
}
