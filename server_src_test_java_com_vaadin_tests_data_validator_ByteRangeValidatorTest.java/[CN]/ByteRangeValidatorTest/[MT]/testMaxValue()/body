{
  assertTrue("Validator without ranges didn't accept value",cleanValidator.isValid((byte)112));
  assertTrue("Didn't accept valid value",maxValidator.isValid((byte)15));
  assertFalse("Accepted too large value",maxValidator.isValid((byte)120));
}
