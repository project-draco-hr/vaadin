{
  openTestURL();
  driver.findElement(By.cssSelector(".v-window-contents .v-scrollable")).sendKeys(Keys.ESCAPE);
  assertTrue("Uncloseable Window should remain open after esc is pressed.",isWindowOpen());
}
