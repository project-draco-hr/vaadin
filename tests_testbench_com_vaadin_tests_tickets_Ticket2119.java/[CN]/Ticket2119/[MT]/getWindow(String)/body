{
  if (!isRunning()) {
    return null;
  }
  Root w=super.getWindow(name);
  if (w == null) {
    w=createWindow();
    addWindow(w);
    w.open(new ExternalResource(getWindowUrl(w)));
  }
  return w;
}
