{
  String filename=getFileSystemName(strName);
  if (filename != null && getFileSize(strName) > 0 && fileOutPutDirectory != null)   return new File(fileOutPutDirectory,filename);
 else   return null;
}
