{
  StringBuilder builder=new StringBuilder();
  builder.append("(?<![\\w-])").append(Pattern.quote(sub)).append("(?![\\w-])");
  Pattern pattern=Pattern.compile(builder.toString());
  Matcher matcher=pattern.matcher(text);
  return matcher.find();
}
