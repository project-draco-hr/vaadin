{
  StringBuilder builder=new StringBuilder();
  builder.append("\\$").append(Pattern.quote(varName)).append("(?![\\w-])");
  Pattern pattern=Pattern.compile(builder.toString());
  Matcher matcher=pattern.matcher(text);
  return matcher.find();
}
