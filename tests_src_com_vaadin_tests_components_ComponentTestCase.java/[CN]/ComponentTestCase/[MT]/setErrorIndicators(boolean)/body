{
  for (  T c : getTestComponents()) {
    if (c == null) {
      continue;
    }
    if (on) {
      c.setComponentError(new UserError("It failed!"));
    }
 else {
      c.setComponentError(null);
    }
  }
}
