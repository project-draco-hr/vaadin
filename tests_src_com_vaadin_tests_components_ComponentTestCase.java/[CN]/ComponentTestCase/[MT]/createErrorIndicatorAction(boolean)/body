{
  return createCheckboxAction("Error indicators",initialState,new Command<T,Boolean>(){
    public void execute(    T c,    Boolean enabled){
      if (enabled) {
        c.setComponentError(new UserError("It failed!"));
      }
 else {
        c.setComponentError(null);
      }
    }
  }
);
}
