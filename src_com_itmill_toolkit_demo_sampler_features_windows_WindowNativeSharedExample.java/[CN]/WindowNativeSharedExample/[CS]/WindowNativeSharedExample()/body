{
  setSpacing(true);
  b1=new Button("Create/open a new native window with shared state",this,"openButtonClick");
  addComponent(b1);
  l=new Label("Amount of windows in memory:");
  addComponent(l);
  b2=new Button("Refresh",this,"refreshButtonClick");
  addComponent(b2);
}
