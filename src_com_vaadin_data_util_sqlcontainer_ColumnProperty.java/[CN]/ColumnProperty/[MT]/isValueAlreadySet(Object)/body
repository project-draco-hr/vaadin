{
  if (isModified()) {
    return (isNullable() && newValue == null && changedValue == null) || newValue.equals(changedValue);
  }
  return (isNullable() && newValue == null && value == null) || newValue.equals(value);
}
