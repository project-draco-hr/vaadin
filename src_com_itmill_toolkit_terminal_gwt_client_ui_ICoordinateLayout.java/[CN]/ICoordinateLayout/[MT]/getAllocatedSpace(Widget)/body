{
  Element area=componentToArea.get(child);
  if (area != null) {
    return new RenderSpace(area.getOffsetWidth(),area.getOffsetHeight());
  }
 else {
    return new RenderSpace(layout[WIDTH],layout[HEIGHT]);
  }
}
