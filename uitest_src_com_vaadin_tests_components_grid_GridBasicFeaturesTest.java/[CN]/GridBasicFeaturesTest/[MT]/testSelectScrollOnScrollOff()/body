{
  openTestURL();
  setSelectionModelMulti();
  assertFalse("row shouldn't start out as selected",isSelected(getRow(0)));
  scrollGridVerticallyTo(10000);
  toggleFirstRowSelection();
  scrollGridVerticallyTo(0);
  assertTrue("row should still be selected when scrolling " + "back into view",isSelected(getRow(0)));
  toggleFirstRowSelection();
  assertFalse("row shouldn't remain selected",isSelected(getRow(0)));
}
