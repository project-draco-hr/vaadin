{
  openTestURL();
  GridElement grid=getGridElement();
  sortBy("Column9, DESC");
  String row="";
  for (int i=0; i < 3; ++i) {
    row+="9";
    assertEquals("Grid is not sorted by Column9 using descending direction.","(" + row + ", 0)",grid.getCell(i,0).getText());
  }
  sortBy("Column10, ASC");
  grid.scrollToRow(50);
  assertFalse("Scrolling caused and exception when shuffled.",getLogRow(0).contains("Exception"));
  for (int i=0; i < 5; ++i) {
    assertGreater("Grid is not sorted by Column10 using ascending direction",Integer.parseInt(grid.getCell(i + 1,10).getText()),Integer.parseInt(grid.getCell(i,10).getText()));
  }
  sortBy("Column7, DESC");
  for (int i=0; i < 3; ++i) {
    assertEquals("Grid is not sorted by Column7 using descending direction","(" + i + ", 0)",grid.getCell(GridBasicFeatures.ROWS - (i + 1),0).getText());
  }
}
