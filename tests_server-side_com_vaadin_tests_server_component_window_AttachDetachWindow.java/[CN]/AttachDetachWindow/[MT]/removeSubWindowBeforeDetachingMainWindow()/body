{
  testApp.addWindow(main);
  main.addWindow(sub);
  main.removeWindow(sub);
  assertAttached(main);
  assertDetached(sub);
  testApp.removeWindow(main);
  assertDetached(main);
  assertDetached(sub);
}
