{
  main.setApplication(testApp);
  main.addWindow(sub);
  main.removeWindow(sub);
  assertAttached(main);
  assertDetached(sub);
  main.setApplication(null);
  assertDetached(main);
  assertDetached(sub);
}
