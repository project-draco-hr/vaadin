{
  assertUnattached(main);
  assertUnattached(sub);
  main.setApplication(testApp);
  assertAttached(main);
  assertUnattached(sub);
  main.addWindow(sub);
  assertAttached(main);
  assertAttached(sub);
}
