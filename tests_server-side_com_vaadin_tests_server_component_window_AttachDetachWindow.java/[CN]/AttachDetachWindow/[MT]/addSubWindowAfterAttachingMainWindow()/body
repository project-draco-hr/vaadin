{
  assertUnattached(main);
  assertUnattached(sub);
  testApp.setMainWindow(main);
  assertAttached(main);
  assertUnattached(sub);
  main.addWindow(sub);
  assertAttached(main);
  assertAttached(sub);
}
