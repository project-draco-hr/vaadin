{
  testApp.addWindow(main);
  main.addWindow(sub);
  testApp.removeWindow(main);
  assertDetached(main);
  assertDetached(sub);
  main.removeWindow(sub);
  assertDetached(main);
  assertDetached(sub);
}
