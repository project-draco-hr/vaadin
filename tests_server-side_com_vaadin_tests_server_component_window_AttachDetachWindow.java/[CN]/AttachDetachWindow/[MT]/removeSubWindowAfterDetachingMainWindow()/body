{
  main.setApplication(testApp);
  main.addWindow(sub);
  main.setApplication(null);
  assertDetached(main);
  assertDetached(sub);
  main.removeWindow(sub);
  assertDetached(main);
  assertDetached(sub);
}
