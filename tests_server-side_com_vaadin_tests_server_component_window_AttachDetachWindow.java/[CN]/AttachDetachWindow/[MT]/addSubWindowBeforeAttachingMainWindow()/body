{
  assertUnattached(main);
  assertUnattached(sub);
  main.addWindow(sub);
  assertUnattached(main);
  assertUnattached(sub);
  main.setApplication(testApp);
  assertAttached(main);
  assertAttached(sub);
}
