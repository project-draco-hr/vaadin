{
  AtmospherePushConnection connection=new AtmospherePushConnection(null,resource);
  try {
    connection.sendMessage(VaadinService.createCriticalNotificationJSON(null,null,null,null));
  }
  finally {
    connection.disconnect();
  }
}
