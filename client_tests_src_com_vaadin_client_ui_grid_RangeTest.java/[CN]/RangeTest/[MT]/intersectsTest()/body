{
  assertTrue("[0..10[ intersects [5..15[",Range.between(0,10).intersects(Range.between(5,15)));
  assertTrue("[0..10[ does not intersect [10..20[",!Range.between(0,10).intersects(Range.between(10,20)));
  assertTrue("empty range does not intersect with [0..10[",!Range.between(5,5).intersects(Range.between(0,10)));
  assertTrue("[0..10[ does not intersect with empty range",!Range.between(0,10).intersects(Range.between(5,5)));
}
