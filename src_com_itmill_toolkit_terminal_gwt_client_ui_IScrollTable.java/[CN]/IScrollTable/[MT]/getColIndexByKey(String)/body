{
  if ("0".equals(colKey))   return 0;
  int index=-1;
  for (int i=0; i < columnOrder.length; i++) {
    if (columnOrder[i].equals(colKey)) {
      index=i;
      break;
    }
  }
  if (rowHeaders)   index++;
  return index;
}
