{
  columnCaptions.clear();
  Iterator it=c.getChildIterator();
  while (it.hasNext()) {
    UIDL col=(UIDL)it.next();
    columnCaptions.put(col.getStringAttribute("cid"),col.getStringAttribute("caption"));
  }
}
