{
  if (BrowserInfo.get().isIE6()) {
    return Util.measureHorizontalBorder(bodyContainer.getElement());
  }
  return bodyContainer.getOffsetWidth() - DOM.getElementPropertyInt(bodyContainer.getElement(),"clientWidth");
}
