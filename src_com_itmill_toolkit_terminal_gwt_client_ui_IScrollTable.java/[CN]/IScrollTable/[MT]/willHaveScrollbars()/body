{
  if (!(height != null && !height.equals(""))) {
    if (pageLength < totalRows) {
      return true;
    }
  }
 else {
    int fakeheight=tBody.getRowHeight() * totalRows;
    int availableHeight=bodyContainer.getElement().getPropertyInt("clientHeight");
    if (fakeheight > availableHeight) {
      return true;
    }
  }
  return false;
}
