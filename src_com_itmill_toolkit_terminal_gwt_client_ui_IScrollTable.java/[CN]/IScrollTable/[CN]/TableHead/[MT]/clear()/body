{
  for (Iterator iterator=availableCells.keySet().iterator(); iterator.hasNext(); ) {
    String cid=(String)iterator.next();
    removeCell(cid);
  }
  availableCells.clear();
  availableCells.put("0",new RowHeadersHeaderCell());
}
