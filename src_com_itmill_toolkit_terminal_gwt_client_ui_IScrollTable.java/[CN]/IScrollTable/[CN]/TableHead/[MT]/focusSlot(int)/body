{
  removeSlotFocus();
  if (index > 0)   DOM.setStyleAttribute(DOM.getFirstChild(DOM.getChild(tr,index - 1)),"borderRight","2px solid black");
 else   DOM.setStyleAttribute(DOM.getFirstChild(DOM.getChild(tr,index)),"borderLeft","2px solid black");
  focusedSlot=index;
}
