{
  final StringBuffer buf=new StringBuffer();
  if (collapsed) {
    buf.append("<span class=\"i-off\">");
  }
  buf.append(super.getHTML());
  if (collapsed) {
    buf.append("</span>");
  }
  return buf.toString();
}
