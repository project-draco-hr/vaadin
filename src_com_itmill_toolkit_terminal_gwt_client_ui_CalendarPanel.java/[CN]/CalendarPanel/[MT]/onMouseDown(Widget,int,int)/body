{
  if (sender instanceof IEventButton) {
    processClickEvent(sender,false);
    timer=new Timer(){
      public void run(){
        processClickEvent(sender,false);
      }
    }
;
    timer.scheduleRepeating(100);
  }
}
