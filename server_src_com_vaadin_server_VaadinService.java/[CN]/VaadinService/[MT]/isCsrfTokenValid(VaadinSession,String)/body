{
  if (session.getService().getDeploymentConfiguration().isXsrfProtectionEnabled()) {
    String keyInSession=(String)session.getSession().getAttribute(ApplicationConstants.UIDL_SECURITY_TOKEN_ID);
    if (keyInSession == null || !keyInSession.equals(requestToken)) {
      return false;
    }
  }
  return true;
}
