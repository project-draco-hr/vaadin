{
  if (isSessionActive(session)) {
    closeInactiveUIs(session);
    removeClosedUIs(session);
  }
 else {
    getLogger().fine("Closed inactive session " + session.getSession().getId());
    closeSession(session);
  }
}
