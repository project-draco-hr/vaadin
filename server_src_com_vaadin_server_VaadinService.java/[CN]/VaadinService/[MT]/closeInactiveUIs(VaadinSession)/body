{
  String sessionId=session.getSession().getId();
  for (  UI ui : session.getUIs()) {
    if (!isUIActive(ui) && !ui.isClosing()) {
      getLogger().fine("Closing inactive UI #" + ui.getUIId() + " in session "+ sessionId);
      ui.close();
    }
  }
}
