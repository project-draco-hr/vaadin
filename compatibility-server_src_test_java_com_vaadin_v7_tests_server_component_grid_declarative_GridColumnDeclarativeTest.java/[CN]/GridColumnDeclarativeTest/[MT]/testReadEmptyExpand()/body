{
  String design="<vaadin7-grid><table>" + "<colgroup>" + "   <col sortable=true expand />"+ "</colgroup>"+ "</table></vaadin7-grid>";
  Grid grid=new Grid();
  grid.addColumn("property-0",String.class).setExpandRatio(1);
  testRead(design,grid);
}
