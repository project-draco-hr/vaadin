{
  Table table=new Table(caption);
  table.addContainerProperty("value",String.class,"");
  for (int i=0; i < 10; i++) {
    table.addItem(new Object[]{caption + " value " + i},Integer.valueOf(i));
  }
  table.setWidth("300px");
  return table;
}
