{
  String uri=ApplicationConstants.APP_PROTOCOL_PREFIX + ApplicationConstants.APP_REQUEST_PATH + ConnectorResource.CONNECTOR_REQUEST_PATH+ connector.getUI().getUIId()+ '/'+ connector.getConnectorId()+ '/'+ encodeFileName(filename);
  return uri;
}
