{
  Connector connector=client.getConnectorMap().getConnector(getConnectorId());
  if (connector instanceof ComponentConnector) {
    return (ComponentConnector)connector;
  }
 else {
    return null;
  }
}
