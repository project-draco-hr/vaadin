{
  return blackPlayer + " (Black) vs. " + whitePlayer+ " (White) ("+ state.length+ "x"+ state[0].length+ ", "+ moves+ " moves done"+ (captured[WHITE] > 0 ? (", Black has captured " + captured[WHITE] + " stones") : "")+ (captured[BLACK] > 0 ? (", White has captured " + captured[BLACK] + " stones") : "")+ ")";
}
