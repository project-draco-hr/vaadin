{
  if (state[i][j] == EMPTY) {
    state[i][j]=isBlack ? BLACK : WHITE;
    moves++;
    removeDeadStonesAround(i,j);
    for (Iterator li=listeners.iterator(); li.hasNext(); )     ((Game.Listener)li.next()).stoneIsAdded(this,i,j,isBlack);
  }
}
