{
synchronized (users) {
    messages.add(new String[]{new String((String)username.getValue()),new String((String)message.getValue())});
    for (Iterator i=users.iterator(); i.hasNext(); )     ((ChatApplication)i.next()).updateTable();
  }
}
