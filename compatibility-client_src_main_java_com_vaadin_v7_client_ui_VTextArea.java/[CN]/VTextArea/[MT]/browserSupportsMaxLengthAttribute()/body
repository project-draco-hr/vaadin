{
  BrowserInfo info=BrowserInfo.get();
  if (info.isFirefox()) {
    return true;
  }
  if (info.isSafari()) {
    return true;
  }
  if (info.isIE11() || info.isEdge()) {
    return true;
  }
  if (info.isAndroid()) {
    return true;
  }
  return false;
}
