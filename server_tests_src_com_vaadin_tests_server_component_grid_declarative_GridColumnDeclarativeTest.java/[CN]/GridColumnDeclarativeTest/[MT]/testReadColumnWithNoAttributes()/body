{
  String design="<vaadin-grid><table>" + "<colgroup>" + "   <col />"+ "</colgroup>"+ "</table></vaadin-grid>";
  Grid grid=new Grid();
  grid.addColumn("property-0",String.class);
  testRead(design,grid);
}
