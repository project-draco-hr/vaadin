{
  if (getFooter() != null) {
    getFooter().setParent(null);
  }
  if (footer == null) {
    footer=new HorizontalLayout();
  }
  getState().setFooter(footer);
  footer.setParent(this);
  requestRepaint();
}
