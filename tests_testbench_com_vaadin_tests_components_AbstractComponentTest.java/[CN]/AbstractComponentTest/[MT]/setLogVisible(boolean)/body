{
  if (visible) {
    log.setHeight(null);
    log.setWidth(null);
    log.setCaption((String)log.getData());
  }
 else {
    log.setHeight("0px");
    log.setWidth("0px");
    log.setCaption(null);
  }
}
