{
  buildTime(redraw || resolution != datefield.currentResolution || readonly != datefield.readonly);
  if (datefield instanceof ITextualDate)   ((ITextualDate)datefield).buildDate();
  resolution=datefield.currentResolution;
  readonly=datefield.readonly;
}
