{
  verifySidebarNotVisible();
  toggleHidableColumn(3);
  toggleHidableColumn(4);
  verifySidebarVisible();
  toggleHidableColumn(3);
  verifySidebarVisible();
  toggleHidableColumn(4);
  verifySidebarNotVisible();
}
