{
  setSpacing(true);
  b1=new Button("Create and open a new child window with its own state",this,"openButtonClick");
  addComponent(b1);
  l=new Label("Amount of child windows attached to the main window: x");
  addComponent(l);
  b2=new Button("Refresh",this,"refreshButtonClick");
  addComponent(b2);
}
