{
  Window w=new Window("New child window");
  Label desc=new Label("This is a new child window with its own state." + " The child window is added to the main window" + " instead of the application.");
  w.addComponent(desc);
  w.addListener(this);
  w.setResizable(false);
  getApplication().getMainWindow().addWindow(w);
}
