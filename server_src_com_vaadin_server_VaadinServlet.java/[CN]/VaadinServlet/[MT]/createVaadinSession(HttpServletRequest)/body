{
  VaadinServletSession session=new VaadinServletSession();
  try {
    ServletPortletHelper.initDefaultUIProvider(session,getVaadinService());
  }
 catch (  ApplicationClassException e) {
    throw new ServletException(e);
  }
  return session;
}
