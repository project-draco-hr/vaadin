{
  if ("/".equals(request.getPathInfo()) && "".equals(request.getServletPath()) && !request.getRequestURI().endsWith("/")) {
    response.setStatus(HttpServletResponse.SC_FOUND);
    response.setHeader("Location",request.getRequestURI() + "/");
    return true;
  }
 else {
    return false;
  }
}
