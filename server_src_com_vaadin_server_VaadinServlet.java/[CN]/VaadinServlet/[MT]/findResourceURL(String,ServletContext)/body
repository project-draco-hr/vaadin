{
  URL resourceUrl=sc.getResource(filename);
  if (resourceUrl == null) {
    if (filename.startsWith("/")) {
      filename=filename.substring(1);
    }
    resourceUrl=getVaadinService().getClassLoader().getResource(filename);
  }
  return resourceUrl;
}
