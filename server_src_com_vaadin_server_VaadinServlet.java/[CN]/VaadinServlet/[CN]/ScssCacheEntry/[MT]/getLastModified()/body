{
  long newest=0;
  for (  String uri : sourceUris) {
    File file=new File(uri);
    if (!file.exists()) {
      return -1;
    }
 else {
      newest=Math.max(newest,file.lastModified());
    }
  }
  return newest;
}
