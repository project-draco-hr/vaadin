{
  RequestType type=(RequestType)request.getAttribute(RequestType.class.getName());
  if (type == null) {
    type=getServlet().getRequestType(VaadinServletRequest.cast(request));
    request.setAttribute(RequestType.class.getName(),type);
  }
  return type;
}
