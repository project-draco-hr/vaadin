{
  boolean hasIndicator=false;
  List<WebElement> e=checkbox.findElements(By.className("v-errorindicator"));
  if (e.size() != 0) {
    hasIndicator=e.get(0).isDisplayed();
  }
  Assert.assertEquals("Checkbox state should be " + (valid ? "valid" : "invalid"),valid,!hasIndicator);
}
