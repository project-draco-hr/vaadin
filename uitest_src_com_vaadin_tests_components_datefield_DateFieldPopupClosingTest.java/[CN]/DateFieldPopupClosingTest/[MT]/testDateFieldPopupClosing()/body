{
  openTestURL();
  for (int i=0; i < N; i++) {
    clickDateDatePickerButton();
    waitUntil(ExpectedConditions.visibilityOfElementLocated(By.className("v-datefield-popup")));
    clickDateDatePickerButton();
    waitUntil(ExpectedConditions.invisibilityOfElementLocated(By.className("v-datefield-popup")));
  }
}
