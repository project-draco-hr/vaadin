{
  int spanWidth=0;
  for (int ix=(showRowHeaders ? 1 : 0); ix < tHead.getVisibleCellCount(); ix++) {
    spanWidth+=tHead.getHeaderCell(ix).getOffsetWidth();
  }
  Util.setWidthExcludingPaddingAndBorder((com.google.gwt.user.client.Element)getElement().getChild(cellIx),spanWidth,13,false);
}
