{
  int upcomingScrollLeft=scrollLeft;
  if (lastScrollLeftWasAtMax) {
    upcomingScrollLeft=Util.getMaxScrollLeft(scrollBodyPanel.getElement());
  }
  scrollBodyPanel.getElement().setScrollLeft(upcomingScrollLeft);
}
