{
  if (features.getFeatureByPath(name) != null) {
    return null;
  }
 else {
    Window w=super.getWindow(name);
    if (w == null) {
      w=new SamplerWindow();
      w.setName(name);
      addWindow(w);
    }
    return w;
  }
}
