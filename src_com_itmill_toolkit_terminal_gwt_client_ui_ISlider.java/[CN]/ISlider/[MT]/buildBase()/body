{
  if (vertical) {
  }
 else {
    if (size > -1)     DOM.setStyleAttribute(getElement(),"width",size + "px");
 else {
      Element p=DOM.getParent(getElement());
      if (Integer.parseInt(DOM.getElementProperty(p,"offsetWidth")) > 50)       DOM.setStyleAttribute(getElement(),"width","auto");
 else {
        DOM.setStyleAttribute(getElement(),"width","50px");
        DeferredCommand.addCommand(new Command(){
          public void execute(){
            Element p=DOM.getParent(getElement());
            if (Integer.parseInt(DOM.getElementProperty(p,"offsetWidth")) > 50)             DOM.setStyleAttribute(getElement(),"width","auto");
          }
        }
);
      }
    }
  }
  DOM.setStyleAttribute(base,"position","relative");
}
