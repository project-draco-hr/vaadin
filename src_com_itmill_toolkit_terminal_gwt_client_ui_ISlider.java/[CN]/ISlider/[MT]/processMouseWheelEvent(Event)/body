{
  int dir=DOM.eventGetMouseWheelVelocityY(event);
  if (dir < 0)   increaseValue(event,false);
 else   decreaseValue(event,false);
  DOM.eventPreventDefault(event);
  DOM.eventCancelBubble(event,true);
}
