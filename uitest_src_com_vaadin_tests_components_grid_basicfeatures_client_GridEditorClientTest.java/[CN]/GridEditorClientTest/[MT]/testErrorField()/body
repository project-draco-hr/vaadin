{
  selectMenuPath(EDIT_ROW_5);
  assertTrue("No errors should be present",getEditor().findElements(By.className("error")).isEmpty());
  selectMenuPath("Component","Editor","Toggle second editor error");
  getSaveButton().click();
  assertEquals("Unexpected amount of error fields",1,getEditor().findElements(By.className("error")).size());
}
