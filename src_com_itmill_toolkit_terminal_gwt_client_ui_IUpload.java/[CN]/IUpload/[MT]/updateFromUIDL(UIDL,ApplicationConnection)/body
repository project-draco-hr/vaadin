{
  if (client.updateComponent(this,uidl,false))   return;
  this.client=client;
  this.paintableId=uidl.getId();
  setAction(client.getAppUri());
  if (uidl.hasAttribute("caption"))   b.setText(uidl.getStringAttribute("caption"));
  fu.setName(paintableId + "_file");
}
