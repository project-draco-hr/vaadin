{
  if (client != null) {
    if (t != null)     t.cancel();
    ApplicationConnection.getConsole().log("Submit complete");
    client.sendPendingVariableChanges();
  }
  submitted=false;
  enableUploaod();
}
