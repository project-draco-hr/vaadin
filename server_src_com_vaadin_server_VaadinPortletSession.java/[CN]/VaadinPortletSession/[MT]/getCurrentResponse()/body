{
  WrappedPortletResponse currentResponse=(WrappedPortletResponse)CurrentInstance.get(WrappedResponse.class);
  if (currentResponse != null) {
    return currentResponse.getPortletResponse();
  }
 else {
    return null;
  }
}
