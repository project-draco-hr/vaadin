{
  assertFalse("field should not be immediate by default",field.isImmediate());
  field.setRequired(true);
  field.setRequired(false);
  assertFalse("field should not be immediate even though it was required",field.isImmediate());
}
