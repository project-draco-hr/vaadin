{
  if (selectedRows.size() > 0) {
    SelectionChangeEvent<T> event=new SelectionChangeEvent<T>(grid,null,getSelectedRows());
    selectedRows.clear();
    grid.fireEvent(event);
    return true;
  }
  return false;
}
