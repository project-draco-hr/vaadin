{
  if (selectedRows.size() > 0) {
    SelectionChangeEvent<T> event=new SelectionChangeEvent<T>(grid,null,selectedRows);
    selectedRows.clear();
    grid.fireEvent(event);
    return true;
  }
  return false;
}
