{
  if (BrowserInfo.get().isIE() && event.getNativeKeyCode() == KeyCodes.KEY_ENTER) {
    valueChange(false);
  }
 else   if (BrowserInfo.get().isFirefox() && event.getNativeKeyCode() == KeyCodes.KEY_ESCAPE && getText().equals("")) {
    possibleInputError=true;
  }
}
