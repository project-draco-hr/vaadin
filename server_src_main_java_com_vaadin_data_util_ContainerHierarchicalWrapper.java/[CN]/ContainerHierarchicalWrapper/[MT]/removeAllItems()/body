{
  final boolean success=container.removeAllItems();
  if (!hierarchical && success) {
    roots.clear();
    parent.clear();
    children.clear();
    noChildrenAllowed.clear();
  }
  return success;
}
