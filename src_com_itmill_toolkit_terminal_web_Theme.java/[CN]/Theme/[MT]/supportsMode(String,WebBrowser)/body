{
  RequirementCollection rc=(RequirementCollection)supportedModes.get(mode);
  if (rc == null || !rc.isMet(terminal))   return false;
  for (Iterator i=parentThemes.iterator(); i.hasNext(); )   if (!((Theme)i.next()).supportsMode(mode,terminal))   return false;
  return true;
}
