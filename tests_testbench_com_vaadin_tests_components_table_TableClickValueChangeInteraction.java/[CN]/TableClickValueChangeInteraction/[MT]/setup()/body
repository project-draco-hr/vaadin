{
  setMainWindow(mainWindow);
  GridLayout layout=new GridLayout(4,4);
  layout.setSpacing(true);
  layout.setMargin(true);
  mainWindow.setContent(layout);
  for (int i=0; i < 16; ++i) {
    mainWindow.addComponent(makeTable((i & 8) > 0,(i & 4) > 0,(i & 2) > 0,(i & 1) > 0));
  }
}
