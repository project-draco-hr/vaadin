{
  final Caption c=widgetToCaption.get(paintable.getWidgetForPaintable());
  if (c != null) {
    c.updateCaption(uidl);
  }
  final ErrorFlag e=widgetToError.get(paintable.getWidgetForPaintable());
  if (e != null) {
    e.updateFromUIDL(uidl,paintable);
  }
}
