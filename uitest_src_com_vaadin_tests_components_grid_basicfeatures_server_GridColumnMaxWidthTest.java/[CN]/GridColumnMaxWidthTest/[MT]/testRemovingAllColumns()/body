{
  setDebug(true);
  openTestURL();
  selectMenuPath("Component","Columns","All columns expanding, Col 0 has max width of 30px");
  assertEquals("Column 0 did not obey max width of 30px.","30px",getGridElement().getCell(0,0).getCssValue("width"));
}
