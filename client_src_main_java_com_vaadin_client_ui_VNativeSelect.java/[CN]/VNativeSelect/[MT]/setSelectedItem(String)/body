{
  if (value == null) {
    setSelectedIndex(-1);
  }
 else {
    for (int i=0; i < getItemCount(); i++) {
      if (Objects.equals(value,getValue(i))) {
        setSelectedIndex(i);
        break;
      }
    }
  }
}
