{
  UserError userError=null;
  if (value) {
    userError=new UserError("Der eingegebene Wert ist nicht zul??ssig!");
  }
  for (  AbstractField<?> c : components) {
    c.setComponentError(userError);
  }
}
