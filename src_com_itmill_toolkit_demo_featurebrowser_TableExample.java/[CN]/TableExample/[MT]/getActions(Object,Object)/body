{
  if (sender == source) {
    final Item item=source.getItem(target);
    if (item != null && item.getItemProperty(PROPERTY_HIRED).getValue() == Boolean.FALSE) {
      return ACTIONS_HIRE;
    }
 else {
      return ACTIONS_NOHIRE;
    }
  }
 else {
    return new Action[]{ACTION_DELETE};
  }
}
