{
  if (getParent() == null) {
    return getState().isVisible();
  }
 else {
    return getState().isVisible() && getParent().isVisible() && ((HasComponents)getParent()).isComponentVisible(this);
  }
}
