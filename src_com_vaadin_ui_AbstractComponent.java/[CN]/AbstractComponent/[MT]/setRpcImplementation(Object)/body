{
  if (this instanceof RpcTarget) {
    rpcManager=new ServerRpcManager((RpcTarget)this,implementation);
  }
 else {
    throw new RuntimeException("Cannot register an RPC implementation for a component that is not an RpcTarget");
  }
}
