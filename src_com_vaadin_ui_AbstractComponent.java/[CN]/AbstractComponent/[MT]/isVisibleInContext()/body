{
  HasComponents p=getParent();
  while (p != null) {
    if (!p.isVisible()) {
      return false;
    }
    p=p.getParent();
  }
  if (getParent() != null && !getParent().isComponentVisible(this)) {
    return false;
  }
  return isVisible();
}
