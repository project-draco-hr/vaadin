{
  Window w=new Window();
  w.addComponent(new Label("<b>Frame: " + (++count) + "</b>",Label.CONTENT_UIDL));
  w.addComponent(new Button("Split",this));
  w.addComponent(new Button("Remove",this));
  windowToFramesetMap.put(w,fs);
  return w;
}
