{
  String targetUrl=cm.getStreamVariableTargetUrl(owner,variableName,streamVariable);
  assertTrue(targetUrl.startsWith("app://APP/UPLOAD/PID0/myName/"));
  StreamVariable streamVariable2=cm.getStreamVariable(cm.getPaintableId(owner),variableName);
  assertSame(streamVariable,streamVariable2);
}
