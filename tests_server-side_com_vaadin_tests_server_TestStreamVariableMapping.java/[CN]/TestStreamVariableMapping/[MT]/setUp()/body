{
  final Application application=new Application();
  final UI uI=new UI(){
    @Override protected void init(    WrappedRequest request){
    }
    @Override public Application getApplication(){
      return application;
    }
  }
;
  owner=new Upload(){
    @Override public UI getRoot(){
      return uI;
    }
  }
;
  streamVariable=EasyMock.createMock(StreamVariable.class);
  cm=createCommunicationManager();
  super.setUp();
}
