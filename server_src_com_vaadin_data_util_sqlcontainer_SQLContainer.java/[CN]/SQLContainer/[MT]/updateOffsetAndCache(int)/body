{
  if (itemIndexes.containsKey(index)) {
    return;
  }
  currentOffset=(index / (pageLength * CACHE_RATIO)) * (pageLength * CACHE_RATIO);
  if (currentOffset < 0) {
    currentOffset=0;
  }
  getPage();
}
