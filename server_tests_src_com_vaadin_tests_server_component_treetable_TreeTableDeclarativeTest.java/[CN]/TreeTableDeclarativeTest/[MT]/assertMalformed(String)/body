{
  String design="<v-tree-table>" + "  <table>" + "    <colgroup><col property-id=''></colgroup>"+ "    <tbody>" + hierarchy + "</tbody>"+ "  </table>"+ "</v-tree-table>";
  try {
    read(design);
    Assert.fail("Malformed hierarchy should fail: " + hierarchy);
  }
 catch (  DesignException expected) {
  }
}
