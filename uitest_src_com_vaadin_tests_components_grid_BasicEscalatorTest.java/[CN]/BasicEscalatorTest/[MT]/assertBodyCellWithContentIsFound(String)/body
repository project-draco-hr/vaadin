{
  String xpath="//tbody/tr/td[.='" + cellContent + "']";
  try {
    assertNotNull("received a null element with \"" + xpath + "\"",getDriver().findElement(By.xpath(xpath)));
  }
 catch (  NoSuchElementException e) {
    fail("Could not find '" + xpath + "'");
  }
}
