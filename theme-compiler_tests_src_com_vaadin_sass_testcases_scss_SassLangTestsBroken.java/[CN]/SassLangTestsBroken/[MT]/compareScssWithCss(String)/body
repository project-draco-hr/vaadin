{
  boolean success=false;
  try {
    super.compareScssWithCss(scssResourceName);
    success=true;
  }
 catch (  CSSException e) {
  }
catch (  AssertionError e) {
  }
  if (success) {
    Assert.fail("Test " + scssResourceName + " from sasslangbroken that was expected to fail has been fixed. Please move the corresponding CSS and SCSS files to sasslang.");
  }
}
