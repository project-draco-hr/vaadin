{
  if (this.height == null && height == null)   return;
  String oldHeight=this.height;
  this.height=height;
  if ((this.height != null && height == null) || (this.height == null && height != null) || !oldHeight.equals(height)) {
    iLayout();
  }
}
