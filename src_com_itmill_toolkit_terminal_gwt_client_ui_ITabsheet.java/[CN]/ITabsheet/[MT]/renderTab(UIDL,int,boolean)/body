{
  Caption c=new Caption(null,client);
  c.updateCaption(tabUidl);
  tb.addTab(c);
  if (selected) {
    renderContent(tabUidl.getChildUIDL(0));
    tb.selectTab(index);
  }
  tp.add(new Label(""));
}
