{
  if (client != null) {
    client.runDescendentsLayout(this);
  }
  updateTabScroller();
  if (tp.getVisibleWidget() >= 0) {
    Util.runWebkitOverflowAutoFix(DOM.getParent(tp.getWidget(tp.getVisibleWidget()).getElement()));
  }
}
