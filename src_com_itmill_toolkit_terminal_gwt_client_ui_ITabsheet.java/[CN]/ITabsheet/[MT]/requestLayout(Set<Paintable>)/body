{
  if (height != null && width != null) {
    return true;
  }
  if (renderInformation.updateSize(getElement())) {
    fixVisibleTabSize();
    iLayout();
    return false;
  }
 else {
    return true;
  }
}
