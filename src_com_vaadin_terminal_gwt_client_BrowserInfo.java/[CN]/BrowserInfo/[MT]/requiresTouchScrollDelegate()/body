{
  if (!isTouchDevice()) {
    return false;
  }
  if (isAndroid() && isWebkit() && getWebkitVersion() >= 534) {
    return false;
  }
  if (isIOS() && isWebkit() && getWebkitVersion() >= 534) {
    return false;
  }
  return true;
}
