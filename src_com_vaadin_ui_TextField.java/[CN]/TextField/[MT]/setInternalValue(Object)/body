{
  if (changingVariables && !newValue.toString().equals(lastKnownTextContent)) {
    lastKnownTextContent=newValue.toString();
    textChangeEventPending=true;
    firePendingTextChangeEvent();
  }
  super.setInternalValue(newValue);
}
