{
  Object itemId=container.getIdByIndex(index);
  String[] row=getRowData(container.getContainerPropertyIds(),itemId);
  getRpcProxy(DataProviderRpc.class).setRowData(index,Collections.singletonList(row));
}
