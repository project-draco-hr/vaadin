{
  this.container=container;
  registerRpc(new DataRequestRpc(){
    @Override public void requestRows(    int firstRow,    int numberOfRows){
      pushRows(firstRow,numberOfRows);
    }
  }
);
  getState().containerSize=container.size();
}
