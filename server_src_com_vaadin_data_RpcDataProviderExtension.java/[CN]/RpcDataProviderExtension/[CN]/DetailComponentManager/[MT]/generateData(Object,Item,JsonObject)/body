{
  if (visibleDetails.contains(itemId)) {
    detailComponentManager.createDetails(itemId);
    Component detailsComponent=visibleDetailsComponents.get(itemId);
    rowData.put(GridState.JSONKEY_DETAILS_VISIBLE,(detailsComponent != null ? detailsComponent.getConnectorId() : ""));
  }
}
