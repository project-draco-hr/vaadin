{
  final GridValueChangeListener removedListener=valueChangeListeners.remove(itemId);
  if (removedListener != null) {
    activeRange=Range.withLength(activeRange.getStart(),activeRange.length() - 1);
  }
}
