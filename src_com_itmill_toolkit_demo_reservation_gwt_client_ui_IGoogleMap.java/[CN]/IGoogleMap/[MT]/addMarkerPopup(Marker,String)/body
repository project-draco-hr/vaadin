{
  marker.addMarkerClickHandler(new MarkerClickHandler(){
    public void onClick(    MarkerClickEvent event){
      widget.getInfoWindow().open(event.getSender().getPoint(),new InfoWindowContent(html));
    }
  }
);
}
