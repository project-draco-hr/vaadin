{
  if (selectedItem != null) {
    getDataCommunicator().refresh(selectedItem);
  }
  selectedItem=keyToItem(key);
  if (selectedItem != null) {
    getDataCommunicator().refresh(selectedItem);
  }
}
