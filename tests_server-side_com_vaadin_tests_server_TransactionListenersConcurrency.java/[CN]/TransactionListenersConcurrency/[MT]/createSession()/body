{
  HttpSession session=createMock(HttpSession.class);
  EasyMock.expect(session.getAttribute(WebApplicationContext.class.getName())).andReturn(null).anyTimes();
  session.setAttribute(EasyMock.eq(WebApplicationContext.class.getName()),EasyMock.anyObject());
  EasyMock.replay(session);
  return session;
}
