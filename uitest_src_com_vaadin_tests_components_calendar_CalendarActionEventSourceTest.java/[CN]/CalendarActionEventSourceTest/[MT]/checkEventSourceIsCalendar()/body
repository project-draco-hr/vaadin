{
  String calendarObject=getDriver().findElement(By.id("calendarlabel")).getText();
  String actionSourceObject=getDriver().findElement(By.id("senderlabel")).getText();
  Assert.assertEquals("Calendar action event source must be the calendar itself",calendarObject,actionSourceObject);
}
