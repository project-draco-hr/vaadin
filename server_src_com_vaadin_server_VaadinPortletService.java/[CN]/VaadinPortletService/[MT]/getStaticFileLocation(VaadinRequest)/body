{
  String staticFileLocation=getPortalProperty(request,Constants.PORTAL_PARAMETER_VAADIN_RESOURCE_PATH);
  if (staticFileLocation != null) {
    return trimTrailingSlashes(staticFileLocation);
  }
 else {
    return "/html";
  }
}
