{
  if (!allItems.remove(itemId)) {
    return false;
  }
  removeAllValueChangeListeners(getItem(itemId));
  beanToItem.remove(itemId);
  list.remove(itemId);
  fireItemSetChange();
  return true;
}
