{
  Button b=new Button("Add 100% before",new ClickListener(){
    public void buttonClick(    ClickEvent event){
      addBefore((OrderedLayout)event.getButton().getData(),event.getButton().getParent(),"100%");
    }
  }
);
  b.setData(ol);
  return b;
}
