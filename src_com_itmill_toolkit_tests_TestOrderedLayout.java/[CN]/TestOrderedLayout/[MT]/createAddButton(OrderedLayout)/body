{
  Button b=new Button("Add before",new ClickListener(){
    public void buttonClick(    ClickEvent event){
      addBefore((OrderedLayout)event.getButton().getData(),event.getButton().getParent(),"");
    }
  }
);
  b.setData(ol);
  return b;
}
