{
  try {
    update("DROP TABLE " + Reservation.TABLE);
  }
 catch (  final SQLException IGNORED) {
  }
  try {
    update("DROP TABLE " + Resource.TABLE);
  }
 catch (  final SQLException IGNORED) {
  }
}
