{
  try {
    Class<?> c=Class.forName(fullyQualifiedClassName);
    if (baseClass.isAssignableFrom(c) && !Modifier.isAbstract(c.getModifiers())) {
      result.add((Class<? extends T>)c);
    }
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
catch (  LinkageError e) {
  }
}
