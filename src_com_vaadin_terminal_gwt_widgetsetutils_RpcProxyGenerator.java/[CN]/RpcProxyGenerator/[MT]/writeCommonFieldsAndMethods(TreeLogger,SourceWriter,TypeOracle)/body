{
  JClassType applicationConnectionClass=typeOracle.findType(ApplicationConnection.class.getCanonicalName());
  writer.println("private " + ServerConnector.class.getName() + " connector;");
  writer.println("public void initRpc(" + ServerConnector.class.getName() + " connector) {");
  writer.indent();
  writer.println("this.connector = connector;");
  writer.outdent();
  writer.println("}");
}
