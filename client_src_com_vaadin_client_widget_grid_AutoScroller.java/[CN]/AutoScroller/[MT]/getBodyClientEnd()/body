{
  if (scrollDirection == ScrollAxis.VERTICAL) {
    return getClientTop(getTfootElement()) - 1;
  }
 else {
    TableSectionElement tbodyElement=getTbodyElement();
    return getClientLeft(tbodyElement) + tbodyElement.getOffsetWidth() - 1;
  }
}
