{
  Element cursor=grid.getElement();
  int scroll=0;
  while (cursor != null) {
    scroll-=scrollDirection == ScrollAxis.VERTICAL ? cursor.getScrollTop() : cursor.getScrollLeft();
    cursor=cursor.getParentElement();
  }
  return scroll;
}
