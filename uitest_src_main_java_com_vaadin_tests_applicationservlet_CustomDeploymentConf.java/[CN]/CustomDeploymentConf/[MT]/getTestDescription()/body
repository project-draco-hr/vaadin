{
  return "Demonstrates the @" + CustomDeploymentConfiguration.class.getSimpleName() + " feature that allows customizing the effective deployment configuration for test UIs run through "+ ApplicationRunnerServlet.class.getSimpleName()+ ".";
}
