{
  return createCheckboxAction("Required",initialState,new Command<PopupDateField,Boolean>(){
    public void execute(    PopupDateField c,    Boolean enabled){
      c.setRequired(enabled);
    }
  }
);
}
