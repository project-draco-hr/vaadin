{
  return createCheckboxAction("Enabled",initialState,new Command<PopupDateField,Boolean>(){
    public void execute(    PopupDateField c,    Boolean enabled){
      c.setEnabled(enabled);
    }
  }
);
}
