{
  return createCheckboxAction("Readonly",initialState,new Command<PopupDateField,Boolean>(){
    public void execute(    PopupDateField c,    Boolean enabled){
      c.setReadOnly(enabled);
    }
  }
);
}
