{
  if (!Objects.equals(item,selectedItem)) {
    if (selectedItem != null) {
      refresh(selectedItem);
    }
    selectedItem=item;
    if (selectedItem != null) {
      refresh(selectedItem);
    }
    fireEvent(new SingleSelectionChange<>(getParent(),selectedItem,userOriginated));
  }
}
