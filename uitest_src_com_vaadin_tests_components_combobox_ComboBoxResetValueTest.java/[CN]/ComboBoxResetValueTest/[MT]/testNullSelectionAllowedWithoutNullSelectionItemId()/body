{
  openTestURL();
  ComboBoxElement comboBoxWebElement=$(ComboBoxElement.class).id(ComboBoxResetValue.NULL_SELECTION_ALLOWED_WITHOUT_NULL_SELECTION_ITEM_ID);
  clickResetButton();
  openPopup(comboBoxWebElement);
  assertEquals("There should be no selection",null,getSelectedInPopupValue());
}
