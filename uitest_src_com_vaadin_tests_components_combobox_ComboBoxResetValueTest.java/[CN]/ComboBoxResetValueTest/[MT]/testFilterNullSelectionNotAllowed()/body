{
  openTestURL();
  ComboBoxElement comboBoxWebElement=$(ComboBoxElement.class).id(ComboBoxResetValue.NULL_SELECTION_NOT_ALLOWED);
  clickResetButton();
  printFilterAndRemoveIt(getComboBoxInput(comboBoxWebElement));
  assertEquals("There should be empty value","",getComboBoxValue(comboBoxWebElement));
}
