{
  Actions actions=new Actions(getDriver());
  actions.click(target).perform();
  actions.sendKeys(Keys.chord(Keys.CONTROL,"a")).perform();
  actions.sendKeys(FILTER_STRING);
  actions.sendKeys(Keys.ENTER).sendKeys(Keys.ESCAPE).sendKeys(Keys.TAB);
  actions.perform();
}
