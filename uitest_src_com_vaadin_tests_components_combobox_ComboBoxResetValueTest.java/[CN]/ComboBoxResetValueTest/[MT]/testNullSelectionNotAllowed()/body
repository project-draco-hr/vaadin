{
  openTestURL();
  ComboBoxElement comboBoxWebElement=$(ComboBoxElement.class).id(ComboBoxResetValue.NULL_SELECTION_NOT_ALLOWED);
  clickResetButton();
  openPopup(comboBoxWebElement);
  assertEquals("There should be no selection",null,getSelectedInPopupValue());
}
