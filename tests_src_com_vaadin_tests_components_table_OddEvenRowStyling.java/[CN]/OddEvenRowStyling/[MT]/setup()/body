{
  Table t=new Table();
  t.setPageLength(10);
  t.addContainerProperty("foo",String.class,"");
  for (int i=0; i < 33; i++) {
    t.addItem(i).getItemProperty("foo").setValue("bar");
  }
  addComponent(t);
}
