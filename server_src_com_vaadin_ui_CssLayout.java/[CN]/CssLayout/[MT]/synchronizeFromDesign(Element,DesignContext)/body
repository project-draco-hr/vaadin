{
  super.synchronizeFromDesign(design,designContext);
  removeAllComponents();
  for (  Element childComponent : design.children()) {
    DesignSynchronizable newChild=designContext.createChild(childComponent);
    addComponent(newChild);
  }
}
