{
  assertSetup();
  return new Iterable<FlyweightCell>(){
    @Override public Iterator<FlyweightCell> iterator(){
      return CellIterator.attached(cells.subList(offset,offset + numberOfCells));
    }
  }
;
}
