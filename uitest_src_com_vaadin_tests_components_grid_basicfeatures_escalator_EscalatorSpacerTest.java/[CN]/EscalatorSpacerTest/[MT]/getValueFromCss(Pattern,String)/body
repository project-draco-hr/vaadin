{
  Matcher matcher=pattern.matcher(css);
  assertTrue("no matches for " + css + " against "+ TRANSLATE_VALUE_PATTERN,matcher.find());
  assertEquals("wrong amount of groups matched in " + css,1,matcher.groupCount());
  return Double.parseDouble(matcher.group(1));
}
