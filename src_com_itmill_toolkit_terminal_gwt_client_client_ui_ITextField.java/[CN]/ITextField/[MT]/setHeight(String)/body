{
  if (height != null && height.indexOf("%") > 0) {
    proportionalHeight=Float.parseFloat(height.substring(0,height.indexOf("%"))) / 100;
    iLayout();
  }
 else {
    super.setHeight(height);
    proportionalHeight=-1;
  }
}
