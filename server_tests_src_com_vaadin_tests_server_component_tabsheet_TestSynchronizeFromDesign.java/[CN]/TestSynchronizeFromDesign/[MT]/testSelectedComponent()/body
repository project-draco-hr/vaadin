{
  TabSheet tabSheet=new TabSheet();
  tabSheet.synchronizeFromDesign(createFirstTabSelectedDesign(),new DesignContext());
  assertEquals(tabSheet.getTab(0).getComponent(),tabSheet.getSelectedTab());
}
