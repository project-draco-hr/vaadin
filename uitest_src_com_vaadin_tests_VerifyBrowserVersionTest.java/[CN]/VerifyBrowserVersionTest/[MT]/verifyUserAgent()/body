{
  openTestURL();
  DesiredCapabilities desiredCapabilities=getDesiredCapabilities();
  assertThat(vaadinElementById("userAgent").getText(),containsString(getExpectedUserAgentString(desiredCapabilities) + desiredCapabilities.getVersion()));
  assertThat(vaadinElementById("touchDevice").getText(),is("Touch device? No"));
}
