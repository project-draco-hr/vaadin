{
  openTestURL();
  DesiredCapabilities desiredCapabilities=getDesiredCapabilities();
  assertThat(vaadinElementById("userAgent").getText(),containsString(expectedUserAgent.get(desiredCapabilities) + desiredCapabilities.getVersion()));
  assertThat(vaadinElementById("touchDevice").getText(),is("Touch device? No"));
}
