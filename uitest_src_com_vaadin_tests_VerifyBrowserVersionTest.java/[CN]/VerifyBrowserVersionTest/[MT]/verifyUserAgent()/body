{
  openTestURL();
  String expected=expectedUserAgent.get(getDesiredCapabilities());
  String actual=vaadinElementById("userAgent").getText();
  Assert.assertEquals("Remote control " + getRemoteControlName() + " uses incorrect browser version",expected,actual);
  Assert.assertEquals("Touch device? No",vaadinElementById("touchDevice").getText());
}
