{
  String c=uidl.getStringAttribute("caption");
  if (c == null) {
    if (caption == null)     return;
    remove(caption);
    caption=null;
  }
 else {
    if (caption == null) {
      caption=new Label(c);
      insert(caption,0);
    }
 else     caption.setText(c);
  }
}
