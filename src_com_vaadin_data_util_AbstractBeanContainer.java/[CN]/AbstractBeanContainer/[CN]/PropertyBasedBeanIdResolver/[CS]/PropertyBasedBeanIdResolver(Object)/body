{
  if (propertyId == null) {
    throw new IllegalArgumentException("Property identifier must not be null");
  }
  this.propertyId=propertyId;
  if (getGetter() == null) {
    throw new IllegalArgumentException("Missing accessor for property " + propertyId);
  }
}
