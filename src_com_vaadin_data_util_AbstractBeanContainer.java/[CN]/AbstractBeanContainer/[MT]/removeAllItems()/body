{
  allItemIds.clear();
  filteredItemIds.clear();
  for (  BeanItem<BT> item : itemIdToItem.values()) {
    removeAllValueChangeListeners(item);
  }
  itemIdToItem.clear();
  fireItemSetChange();
  return true;
}
