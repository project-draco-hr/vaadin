{
  if (!classpathEntry.endsWith(".jar")) {
    return true;
  }
 else {
    if (classpathEntry.contains("vaadin-") || classpathEntry.contains(".vaadin.")) {
      return true;
    }
 else {
      return false;
    }
  }
}
