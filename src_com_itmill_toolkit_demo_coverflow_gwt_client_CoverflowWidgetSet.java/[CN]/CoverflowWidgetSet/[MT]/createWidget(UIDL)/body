{
  final String className=resolveWidgetTypeName(uidl);
  if ("com.itmill.toolkit.demo.coverflow.gwt.client.ui.ICoverflow".equals(className)) {
    return new ICoverflow();
  }
  return super.createWidget(uidl);
}
