{
  if (newComponent == null || newComponent instanceof CustomPopup) {
    popup.hide();
    if (popup != null) {
      client.unregisterPaintable((Paintable)popup);
    }
    popup=(CustomPopup)newComponent;
  }
 else {
    throw new IllegalArgumentException("PopupPanel only supports components of type CustomPopup");
  }
}
