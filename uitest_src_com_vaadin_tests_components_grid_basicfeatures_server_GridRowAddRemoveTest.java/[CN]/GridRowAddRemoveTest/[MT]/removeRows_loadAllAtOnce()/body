{
  openTestURL();
  selectMenuPath("Settings","Clear log");
  selectMenuPath("Component","Body rows","Remove 18 first rows");
  selectMenuPath("Component","Body rows","Remove 18 first rows");
  Assert.assertEquals("All newly required rows should be fetched in the same round trip.","2. Requested items 64 - 100",getLogRow(0));
}
