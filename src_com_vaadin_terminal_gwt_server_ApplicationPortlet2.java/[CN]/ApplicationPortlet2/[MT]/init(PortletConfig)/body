{
  super.init(config);
  try {
    applicationClass=ServletPortletHelper.getApplicationClass(config.getInitParameter("application"),config.getInitParameter(Application.ROOT_PARAMETER),getClassLoader());
  }
 catch (  ApplicationClassException e) {
    throw new PortletException(e);
  }
}
