{
  for (  Element element : measuredNonConnectorElements) {
    measuredAndUpdate(element,getMeasuredSize(element,null));
  }
  VConsole.log("Measured " + measuredNonConnectorElements.size() + " non connector elements");
}
