{
  assertUnattached(main);
  assertUnattached(sub);
  main.setSession(testApp);
  assertAttached(main);
  assertUnattached(sub);
  main.addWindow(sub);
  assertAttached(main);
  assertAttached(sub);
}
