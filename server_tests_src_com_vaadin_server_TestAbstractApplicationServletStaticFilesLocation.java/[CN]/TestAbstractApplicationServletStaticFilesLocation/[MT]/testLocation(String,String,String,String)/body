{
  HttpServletRequest request=createNonIncludeRequest(base,contextPath,servletPath,pathInfo);
  replay(request);
  String location=servlet.getDeploymentConfiguration().getStaticFileLocation(servlet.createWrappedRequest(request));
  return location;
}
