{
  HttpServletRequest request=createIncludeRequest(base,portletContextPath,servletPath,pathInfo);
  replay(request);
  String location=servlet.getVaadinService().getStaticFileLocation(servlet.createWrappedRequest(request));
  return location;
}
