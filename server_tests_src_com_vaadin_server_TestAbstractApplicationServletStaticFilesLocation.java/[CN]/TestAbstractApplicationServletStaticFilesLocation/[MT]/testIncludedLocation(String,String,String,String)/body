{
  HttpServletRequest request=createIncludeRequest(base,portletContextPath,servletPath,pathInfo);
  replay(request);
  String location=servlet.getVaadinService().getStaticFileLocation(servlet.createVaadinRequest(request));
  return location;
}
