{
  int origSize=size();
  internalRemoveAllItems();
  for (  Item item : itemIdToItem.values()) {
    removeAllValueChangeListeners(item);
  }
  itemIdToItem.clear();
  if (origSize != 0) {
    fireItemSetChange();
  }
  return true;
}
