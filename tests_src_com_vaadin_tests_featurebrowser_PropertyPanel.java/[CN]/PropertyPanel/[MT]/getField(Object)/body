{
  for (final Iterator<Form> i=forms.iterator(); i.hasNext(); ) {
    final Form f=i.next();
    final Field af=f.getField(propertyId);
    if (af != null) {
      return af;
    }
  }
  return null;
}
