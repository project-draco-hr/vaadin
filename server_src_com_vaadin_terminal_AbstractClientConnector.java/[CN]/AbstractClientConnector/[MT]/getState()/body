{
  if (null == sharedState) {
    sharedState=createState();
  }
  Root root=getRoot();
  if (root != null && !root.getConnectorTracker().isDirty(this)) {
    requestRepaint();
  }
  return sharedState;
}
