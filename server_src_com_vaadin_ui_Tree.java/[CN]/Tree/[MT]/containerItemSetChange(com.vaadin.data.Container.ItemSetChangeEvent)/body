{
  super.containerItemSetChange(event);
  if (getContainerDataSource() instanceof Filterable) {
    boolean hasFilters=((Filterable)getContainerDataSource()).hasContainerFilters();
    if (!hasFilters) {
      cleanupExpandedItems();
    }
  }
}
