{
  if (totalRows >= 0 && lastRendered > totalRows) {
    VConsole.log("setLastRendered: " + this.lastRendered + " -> "+ lastRendered);
    this.lastRendered=totalRows - 1;
  }
 else {
    this.lastRendered=lastRendered;
  }
}
