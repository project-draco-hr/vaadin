{
  mainStyleSheet=this;
  mixinDefs.clear();
  variables.clear();
  ifElseDefNodes.clear();
  ParentSelectorHandler.clear();
  importOtherFiles(this);
  populateDefinitions(this);
  traverse(this);
}
