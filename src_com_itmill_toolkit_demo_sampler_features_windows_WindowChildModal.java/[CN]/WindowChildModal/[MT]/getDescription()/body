{
  return "Creates and opens a new modal child window with its own state." + "<br>Child window modality means that you cannot access the" + " underlying window(s) while the modal window is displayed.";
}
