{
  List entries=(List)dates.get(date.getYear() + "" + date.getMonth()+ ""+ date.getDate());
  ArrayList res=new ArrayList();
  if (entries == null) {
    return res;
  }
  for (Iterator it=entries.iterator(); it.hasNext(); ) {
    CalendarEntry item=(CalendarEntry)it.next();
    if (DateTimeService.isInRange(date,item.getStart(),item.getEnd(),resolution)) {
      res.add(item);
    }
  }
  return res;
}
