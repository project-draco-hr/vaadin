{
  if (paintableId == null || client == null) {
    return;
  }
  if (BrowserInfo.get().isSafari()) {
    VButton.this.setFocus(true);
  }
  if (disableOnClick) {
    setEnabled(false);
    client.updateVariable(paintableId,"disabledOnClick",true,false);
  }
  client.updateVariable(paintableId,"state",true,false);
  MouseEventDetails details=new MouseEventDetails(event.getNativeEvent(),getElement());
  client.updateVariable(paintableId,"mousedetails",details.serialize(),true);
  clickPending=false;
}
