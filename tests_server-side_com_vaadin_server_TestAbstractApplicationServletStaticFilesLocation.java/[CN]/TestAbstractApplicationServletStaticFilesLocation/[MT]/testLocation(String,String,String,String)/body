{
  HttpServletRequest request=createNonIncludeRequest(base,contextPath,servletPath,pathInfo);
  replay(request);
  String location=(String)getStaticFilesLocationMethod.invoke(servlet,request);
  return location;
}
