{
  String themeName;
  themeName=null;
  if (themeName == null) {
    themeName=request.getPortalProperty(Constants.PORTAL_PARAMETER_VAADIN_THEME);
  }
  if (themeName == null) {
    themeName=DEFAULT_THEME_NAME;
  }
  return themeName;
}
