{
  String val=null;
  val=getApplicationProperty(parameterName);
  if (val != null) {
    return val;
  }
  val=getSystemProperty(parameterName);
  if (val != null) {
    return val;
  }
  return defaultValue;
}
