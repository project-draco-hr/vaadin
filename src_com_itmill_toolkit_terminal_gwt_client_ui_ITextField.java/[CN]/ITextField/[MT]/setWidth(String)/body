{
  if (width != null && width.indexOf("%") > 0) {
    proportionalWidth=Float.parseFloat(width.substring(0,width.indexOf("%"))) / 100;
    iLayout();
  }
 else {
    super.setWidth(width);
    proportionalWidth=-1;
  }
}
