{
  if (this.calendar == null) {
    this.calendar=Calendar.getInstance();
  }
  Calendar newCal=(Calendar)this.calendar.clone();
  Date currentDate=(Date)getValue();
  if (currentDate != null)   newCal.setTime(currentDate);
  return newCal;
}
