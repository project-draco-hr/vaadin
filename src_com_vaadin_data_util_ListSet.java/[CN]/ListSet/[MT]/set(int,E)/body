{
  if (contains(element)) {
    if (get(index) == element) {
      return element;
    }
 else {
      return null;
    }
  }
  E old=super.set(index,element);
  itemSet.remove(old);
  itemSet.add(element);
  return old;
}
