{
  Item generatedItem=new GeneratedPropertyItem(itemId,item);
  for (  Object propertyId : propertyGenerators.keySet()) {
    generatedItem.addItemProperty(propertyId,createProperty(item,propertyId,itemId,propertyGenerators.get(propertyId)));
  }
  return generatedItem;
}
