{
  checkList();
  if (clientEvents.keySet().contains(eventIdentifier)) {
    clientEvents.put(eventIdentifier,clientEvents.get(eventIdentifier) - 1);
    if (clientEvents.get(eventIdentifier) <= 0)     clientEvents.remove(eventIdentifier);
  }
}
