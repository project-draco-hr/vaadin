{
  parent=getParent();
  column=new Column<JsonValue,JsonObject>(){
    @Override public JsonValue getValue(    JsonObject row){
      return row.getObject(DataCommunicatorConstants.DATA).get(getState().id);
    }
  }
;
  getParent().addColumn(column,getState().id);
}
