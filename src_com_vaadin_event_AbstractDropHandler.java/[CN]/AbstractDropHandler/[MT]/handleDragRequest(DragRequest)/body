{
  boolean acceptTransferrable=acceptTransferrable(event.getTransferrable());
  if (acceptTransferrable) {
    if (event.getType() == DragEventType.DROP) {
      receive(event.getTransferrable());
    }
 else {
      event.setResponseParameter("accepted",true);
    }
  }
}
