{
  try {
    TreeDropDetails eventDetails=(TreeDropDetails)event.getEventDetails();
    return eventDetails.getDropLocation() == Location.MIDDLE;
  }
 catch (  Exception e) {
    return false;
  }
}
