{
  if (event.getTransferable() instanceof ComponentTransferrable) {
    return ((ComponentTransferrable)event.getTransferable()).getSourceComponent() == component;
  }
 else {
    return false;
  }
}
