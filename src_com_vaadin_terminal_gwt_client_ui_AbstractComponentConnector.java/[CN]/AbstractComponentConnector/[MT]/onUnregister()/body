{
  super.onUnregister();
  if (getWidget() != null && getWidget().isAttached()) {
    VConsole.log("Widget for unregistered connector " + Util.getConnectorString(this) + " is still attached to the DOM.");
  }
}
