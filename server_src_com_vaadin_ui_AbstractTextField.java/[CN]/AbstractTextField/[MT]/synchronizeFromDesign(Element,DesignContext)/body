{
  super.synchronizeFromDesign(design,designContext);
  AbstractTextField def=designContext.getDefaultInstance(this.getClass());
  Attributes attr=design.attributes();
  int maxLength=DesignAttributeHandler.readAttribute("maxlength",attr,def.getMaxLength(),Integer.class);
  setMaxLength(maxLength);
}
