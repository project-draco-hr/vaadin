{
  HorizontalLayout contextClickControls=new HorizontalLayout();
  contextClickControls.addComponent(new Button("Add/Remove default listener",new ListenerHandler(defaultListener)));
  contextClickControls.addComponent(new Button("Add/Remove typed listener",new ListenerHandler(typedListener)));
  return contextClickControls;
}
