{
  Date date=new Date(2000 - 1900,0,1);
  if (year >= 0) {
    date.setYear(year - 1900);
  }
  if (month >= 0) {
    date.setMonth(month - 1);
  }
  if (day >= 0) {
    date.setDate(day);
  }
  return date;
}
