{
  Integer dupl=duplicates.get(e);
  if (dupl != null) {
    if (dupl == 1) {
      duplicates.remove(e);
    }
 else {
      duplicates.put(e,dupl - 1);
    }
  }
 else {
    itemSet.remove(e);
  }
}
