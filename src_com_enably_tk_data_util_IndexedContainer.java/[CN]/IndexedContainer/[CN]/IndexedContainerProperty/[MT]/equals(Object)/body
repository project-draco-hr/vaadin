{
  if (obj == null || !obj.getClass().equals(IndexedContainerProperty.class))   return false;
  IndexedContainerProperty lp=(IndexedContainerProperty)obj;
  return lp.getHost() == getHost() && lp.propertyId.equals(propertyId) && lp.itemId.equals(itemId);
}
