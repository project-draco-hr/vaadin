{
  if (itemSetChangeListeners != null) {
    Object[] l=itemSetChangeListeners.toArray();
    Container.ItemSetChangeEvent event=new IndexedContainer.ItemSetChangeEvent(this);
    for (int i=0; i < l.length; i++)     ((Container.ItemSetChangeListener)l[i]).containerItemSetChange(event);
  }
}
