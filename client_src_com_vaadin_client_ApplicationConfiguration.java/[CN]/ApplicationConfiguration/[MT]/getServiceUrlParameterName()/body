{
  String prefix=getJsoConfiguration(id).getConfigString(ApplicationConstants.SERVICE_URL_PARAMETER_NAMESPACE);
  if (prefix == null) {
    prefix="";
  }
  return prefix + ApplicationConstants.V_RESOURCE_PATH;
}
