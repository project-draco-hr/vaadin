{
  String stringValue=value.toString() + (important ? " !important" : "");
  if (!"".equals(stringValue.trim())) {
    stringValue=variable + ": " + stringValue+ ";";
  }
 else {
    stringValue="";
  }
  if (comment != null) {
    stringValue+=comment;
  }
  return stringValue;
}
