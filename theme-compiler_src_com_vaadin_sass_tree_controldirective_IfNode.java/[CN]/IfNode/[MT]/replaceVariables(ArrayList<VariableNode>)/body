{
  for (  final VariableNode node : variables) {
    if (expression.contains("$" + node.getName())) {
      expression=expression.replaceAll(Pattern.quote("$" + node.getName()),node.getExpr().toString());
    }
  }
}
