{
  for (  final VariableNode node : variables) {
    String variable="$" + node.getName();
    if (expression.contains(variable)) {
      expression=expression.replaceAll(Pattern.quote(variable),node.getExpr().toString());
    }
  }
}
