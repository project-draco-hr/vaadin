{
  createBooleanAction("Column collapsing allowed",category,true,new Command<T,Boolean>(){
    @Override public void execute(    T c,    Boolean value,    Object data){
      c.setColumnCollapsingAllowed(value);
    }
  }
);
}
