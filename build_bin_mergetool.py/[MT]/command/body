def command(cmd, dryrun=0):
    print cmd
    if (not dryrun):
        if os.system(cmd):
            print 'Command failed, exiting.'
            sys.exit(1)
    else:
        print 'Dry run - not executing.'
