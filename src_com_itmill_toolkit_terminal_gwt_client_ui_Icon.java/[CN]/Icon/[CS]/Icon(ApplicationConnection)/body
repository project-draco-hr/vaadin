{
  setElement(DOM.createImg());
  DOM.setElementProperty(getElement(),"alt","icon");
  setStyleName("i-icon");
  this.client=client;
  BrowserInfo b=BrowserInfo.get();
  if (b.isIE6()) {
    addPngFix(getElement(),client.getThemeUri() + "/../default/common/img/blank.gif");
  }
}
