{
  String s="<" + getTag() + " ";
  s+="id=" + getId();
  s+=">\n";
  Iterator i=getChildIterator();
  while (i.hasNext()) {
    UIDL c=(UIDL)i.next();
    s+=c.toString();
  }
  s+="</" + getTag() + ">\n";
  return s;
}
