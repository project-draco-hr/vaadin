{
  JSONArray a=getArrayVariable(name);
  int[] s=new int[a.size()];
  for (int i=0; i < a.size(); i++) {
    JSONValue v=a.get(i);
    s[i]=v.isNumber() != null ? (int)((JSONNumber)v).getValue() : Integer.parseInt(v.toString());
  }
  return s;
}
