{
  DOM.removeEventPreview(this);
  if (delayMsec > 0) {
    delay=new Timer(){
      @Override public void run(){
        fade();
      }
    }
;
    delay.schedule(delayMsec);
  }
 else   if (delayMsec == 0) {
    fade();
  }
}
