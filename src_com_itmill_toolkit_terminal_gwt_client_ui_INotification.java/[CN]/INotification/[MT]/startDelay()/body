{
  DOM.removeEventPreview(this);
  if (delayMsec > 0) {
    delay=new Timer(){
      public void run(){
        fade();
      }
    }
;
    delay.scheduleRepeating(delayMsec);
  }
 else   if (delayMsec == 0) {
    fade();
  }
}
