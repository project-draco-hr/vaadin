{
  openTestURL();
  assertFalse("Rows should not be selected initially.",$(LegacyGridElement.class).first().getRow(0).isSelected());
  $(ButtonElement.class).first().click();
  assertTrue("Rows should be selected after button click.",$(LegacyGridElement.class).first().getRow(0).isSelected());
}
