{
  if (itemId instanceof Integer) {
    int pos=((Integer)itemId).intValue();
    if (pos >= 0 && pos < 100) {
      Item item=items[pos];
      return item.getItemProperty(propertyId);
    }
  }
  return null;
}
