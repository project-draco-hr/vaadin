{
  super.onBrowserEvent(event);
  if (DOM.eventGetType(event) == Event.ONKEYDOWN && actionHandler != null) {
    final int modifiers=KeyboardListenerCollection.getKeyboardModifiers(event);
    actionHandler.handleKeyboardEvent((char)DOM.eventGetKeyCode(event),modifiers);
    return;
  }
}
