{
  super(new DataCommunicator<T>(){
    @Override protected DataKeyMapper<T> createKeyMapper(){
      return new KeyMapper<T>(){
        @Override public void remove(        T removeobj){
        }
      }
;
    }
  }
);
  setSelectionModel(new ComboBoxSelectionModel());
  init();
}
