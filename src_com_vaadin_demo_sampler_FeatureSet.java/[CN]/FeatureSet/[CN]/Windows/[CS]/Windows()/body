{
  super("Windows","Windows","Windows can (for instance) organize the UI, save space (popup windows), focus attention (modal windows), or provide multiple views for the same data for multitasking or dual screen setups (native browser windows).",new Feature[]{new Subwindow(),new SubwindowModal(),new SubwindowAutoSized(),new SubwindowSized(),new SubwindowPositioned(),new SubwindowClose(),new NativeWindow()});
}
