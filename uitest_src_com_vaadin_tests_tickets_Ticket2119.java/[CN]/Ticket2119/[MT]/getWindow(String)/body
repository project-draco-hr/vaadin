{
  if (!isRunning()) {
    return null;
  }
  LegacyWindow w=super.getWindow(name);
  if (w == null) {
    w=createWindow();
    addWindow(w);
    w.open(new ExternalResource(w.getURL()));
  }
  return w;
}
