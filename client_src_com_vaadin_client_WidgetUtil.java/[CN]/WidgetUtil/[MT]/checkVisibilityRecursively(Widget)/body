{
  if (widget.isVisible()) {
    Widget parent=widget.getParent();
    if (parent == null) {
      return true;
    }
 else {
      return checkVisibilityRecursively(parent);
    }
  }
 else {
    return false;
  }
}
