{
  running=false;
  @SuppressWarnings("unchecked") Collection<T> emptySet=(Collection<T>)Collections.emptySet();
  remoteDataSource.transactionPin(emptySet);
  remoteDataSource=null;
  batchedSelectionModel=null;
  if (handle != null) {
    handle.cancel();
    handle=null;
  }
}
