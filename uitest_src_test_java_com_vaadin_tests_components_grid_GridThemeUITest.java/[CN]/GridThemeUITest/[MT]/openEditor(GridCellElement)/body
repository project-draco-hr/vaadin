{
  new Actions(getDriver()).doubleClick(targetCell).perform();
  try {
    if (grid.getEditor().isDisplayed()) {
      return;
    }
  }
 catch (  Exception e) {
  }
  new Actions(getDriver()).doubleClick(targetCell).perform();
}
