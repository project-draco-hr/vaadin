{
  if (spacer == null) {
    return 0;
  }
  if (layoutManager != null) {
    if (vertical) {
      return layoutManager.getOuterHeight(spacer);
    }
 else {
      return layoutManager.getOuterWidth(spacer);
    }
  }
  return vertical ? spacer.getOffsetHeight() : spacer.getOffsetWidth();
}
