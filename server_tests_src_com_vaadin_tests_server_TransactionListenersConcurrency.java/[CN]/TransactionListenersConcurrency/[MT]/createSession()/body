{
  HttpSession session=createMock(HttpSession.class);
  EasyMock.expect(session.getAttribute(ServletApplicationContext.class.getName())).andReturn(null).anyTimes();
  session.setAttribute(EasyMock.eq(ServletApplicationContext.class.getName()),EasyMock.anyObject());
  EasyMock.replay(session);
  return session;
}
