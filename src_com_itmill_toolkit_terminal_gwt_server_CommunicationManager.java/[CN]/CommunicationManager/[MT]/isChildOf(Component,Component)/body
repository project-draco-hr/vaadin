{
  if (parent instanceof ComponentContainer) {
    Component p;
    p=child.getParent();
    while (p != null) {
      if (parent == p) {
        return true;
      }
      p=p.getParent();
    }
  }
  return false;
}
