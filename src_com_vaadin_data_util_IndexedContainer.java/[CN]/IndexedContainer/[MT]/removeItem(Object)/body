{
  if (itemId == null || items.remove(itemId) == null) {
    return false;
  }
  int origSize=size();
  if (internalRemoveItem(itemId)) {
    if (size() != origSize) {
      fireContentsChange(-1);
    }
    return true;
  }
 else {
    return false;
  }
}
