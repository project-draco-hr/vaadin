{
  Item item=internalAddItemAt(index,newItemId,new IndexedContainerItem(newItemId));
  if (item != null && getFilteredItemIds() == null) {
    int position=indexOfId(newItemId);
    if (position >= 0) {
      fireItemAdded(position,newItemId,item);
    }
  }
  return item;
}
