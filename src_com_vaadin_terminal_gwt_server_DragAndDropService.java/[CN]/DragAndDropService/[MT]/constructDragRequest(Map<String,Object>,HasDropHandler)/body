{
  Transferable transferable=constructTransferrable(variables,dropHandlerOwner);
  int type=(Integer)variables.get("type");
  DragRequest dragRequest=new DragRequest(DragEventType.values()[type],transferable);
  return dragRequest;
}
