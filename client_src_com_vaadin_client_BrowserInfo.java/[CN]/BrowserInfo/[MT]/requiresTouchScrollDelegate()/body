{
  if (!isTouchDevice()) {
    return false;
  }
  if (isAndroid() && isWebkit() && getWebkitVersion() >= 534) {
    return false;
  }
  if (isIOS() && isSafari() && getBrowserMajorVersion() >= 6) {
    return false;
  }
  return true;
}
