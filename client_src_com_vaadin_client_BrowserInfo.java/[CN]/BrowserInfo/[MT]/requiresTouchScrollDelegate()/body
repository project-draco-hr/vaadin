{
  if (!isTouchDevice()) {
    return false;
  }
  if (isAndroid() && isWebkit() && getWebkitVersion() >= 534) {
    return false;
  }
  if (isIOS6() && isWebkit()) {
    return false;
  }
  return true;
}
