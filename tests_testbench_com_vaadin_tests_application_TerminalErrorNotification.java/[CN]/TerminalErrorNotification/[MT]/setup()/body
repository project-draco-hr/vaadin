{
  Button button=new Button("Throw exception",new Button.ClickListener(){
    public void buttonClick(    ClickEvent event){
      throw new RuntimeException("You asked for it");
    }
  }
);
  addComponent(button);
}
