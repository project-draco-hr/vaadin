{
  if (currentDrag != null) {
    ApplicationConnection.getConsole().log("Drag operation interrupted");
    if (currentDropHandler != null) {
      currentDrag.currentGwtEvent=null;
      currentDropHandler.dragLeave(currentDrag);
      currentDropHandler=null;
    }
    currentDrag=null;
  }
}
