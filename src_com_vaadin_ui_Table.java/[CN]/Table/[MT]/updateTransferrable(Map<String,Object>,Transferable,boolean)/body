{
  Map<String,Object> payload=(Map<String,Object>)rawVariables.get("payload");
  if (!isDropTarget) {
    Object object=payload.get("itemId");
    if (object != null) {
      tr.setData("itemId",itemIdMapper.get((String)object));
      payload.remove("itemId");
    }
  }
}
