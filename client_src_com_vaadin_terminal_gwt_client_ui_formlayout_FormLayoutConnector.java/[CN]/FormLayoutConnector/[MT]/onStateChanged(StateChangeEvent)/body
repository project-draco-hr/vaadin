{
  super.onStateChanged(stateChangeEvent);
  VFormLayoutTable formLayoutTable=getWidget().table;
  formLayoutTable.setMargins(new MarginInfo(getState().getMarginsBitmask()));
  formLayoutTable.setSpacing(getState().isSpacing());
}
