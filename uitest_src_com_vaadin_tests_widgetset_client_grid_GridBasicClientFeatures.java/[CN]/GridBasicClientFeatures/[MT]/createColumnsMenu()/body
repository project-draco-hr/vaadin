{
  for (int i=0; i < COLUMNS; i++) {
    final int index=i;
    addMenuCommand("Visible",new ScheduledCommand(){
      @Override public void execute(){
        grid.getColumn(index).setVisible(!grid.getColumn(index).isVisible());
      }
    }
,"Component","Columns","Column " + i);
  }
}
