{
  if ((propertyId != null) && Resource.class.isAssignableFrom(getType(propertyId))) {
    this.itemIconPropertyId=propertyId;
    requestRepaint();
  }
 else {
    this.itemIconPropertyId=null;
  }
}
