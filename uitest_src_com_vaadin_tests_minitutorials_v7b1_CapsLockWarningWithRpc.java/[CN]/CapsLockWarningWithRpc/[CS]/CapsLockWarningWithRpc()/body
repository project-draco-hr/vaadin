{
  registerRpc(new CapsLockWarningRpc(){
    @Override public void isCapsLockEnabled(    boolean isCapsLockEnabled){
      Notification.show("Caps Lock was " + (isCapsLockEnabled ? "enabled" : "disabled"));
    }
  }
);
}
