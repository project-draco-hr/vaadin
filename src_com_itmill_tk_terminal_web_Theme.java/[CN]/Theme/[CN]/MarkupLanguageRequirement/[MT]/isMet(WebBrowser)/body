{
  if (terminal.getMarkupVersion().supports(this.requiredVersion))   return true;
  Log.info("Requirement: " + this.requiredVersion + " is not met by "+ terminal.getMarkupVersion());
  return false;
}
