{
  BeanItemContainer<Parent> bc=new BeanItemContainer<Parent>(Parent.class);
  for (int i=0; i < names.length; i++) {
    Parent p=new Parent();
    p.setName(names[i]);
    p.setAge(ages[i]);
    bc.addBean(p);
  }
  return bc;
}
