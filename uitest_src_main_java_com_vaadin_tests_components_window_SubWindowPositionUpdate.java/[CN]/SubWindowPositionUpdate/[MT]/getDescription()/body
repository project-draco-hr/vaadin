{
  return "The window position should not jump inconsistently while " + "dragging, even though external UIDL updates are sent and " + "received by the progress indicator. A small delay is used "+ "on the server side to surface the issue (" + delay + "ms).";
}
