{
  return "Horizontal and vertical layouts are the foremost " + "basic layouts of all applications. You can add spacing " + "between the elements, set the alignment of the components "+ "inside the cells of the layouts, and set one or more "+ "components as expanding so that they fill the available space "+ "according to the specified expand ratio.";
}
