{
  selectMenuPath("Component","Listeners","Blur listener");
  getGridElement().click();
  $(MenuBarElement.class).first().click();
  assertTrue("Blur listener should be invoked",getLogRow(0).contains("BlurEvent"));
}
