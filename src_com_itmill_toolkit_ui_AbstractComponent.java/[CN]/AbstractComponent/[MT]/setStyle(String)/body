{
  if (style == null || "".equals(style)) {
    styles=null;
    return;
  }
  if (this.styles == null) {
    styles=new ArrayList();
  }
  styles.clear();
  styles.add(style);
  requestRepaint();
}
