{
  if (style == null || "".equals(style)) {
    return;
  }
  if (styles == null) {
    styles=new ArrayList();
  }
  if (!styles.contains(style)) {
    styles.add(style);
    requestRepaint();
  }
}
