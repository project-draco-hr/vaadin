{
  String design="<vaadin-legacy-grid><table>" + "<colgroup>" + "   <col sortable=true expand />"+ "</colgroup>"+ "</table></vaadin-legacy-grid>";
  LegacyGrid grid=new LegacyGrid();
  grid.addColumn("property-0",String.class).setExpandRatio(1);
  testRead(design,grid);
}
