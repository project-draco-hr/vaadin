{
  if (needsReturnType.containsKey(type) && needsReturnType.get(type).contains(method)) {
    return true;
  }
 else {
    return previousBundle != null && previousBundle.isNeedsReturnType(type,method);
  }
}
