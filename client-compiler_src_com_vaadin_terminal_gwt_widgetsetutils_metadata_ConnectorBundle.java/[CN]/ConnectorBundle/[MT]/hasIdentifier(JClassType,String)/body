{
  if (identifiers.containsKey(type) && identifiers.get(type).contains(identifier)) {
    return true;
  }
 else {
    return previousBundle != null && previousBundle.hasIdentifier(type,identifier);
  }
}
