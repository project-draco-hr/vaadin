{
  if (event.getChildComponent() == null) {
    getMainWindow().showNotification("You missed!");
  }
 else {
    getMainWindow().showNotification("Clicked on " + event.getChildComponent() + "!");
  }
}
