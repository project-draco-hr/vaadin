{
  for (int i=0; i < getWidget().getWidgetCount(); i++) {
    Slot slot=(Slot)getWidget().getWidget(i);
    getLayoutManager().removeElementResizeListener(slot.getWidget().getElement(),this);
  }
  super.onUnregister();
}
