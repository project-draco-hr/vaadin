{
  Slot slot=getWidget().getSlot(connector.getWidget());
  URLReference icon=connector.getState().getIcon();
  slot.setCaption(connector.getState().getCaption(),icon != null ? icon.getURL() : null,connector.getState().getStyles());
}
