{
  if (isAttached()) {
    DeferredCommand.addCommand(new Command(){
      public void execute(){
        if (!hasFocus) {
          onTabOut(event);
        }
      }
    }
);
  }
  if (event.getSource() instanceof VCalendarPanel) {
    hasFocus=false;
    focusDay(-1);
  }
}
