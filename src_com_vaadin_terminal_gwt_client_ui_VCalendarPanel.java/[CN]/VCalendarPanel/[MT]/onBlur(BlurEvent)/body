{
  if (isAttached()) {
    Scheduler.get().scheduleDeferred(new Command(){
      public void execute(){
        if (!hasFocus) {
          onTabOut(event);
        }
      }
    }
);
  }
  if (event.getSource() instanceof VCalendarPanel) {
    hasFocus=false;
    focusDay(-1);
  }
}
