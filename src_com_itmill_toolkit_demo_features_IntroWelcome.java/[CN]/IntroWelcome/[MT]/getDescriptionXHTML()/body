{
  WebBrowser wb=(WebBrowser)getWindow().getTerminal();
  return WELCOME_TEXT_LOWER + "<br /><br />IT Mill Toolkit version: " + ApplicationServlet.VERSION+ (wb != null ? "<br />Browser: " + wb.getBrowserApplication() : "");
}
