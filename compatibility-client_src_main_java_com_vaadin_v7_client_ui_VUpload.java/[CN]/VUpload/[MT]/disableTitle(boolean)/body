{
  if (disable) {
    if (BrowserInfo.get().isChrome()) {
      fu.setTitle(" ");
    }
 else     if (BrowserInfo.get().isFirefox()) {
      fu.getElement().setAttribute("title","");
    }
  }
 else {
    fu.setTitle(null);
  }
}
