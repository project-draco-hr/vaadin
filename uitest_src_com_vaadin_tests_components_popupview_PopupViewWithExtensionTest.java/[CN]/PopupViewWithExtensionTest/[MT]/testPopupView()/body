{
  setDebug(true);
  openTestURL();
  WebElement view=driver.findElement(By.className("v-popupview"));
  view.click();
  Assert.assertFalse("Popup view with extension should not throw an exception. " + "(Error notification window is shown).",isElementPresent(By.className("v-Notification-error")));
}
