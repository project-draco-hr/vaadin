{
  DecimalFormat df=new DecimalFormat("0.00");
  createClickAction(df.format(i) + " rows","Height by Rows",new Command<LegacyGrid,String>(){
    @Override public void execute(    LegacyGrid c,    String value,    Object data){
      c.setHeightByRows(i);
    }
  }
,null);
}
