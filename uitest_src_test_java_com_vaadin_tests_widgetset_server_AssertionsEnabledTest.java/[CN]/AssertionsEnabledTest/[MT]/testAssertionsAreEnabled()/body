{
  setDebug(true);
  openTestURL();
  Assert.assertFalse("Label with classname " + FAILING_CLASSNAME + " should not exist",isElementPresent(By.className(FAILING_CLASSNAME)));
  Assert.assertTrue("Assertion error Notification is not present",isElementPresent(NotificationElement.class));
}
