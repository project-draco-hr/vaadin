{
  for (int w=0; w < 2; w++) {
    final ExpandLayout layout=new ExpandLayout(OrderedLayout.ORIENTATION_VERTICAL);
    final Window window=new Window("A Child Window",layout);
    main.addWindow(window);
    layout.addComponent(new Label("Here be some component."));
    final Table table=new Table("My Ever-Expanding Table");
    layout.addComponent(table);
    layout.expand(table);
    table.setSizeFull();
    final Button button2=new Button("Ok");
    layout.addComponent(button2);
    layout.setComponentAlignment(button2,OrderedLayout.ALIGNMENT_RIGHT,0);
  }
}
