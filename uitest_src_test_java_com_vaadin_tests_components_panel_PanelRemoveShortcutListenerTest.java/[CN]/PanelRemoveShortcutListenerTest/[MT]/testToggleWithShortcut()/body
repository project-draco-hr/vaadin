{
  assertThat(panel.findElement(By.className("v-panel-caption")).findElement(By.tagName("span")).getText(),is("No shortcut effects (press 'A')"));
  attemptShortcut("A on");
  attemptShortcut("A off");
}
