{
  this.width=width;
  super.setWidth(width);
  if (width.endsWith("px")) {
    try {
      borderPaddingHorizontal=getElement().getOffsetWidth() - Integer.parseInt(width.substring(0,width.length() - 2));
    }
 catch (    Exception e) {
      ClientExceptionHandler.displayError(e);
    }
  }
}
