{
  int index=panel.getWidgetIndex(from);
  if (index >= 0) {
    panel.remove(index);
    if (panel instanceof HorizontalPanel)     ((HorizontalPanel)panel).insert(to,index);
 else     ((VerticalPanel)panel).insert(to,index);
  }
}
