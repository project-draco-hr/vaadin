{
  StackItem item=new StackItem(caption);
  if (stack.size() == 0) {
    item.addStyleDependentName("first");
  }
  stack.add(item);
  add(item);
  if (selected) {
    item.open();
    item.setContent(contentUidl);
  }
}
