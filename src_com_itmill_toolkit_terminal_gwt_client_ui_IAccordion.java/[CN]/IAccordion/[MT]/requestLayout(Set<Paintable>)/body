{
  if (!isDynamicHeight() && !isDynamicWidth()) {
    return true;
  }
  updateOpenTabSize();
  if (renderInformation.updateSize(getElement())) {
    iLayout();
    return false;
  }
 else {
    return true;
  }
}
