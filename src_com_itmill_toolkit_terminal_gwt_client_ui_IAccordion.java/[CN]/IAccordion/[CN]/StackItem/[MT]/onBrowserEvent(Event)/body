{
  if (DOM.eventGetType(evt) == Event.ONCLICK) {
    Element target=DOM.eventGetTarget(evt);
    if (DOM.compare(target,captionNode) || DOM.compare(target,DOM.getFirstChild(captionNode))) {
      ((IAccordion)getParent()).onSelectTab(this);
    }
  }
}
