{
  setDebug(true);
  openTestURL();
  assertNoErrorNotifications();
  assertEquals("LabelEditor content not correct.","FooFoo",$(LegacyGridElement.class).first().getEditor().getField(0).getText());
}
