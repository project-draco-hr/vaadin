{
  addComponent(upload);
  addComponent(result);
  upload.addListener(new Upload.FinishedListener(){
    public void uploadFinished(    FinishedEvent event){
      result.setValue("Uploaded file contained " + counter.getLineBreakCount() + " linebreaks");
    }
  }
);
}
