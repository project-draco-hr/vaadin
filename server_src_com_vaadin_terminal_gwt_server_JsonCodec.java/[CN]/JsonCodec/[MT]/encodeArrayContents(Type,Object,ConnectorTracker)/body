{
  JSONArray jsonArray=new JSONArray();
  for (int i=0; i < Array.getLength(array); i++) {
    jsonArray.put(encode(Array.get(array,i),null,componentType,connectorTracker));
  }
  return jsonArray;
}
