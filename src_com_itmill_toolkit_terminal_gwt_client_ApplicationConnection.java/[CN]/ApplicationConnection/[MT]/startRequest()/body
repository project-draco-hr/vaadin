{
  activeRequests++;
  requestStartTime=new Date();
  if (loadTimer == null) {
    loadTimer=new Timer(){
      @Override public void run(){
        showLoadingIndicator();
      }
    }
;
  }
  loadTimer.schedule(300);
}
