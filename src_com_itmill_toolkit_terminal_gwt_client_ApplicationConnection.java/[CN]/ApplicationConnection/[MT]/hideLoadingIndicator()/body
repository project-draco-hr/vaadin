{
  if (loadTimer != null) {
    loadTimer.cancel();
    if (loadTimer2 != null) {
      loadTimer2.cancel();
      loadTimer3.cancel();
    }
  }
  if (loadElement != null) {
    DOM.setStyleAttribute(loadElement,"display","none");
  }
}
