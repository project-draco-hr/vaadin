{
  openTestURL();
  assertTrue("Body 0,0 should've been active",getGridElement().getCell(0,0).isActive());
  selectMenuPath("Component","Body rows","Add second row");
  assertTrue("Body 0,0 should've remained active",getGridElement().getCell(0,0).isActive());
}
