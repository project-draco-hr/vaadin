{
  openTestURL();
  assertTrue("Body 0,0 should've been",getGridElement().getCell(0,0).isActive());
  selectMenuPath("Component","Body rows","Add first row");
  assertTrue("Body 1,0 should've become active",getGridElement().getCell(1,0).isActive());
}
