{
  openTestURL();
  assertTrue("Body 0,0 should've gotten focus",getGridElement().getCell(0,0).isFocused());
  selectMenuPath("Component","Body rows","Add first row");
  assertTrue("Body 1,0 should've gotten focus",getGridElement().getCell(1,0).isFocused());
}
