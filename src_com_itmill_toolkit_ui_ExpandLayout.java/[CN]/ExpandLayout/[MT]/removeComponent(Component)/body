{
  super.removeComponent(c);
  if (c == expanded) {
    if (getComponentIterator().hasNext()) {
      expanded=(Component)getComponentIterator().next();
    }
 else {
      expanded=null;
    }
  }
}
