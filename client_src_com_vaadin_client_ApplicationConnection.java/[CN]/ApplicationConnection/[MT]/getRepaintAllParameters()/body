{
  String nativeBootstrapParameters=getNativeBrowserDetailsParameters(getConfiguration().getRootPanelId());
  String parameters=ApplicationConstants.URL_PARAMETER_REPAINT_ALL + "=1&" + nativeBootstrapParameters;
  return parameters;
}
