{
  for (  TextField tf : textFields) {
    if (tf == null) {
      continue;
    }
    if (on) {
      tf.setComponentError(new UserError("It failed!"));
    }
 else {
      tf.setComponentError(null);
    }
  }
}
