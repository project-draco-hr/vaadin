{
  String[] selectedIndexes=new String[options.getItemCount()];
  for (int i=0; i < options.getItemCount(); i++) {
    if (options.isItemSelected(i))     selectedIndexes[i]=options.getValue(i);
  }
  return selectedIndexes;
}
