{
  String[] selectedIndexes=new String[selections.getItemCount()];
  for (int i=0; i < selections.getItemCount(); i++) {
    if (selections.isItemSelected(i))     selectedIndexes[i]=selections.getValue(i);
  }
  return selectedIndexes;
}
