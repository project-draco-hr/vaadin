{
  if (isReadOnly()) {
    return;
  }
  if (Objects.equals(key,getState(false).selectedItemKey)) {
    return;
  }
  getState().selectedItemKey=key;
  fireEvent(new SingleSelectionChange<>(AbstractSingleSelect.this,getSelectedItem().orElse(null),true));
}
