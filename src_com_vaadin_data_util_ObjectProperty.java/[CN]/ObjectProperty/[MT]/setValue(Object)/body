{
  if (isReadOnly()) {
    throw new Property.ReadOnlyException();
  }
  if (newValue != null && !type.isAssignableFrom(newValue.getClass())) {
    throw new Property.ConversionException("Invalid value type for ObjectProperty.");
  }
  this.value=(T)newValue;
  fireValueChange();
}
