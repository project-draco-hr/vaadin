{
  openTestURL();
  for (int i=0; i < GridBasicFeatures.COLUMNS; ++i) {
    assertFalse(getGridElement().getHeaderCell(0,i).findElement(By.cssSelector("div.v-grid-column-resize-handle")) == null);
  }
}
