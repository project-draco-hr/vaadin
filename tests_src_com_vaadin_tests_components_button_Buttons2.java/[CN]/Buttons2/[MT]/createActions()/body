{
  super.createActions();
  createBooleanAction("Switch mode",CATEGORY_FEATURES,false,switchModeCommand);
  createBooleanAction("Disable on click",CATEGORY_FEATURES,false,disableOnClickCommand);
  addClickListener(CATEGORY_LISTENERS);
}
