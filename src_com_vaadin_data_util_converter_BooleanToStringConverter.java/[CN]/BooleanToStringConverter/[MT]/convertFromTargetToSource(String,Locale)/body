{
  if (value == null) {
    return null;
  }
  value=value.trim();
  try {
    return Boolean.valueOf(value);
  }
 catch (  Exception e) {
    throw new ConversionException("Cannot convert " + value + " to Boolean");
  }
}
