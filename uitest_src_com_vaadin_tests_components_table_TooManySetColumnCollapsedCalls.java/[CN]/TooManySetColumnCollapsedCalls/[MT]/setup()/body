{
  label=new Label(String.valueOf(counter));
  label.setId("label");
  table=createTable();
  table.setId("table");
  addComponent(table);
  addComponent(label);
  getLayout().setSpacing(true);
  table.setColumnCollapsed("p2",true);
}
