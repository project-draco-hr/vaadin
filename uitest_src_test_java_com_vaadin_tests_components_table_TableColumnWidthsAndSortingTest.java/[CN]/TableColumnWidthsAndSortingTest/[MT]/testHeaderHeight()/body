{
  openTestURL();
  TableElement t=$(TableElement.class).first();
  assertHeaderCellHeight(t);
  t.getHeaderCell(2).click();
  assertHeaderCellHeight(t);
  t.getHeaderCell(2).click();
  assertHeaderCellHeight(t);
}
