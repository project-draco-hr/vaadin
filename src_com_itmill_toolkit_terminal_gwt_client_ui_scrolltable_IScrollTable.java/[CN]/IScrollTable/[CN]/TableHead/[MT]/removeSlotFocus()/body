{
  if (focusedSlot < 0)   return;
  if (focusedSlot == 0)   DOM.setStyleAttribute(DOM.getChild(tr,focusedSlot),"borderLeft","none");
 else   if (focusedSlot > 0)   DOM.setStyleAttribute(DOM.getChild(tr,focusedSlot - 1),"borderRight","none");
  focusedSlot=-1;
}
