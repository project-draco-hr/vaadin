{
  if (index < cells.size()) {
  }
 else   if (index == cells.size()) {
    adopt(cell,tr);
    cells.add(cell);
  }
 else {
    throw new RuntimeException("Header cells must be appended in order");
  }
}
