{
  if (hasEventListener() && mouseUpPreviewMatched && lastMouseDownTarget != null && Util.getElementUnderMouse(event.getNativeEvent()) == lastMouseDownTarget && shouldFireEvent(event)) {
    fireClick(event.getNativeEvent());
  }
  mouseUpPreviewMatched=false;
  lastMouseDownTarget=null;
}
