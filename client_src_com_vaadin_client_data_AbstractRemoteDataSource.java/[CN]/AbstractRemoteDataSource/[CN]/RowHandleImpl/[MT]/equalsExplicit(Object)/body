{
  if (obj instanceof AbstractRemoteDataSource.RowHandleImpl) {
    @SuppressWarnings("unchecked") final RowHandleImpl rhi=(RowHandleImpl)obj;
    return key.equals(rhi.key);
  }
 else {
    return false;
  }
}
