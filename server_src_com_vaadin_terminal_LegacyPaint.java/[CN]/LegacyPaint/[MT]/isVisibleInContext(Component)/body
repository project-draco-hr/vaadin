{
  HasComponents p=c.getParent();
  while (p != null) {
    if (!p.isVisible()) {
      return false;
    }
    p=p.getParent();
  }
  if (c.getParent() != null && !c.getParent().isComponentVisible(c)) {
    return false;
  }
  return c.isVisible();
}
