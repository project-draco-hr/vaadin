{
  setDebug(true);
  openTestURL();
  openLog();
  checkConnectorCount();
  clearLog();
  $(ButtonElement.class).caption("Set leaky content").first().click();
  checkConnectorCount();
  clearLog();
  $(ButtonElement.class).caption("Set leaky content").first().click();
  checkConnectorCount();
}
