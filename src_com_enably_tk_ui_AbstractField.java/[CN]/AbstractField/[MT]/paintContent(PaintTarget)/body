{
  if (this.focusableId > 0) {
    target.addAttribute("focusid",this.focusableId);
  }
  if (this.tabIndex > 0)   target.addAttribute("tabindex",this.tabIndex);
  if (isModified())   target.addAttribute("modified",true);
  if (isRequired())   target.addAttribute("required",true);
}
