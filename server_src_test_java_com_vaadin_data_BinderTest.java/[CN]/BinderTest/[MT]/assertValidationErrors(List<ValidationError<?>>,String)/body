{
  Assert.assertEquals(errorMessages.length,validationErrors.size());
  for (int i=0; i < errorMessages.length; i++) {
    Assert.assertEquals(errorMessages[i],validationErrors.get(i).getMessage());
  }
}
