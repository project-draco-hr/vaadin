{
  Table table=new Table();
  BeanItemContainer<MyBean> dataSource=new BeanItemContainer<MyBean>(getBeans());
  table.setContainerDataSource(dataSource);
  table.setSelectable(true);
  table.setPageLength(0);
  addComponent(table);
}
