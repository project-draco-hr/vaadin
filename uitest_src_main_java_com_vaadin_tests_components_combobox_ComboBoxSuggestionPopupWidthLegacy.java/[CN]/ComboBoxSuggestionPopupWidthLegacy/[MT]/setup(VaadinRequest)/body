{
  ComboBox legacy=new ComboBox("200px wide ComboBox with legacy mode suggestion popup setPopupWidth(null)",items);
  legacy.addStyleName("legacy");
  legacy.setWidth("200px");
  legacy.setPopupWidth(null);
  addComponent(legacy);
}
