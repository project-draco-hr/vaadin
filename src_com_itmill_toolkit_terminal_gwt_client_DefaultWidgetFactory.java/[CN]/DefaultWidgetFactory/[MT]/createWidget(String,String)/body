{
  if ("button".equals(tag))   return new Button();
  if ("window".equals(tag))   return new Window();
  if ("orderedlayout".equals(tag))   return new OrderedLayout();
  if ("label".equals(tag))   return new Label();
  return null;
}
