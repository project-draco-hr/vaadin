{
  getGridElement().scrollToRow(500);
  selectMenuPath(CUSTOM_DETAILS_GENERATOR);
  selectMenuPath(FIRST_ITEM_DETAILS);
  getGridElement().scrollToRow(0);
  assertNotNull("unexpected null details row",getGridElement().getDetails(0));
}
