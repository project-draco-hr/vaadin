{
  try {
    getGridElement().getDetails(0);
    fail("Expected NoSuchElementException");
  }
 catch (  NoSuchElementException ignore) {
  }
  selectMenuPath(FIRST_ITEM_DETAILS);
  assertNotNull("details should've opened",getGridElement().getDetails(0));
}
