{
  if (widthInPercents < 0 || widthInPercents > 100)   throw new IllegalArgumentException("Relative width must " + "be between 0% and 100%");
  width=String.valueOf(widthInPercents) + "%";
  requestRepaint();
}
