{
  VaadinServletService vss=new VaadinServletService(EasyMock.createMock(VaadinServlet.class),new MockDeploymentConfiguration());
  vss.init();
  return new LegacyCommunicationManager(new AlwaysLockedVaadinSession(vss));
}
