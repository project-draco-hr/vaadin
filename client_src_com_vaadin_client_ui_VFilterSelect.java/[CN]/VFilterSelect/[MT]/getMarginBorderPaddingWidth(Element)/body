{
  final ComputedStyle s=new ComputedStyle(element);
  int[] margin=s.getMargin();
  int[] border=s.getBorder();
  int[] padding=s.getPadding();
  return margin[1] + margin[3] + border[1]+ border[3]+ padding[1]+ padding[3];
}
