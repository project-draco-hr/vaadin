{
  if (row > rows) {
    throw new IllegalArgumentException("Cannot insert row at " + row + " in a gridlayout with height "+ rows);
  }
  for (Iterator<Area> i=areas.iterator(); i.hasNext(); ) {
    Area existingArea=i.next();
    if (existingArea.row2 >= row) {
      existingArea.row2++;
      if (existingArea.row1 >= row) {
        existingArea.row1++;
      }
    }
  }
  if (cursorY >= row) {
    cursorY++;
  }
  setRows(rows + 1);
  structuralChange=true;
  requestRepaint();
}
