{
  Container c=new IndexedContainer();
  if (options != null)   for (Iterator i=options.iterator(); i.hasNext(); )   c.addItem(i.next());
  setCaption(caption);
  setContainerDataSource((Container)c);
}
