{
  unselect(itemId);
  boolean retval=items.removeItem(itemId);
  this.itemIdMapper.remove(itemId);
  if (retval && !(items instanceof Container.ItemSetChangeNotifier))   fireItemSetChange();
  return retval;
}
