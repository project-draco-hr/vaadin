{
  if (position < 0 || position > allItemIds.size() || itemId == null || item == null) {
    return null;
  }
  if (allItemIds.contains(itemId)) {
    return null;
  }
  allItemIds.add(position,itemId);
  registerNewItem(position,itemId,item);
  return item;
}
