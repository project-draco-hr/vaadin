{
  try {
    while (!applications.isEmpty()) {
      final Application app=applications.iterator().next();
      app.close();
      removeApplication(app);
    }
  }
 catch (  Exception e) {
    logger.log(Level.SEVERE,"Could not remove application, leaking memory.",e);
  }
}
