{
  WebElement nonSelectedDay=driver.findElement(By.cssSelector(cssClass + " .v-inline-datefield-calendarpanel-day"));
  assertFalse(nonSelectedDay.getAttribute("class").contains("selected"));
  nonSelectedDay.click();
  assertFalse(nonSelectedDay.getAttribute("class").contains("selected"));
}
