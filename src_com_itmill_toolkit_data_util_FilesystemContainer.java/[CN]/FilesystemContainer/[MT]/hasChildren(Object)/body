{
  if (!(itemId instanceof File))   return false;
  String[] l;
  if (this.filter != null)   l=((File)itemId).list(this.filter);
 else   l=((File)itemId).list();
  return (l != null) && (l.length > 0);
}
