{
  setContainerHeight();
  if (initializedAndAttached) {
    updatePageLength();
  }
  if (!rendering) {
    Util.runWebkitOverflowAutoFix(scrollBodyPanel.getElement());
  }
  setProperTabIndex();
}
