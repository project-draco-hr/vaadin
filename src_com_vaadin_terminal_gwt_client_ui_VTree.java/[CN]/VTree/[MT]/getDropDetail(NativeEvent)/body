{
  TreeNode treeNode=keyToNode.get(currentMouseOverKey);
  if (treeNode == null) {
    return null;
  }
  VerticalDropLocation verticalDropLocation=VerticalDropLocation.get(treeNode.getElement(),event.getClientY(),0.2);
  return verticalDropLocation.toString();
}
