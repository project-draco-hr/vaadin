{
  createTreeTableAndPopulate();
  addComponent(new Button("Create another TreeTable",new Button.ClickListener(){
    @Override public void buttonClick(    ClickEvent event){
      createTreeTableAndPopulate();
    }
  }
));
}
