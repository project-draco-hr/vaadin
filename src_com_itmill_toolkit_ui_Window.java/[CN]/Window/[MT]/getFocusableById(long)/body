{
  final WeakReference ref=(WeakReference)focusableComponents.get(new Long(focusableId));
  if (ref != null) {
    final Object o=ref.get();
    if (o != null) {
      return (Component.Focusable)o;
    }
  }
  return null;
}
