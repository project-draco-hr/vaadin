{
  if (getApplication() != null && getApplication().getMainWindow() == this) {
    return;
  }
  Window parent=(Window)getParent();
  if (parent == null) {
    setVisible(false);
    fireClose();
  }
 else {
    parent.removeWindow(this);
    fireClose();
  }
}
