{
  currentMimeResponse=(RenderResponse)((WrappedPortletResponse)response).getPortletResponse();
  try {
    return super.handleApplicationRequest(request,response);
  }
  finally {
    currentMimeResponse=null;
  }
}
