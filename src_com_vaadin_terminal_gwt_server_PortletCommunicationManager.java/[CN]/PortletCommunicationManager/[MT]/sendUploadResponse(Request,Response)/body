{
  if (response.getWrappedResponse() instanceof ActionResponse) {
    ((ActionResponse)response.getWrappedResponse()).sendRedirect("http://www.google.com");
  }
 else   super.sendUploadResponse(request,response);
}
