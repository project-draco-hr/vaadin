{
  currentUidlResponse=response;
  doHandleUidlRequest(new WrappedPortletRequest(request),new WrappedPortletResponse(response),new AbstractApplicationPortletWrapper(applicationPortlet),root);
  currentUidlResponse=null;
}
