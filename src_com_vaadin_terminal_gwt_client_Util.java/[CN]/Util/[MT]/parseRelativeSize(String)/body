{
  if (size == null || !size.endsWith("%")) {
    return -1;
  }
  try {
    return Float.parseFloat(size.substring(0,size.length() - 1));
  }
 catch (  Exception e) {
    ClientExceptionHandler.displayError("Unable to parse relative size",e);
  }
  return -1;
}
