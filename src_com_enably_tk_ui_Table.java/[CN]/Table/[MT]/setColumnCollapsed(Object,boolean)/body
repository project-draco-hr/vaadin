{
  if (!this.isColumnCollapsingAllowed()) {
    throw new IllegalAccessException("Column collapsing not allowed!");
  }
  if (collapsed)   this.collapsedColumns.add(propertyId);
 else   this.collapsedColumns.remove(propertyId);
  refreshCurrentPage();
}
