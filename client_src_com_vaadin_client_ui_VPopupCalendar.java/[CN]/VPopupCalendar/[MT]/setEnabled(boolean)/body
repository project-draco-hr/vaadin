{
  super.setEnabled(enabled);
  if (enabled) {
    Roles.getButtonRole().setAriaDisabledState(calendarToggle.getElement(),true);
  }
 else {
    Roles.getButtonRole().setAriaDisabledState(calendarToggle.getElement(),false);
  }
}
