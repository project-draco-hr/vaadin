{
  if (event.getSource() == popup) {
    buildDate();
    if (!BrowserInfo.get().isTouchDevice() && textFieldEnabled) {
      focus();
    }
    Timer t=new Timer(){
      @Override public void run(){
        open=false;
      }
    }
;
    t.schedule(100);
  }
}
