{
  setCp1251Charset();
  String cp1251Html=getHtml();
  Assert.assertEquals("Non ascii string parsed from serialized HTML " + "differs from expected",NON_ASCII_STRING,getHtmlLabelValue(cp1251Html));
}
