{
  setMargin(true);
  ComboBox l=new ComboBox();
  l.setInputPrompt("Please select a city");
  l.setImmediate(true);
  l.addListener(this);
  for (int i=0; i < cities.length; i++) {
    l.addItem(cities[i]);
  }
  addComponent(l);
}
