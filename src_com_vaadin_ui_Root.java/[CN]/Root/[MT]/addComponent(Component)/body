{
  if (Application.getCurrentApplication() instanceof Application.LegacyApplication) {
    getContent().addComponent(c);
  }
 else {
    throw new UnsupportedOperationException("Add components to the Root's content instead");
  }
}
