{
  if (Application.getCurrentApplication() instanceof Application.LegacyApplication) {
    getContent().removeAllComponents();
  }
 else {
    throw new UnsupportedOperationException("Remove components from the Root's content instead");
  }
}
