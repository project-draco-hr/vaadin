{
  if (W3CDebug) {
    System.err.println("LocatorImpl::reInit(" + p + ", "+ line+ ", "+ column+ ");");
  }
  uri=p.getInputSource().getURI();
  this.line=line;
  this.column=column;
  return this;
}
