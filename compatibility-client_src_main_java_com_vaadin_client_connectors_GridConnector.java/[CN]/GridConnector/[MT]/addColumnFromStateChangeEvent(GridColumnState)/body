{
  @SuppressWarnings("unchecked") CustomGridColumn column=new CustomGridColumn(state.id,((AbstractGridRendererConnector<Object>)state.rendererConnector));
  columnIdToColumn.put(state.id,column);
  getWidget().addColumn(column);
  columnOrder.add(state.id);
}
