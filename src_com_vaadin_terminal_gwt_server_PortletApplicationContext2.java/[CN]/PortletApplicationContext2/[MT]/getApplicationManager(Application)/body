{
  PortletCommunicationManager mgr=applicationToAjaxAppMgrMap.get(application);
  if (mgr == null) {
    mgr=new PortletCommunicationManagerImpl();
    applicationToAjaxAppMgrMap.put(application,mgr);
  }
  return mgr;
}
