{
  assertTrue("Didn't accept valid value",minMaxValidator.isValid((byte)15));
  assertTrue("Didn't accept valid value",minMaxValidator.isValid((byte)99));
  assertFalse("Accepted too small value",minMaxValidator.isValid((byte)9));
  assertFalse("Accepted too large value",minMaxValidator.isValid((byte)110));
}
