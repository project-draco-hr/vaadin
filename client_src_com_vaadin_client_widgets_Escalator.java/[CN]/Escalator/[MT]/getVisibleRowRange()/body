{
  if (!body.visualRowOrder.isEmpty()) {
    return Range.withLength(body.getLogicalRowIndex(body.visualRowOrder.getFirst()),body.visualRowOrder.size());
  }
 else {
    return Range.withLength(0,0);
  }
}
