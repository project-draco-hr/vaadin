{
  definedWidth=px;
  if (px < 0) {
    if (isAttached()) {
      autosizeColumns(Collections.singletonList(columns.indexOf(this)));
    }
 else {
      measuringRequested=true;
    }
  }
 else {
    calculatedWidth=px;
  }
}
