{
  Style style=deco.getStyle();
  decoHeight=detailsHeight + getBody().getDefaultRowHeight();
  style.setTop(-(getBody().getDefaultRowHeight() - getBorderTopHeight(getElement())),Unit.PX);
  style.setHeight(decoHeight,Unit.PX);
}
