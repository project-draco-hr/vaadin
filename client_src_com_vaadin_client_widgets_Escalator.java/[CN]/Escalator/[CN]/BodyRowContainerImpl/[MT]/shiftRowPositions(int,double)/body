{
  for (  TableRowElement tr : getVisibleRowsAfter(row)) {
    setRowPosition(tr,0,getRowTop(tr) + diff);
  }
  if (row > getTopRowLogicalIndex()) {
    getLogger().warning("[[spacers]] scrollbar compensation not yet " + "implemented");
  }
}
