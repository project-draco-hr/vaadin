{
  while (element != null) {
    TooltipInfo info=getTooltip(element);
    if (info != null) {
      return info;
    }
    if (ac.getPid(element) != null) {
      break;
    }
    element=DOM.getParent(element);
  }
  return null;
}
