{
  for (  final VariableNode node : variables) {
    if (StringUtil.containsVariable(text,node.getName())) {
      text=StringUtil.replaceVariable(text,node.getName(),node.getExpr().printState());
    }
  }
}
