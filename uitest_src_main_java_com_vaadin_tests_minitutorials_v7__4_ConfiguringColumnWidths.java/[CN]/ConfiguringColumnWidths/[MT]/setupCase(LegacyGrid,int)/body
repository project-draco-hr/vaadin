{
  if (number == 0) {
    return;
  }
  try {
    getClass().getDeclaredMethod("setupCase" + number,LegacyGrid.class).invoke(this,grid);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
}
