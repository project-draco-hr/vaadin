{
  super.onBrowserEvent(event);
  if (DOM.eventGetType(event) == Event.ONLOAD && icon == DOM.eventGetTarget(event)) {
    if (layoutManager != null) {
      layoutManager.layoutLater();
    }
 else {
      updateSize(caption);
    }
  }
}
