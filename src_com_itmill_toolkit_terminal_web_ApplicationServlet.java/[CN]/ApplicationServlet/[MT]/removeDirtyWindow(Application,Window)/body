{
synchronized (applicationToDirtyWindowSetMap) {
    HashMap dirtyWindows=(HashMap)applicationToDirtyWindowSetMap.get(application);
    if (dirtyWindows != null)     dirtyWindows.remove(window);
  }
}
