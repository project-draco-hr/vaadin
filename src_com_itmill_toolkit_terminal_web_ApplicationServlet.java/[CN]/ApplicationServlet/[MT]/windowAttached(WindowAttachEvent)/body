{
  Window win=event.getWindow();
  win.addListener((Paintable.RepaintRequestListener)this);
  if (win.isVisible())   addDirtyWindow(event.getApplication(),win);
 else   win.requestRepaintRequests();
}
