{
  blurOccured=true;
  if (sendBlurEvents) {
    DeferredCommand.addCommand(new Command(){
      public void execute(){
        if (blurOccured) {
          client.updateVariable(id,EventId.BLUR,"",true);
          blurOccured=false;
        }
      }
    }
);
  }
}
