{
  blurOccured=true;
  if (sendBlurEvents) {
    Scheduler.get().scheduleDeferred(new Command(){
      public void execute(){
        if (blurOccured) {
          client.updateVariable(id,EventId.BLUR,"",true);
          blurOccured=false;
        }
      }
    }
);
  }
}
