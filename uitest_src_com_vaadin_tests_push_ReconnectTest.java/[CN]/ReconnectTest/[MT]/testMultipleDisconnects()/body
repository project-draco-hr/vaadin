{
  setDebug(true);
  openTestURL();
  startTimer();
  waitUntilServerCounterChanges();
  for (int i=0; i < 5; i++) {
    disconnectProxy();
    Thread.sleep(1000);
    connectProxy();
    waitUntilServerCounterChanges();
  }
}
