{
  TextFieldConnector connector=getUsernameFieldConnector();
  if (connector != null) {
    connector.flush();
  }
  connector=getPasswordFieldConnector();
  if (connector != null) {
    connector.flush();
  }
}
