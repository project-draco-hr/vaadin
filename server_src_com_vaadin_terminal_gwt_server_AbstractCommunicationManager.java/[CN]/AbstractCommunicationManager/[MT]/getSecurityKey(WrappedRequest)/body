{
  String seckey=null;
  seckey=(String)request.getSessionAttribute(ApplicationConstants.UIDL_SECURITY_TOKEN_ID);
  if (seckey == null) {
    seckey=UUID.randomUUID().toString();
    request.setSessionAttribute(ApplicationConstants.UIDL_SECURITY_TOKEN_ID,seckey);
  }
  return seckey;
}
