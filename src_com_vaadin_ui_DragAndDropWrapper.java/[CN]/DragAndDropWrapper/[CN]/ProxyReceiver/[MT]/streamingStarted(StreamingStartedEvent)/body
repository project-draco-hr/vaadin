{
  listenProgressOfUploadedFile=file.getReceiver() != null;
  if (listenProgressOfUploadedFile) {
    file.getReceiver().streamingStarted(new ReceivingEventWrapper(event));
  }
  receivers.remove(file);
}
