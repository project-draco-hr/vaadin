{
  int newY=origY + y - origMouseY;
  if (newY < 0)   newY=0;
  if (newY + SPLITTER_SIZE > getOffsetHeight())   newY=getOffsetHeight() - SPLITTER_SIZE;
  DOM.setStyleAttribute(splitter,"top",newY + "px");
}
