{
  int newX=origX + x - origMouseX;
  if (newX < 0)   newX=0;
  if (newX + SPLITTER_SIZE > getOffsetWidth())   newX=getOffsetWidth() - SPLITTER_SIZE;
  DOM.setStyleAttribute(splitter,"left",newX + "px");
}
