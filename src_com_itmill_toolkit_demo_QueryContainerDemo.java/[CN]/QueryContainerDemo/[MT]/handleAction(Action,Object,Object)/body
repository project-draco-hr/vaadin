{
  if (sender == table) {
    tableLastAction.setValue("Last action clicked was '" + action.getCaption() + "' on item "+ target);
  }
 else   if (sender == tree) {
    treeLastAction.setValue("Last action clicked was '" + action.getCaption() + "' on item "+ target);
  }
}
