{
  if (newMaxLength >= 0 && newMaxLength != maxLength) {
    maxLength=newMaxLength;
    updateMaxLength(maxLength);
  }
 else   if (maxLength != -1) {
    maxLength=-1;
    updateMaxLength(maxLength);
  }
}
