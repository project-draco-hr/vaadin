{
  assertTrue("[15..15[ does not intersect with [0..10[",!Range.between(15,15).intersects(Range.between(0,10)));
  assertTrue("[0..10[ does not intersect with [15..15[",!Range.between(0,10).intersects(Range.between(15,15)));
}
