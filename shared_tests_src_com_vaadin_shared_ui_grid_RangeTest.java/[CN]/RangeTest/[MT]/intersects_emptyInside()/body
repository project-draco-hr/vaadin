{
  assertTrue("[5..5[ does intersect with [0..10[",Range.between(5,5).intersects(Range.between(0,10)));
  assertTrue("[0..10[ does intersect with [5..5[",Range.between(0,10).intersects(Range.between(5,5)));
}
