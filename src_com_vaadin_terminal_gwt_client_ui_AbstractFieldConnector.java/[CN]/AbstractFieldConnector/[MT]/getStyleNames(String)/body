{
  String styleNames=super.getStyleNames(primaryStyleName);
  if (isModified()) {
    styleNames+=" " + ApplicationConnection.MODIFIED_CLASSNAME;
  }
  if (isRequired()) {
    styleNames+=" " + primaryStyleName + ApplicationConnection.REQUIRED_CLASSNAME_EXT;
  }
  return styleNames;
}
