{
  List<WebElement> rows=getAllRows();
  new Actions(driver).moveToElement(rows.get(dragIdx)).clickAndHold().moveByOffset(5,0).perform();
  new Actions(driver).moveByOffset(xOffset - 5,yOffset).perform();
}
