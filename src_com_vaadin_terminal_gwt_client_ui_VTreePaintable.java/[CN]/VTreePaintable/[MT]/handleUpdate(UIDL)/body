{
  final TreeNode rootNode=getWidgetForPaintable().getNodeByKey(uidl.getStringAttribute("rootKey"));
  if (rootNode != null) {
    if (!rootNode.getState()) {
      rootNode.setState(true,false);
    }
    renderChildNodes(rootNode,(Iterator)uidl.getChildIterator());
  }
}
