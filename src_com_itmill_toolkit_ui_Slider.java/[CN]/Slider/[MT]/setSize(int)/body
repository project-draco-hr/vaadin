{
  this.size=size;
switch (orientation) {
case ORIENTATION_HORIZONTAL:
    setWidth(size);
  setWidthUnits(UNITS_PIXELS);
break;
default :
setHeight(size);
setHeightUnits(UNITS_PIXELS);
break;
}
requestRepaint();
}
