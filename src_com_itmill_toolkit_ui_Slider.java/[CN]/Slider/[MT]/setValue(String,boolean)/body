{
  if (this.values != null) {
    String v=new String(value.toString());
    if (this.values.contains(v))     super.setValue(v,repaintIsNotNeeded);
 else     throw new ValueOutOfBoundsException(value);
  }
 else {
  }
}
