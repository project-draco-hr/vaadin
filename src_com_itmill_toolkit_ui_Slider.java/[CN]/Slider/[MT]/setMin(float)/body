{
  this.min=min;
  this.values=null;
  try {
    if ((new Float(getValue().toString())).floatValue() < min)     super.setValue(new Float(min));
  }
 catch (  ClassCastException e) {
    super.setValue(new Float(min));
  }
  requestRepaint();
}
