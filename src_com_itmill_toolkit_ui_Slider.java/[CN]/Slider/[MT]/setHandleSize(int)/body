{
  if (handleSize < 0)   this.handleSize=-1;
 else   if (handleSize > 99)   this.handleSize=99;
 else   if (handleSize < 1)   this.handleSize=1;
 else   this.handleSize=handleSize;
  requestRepaint();
}
