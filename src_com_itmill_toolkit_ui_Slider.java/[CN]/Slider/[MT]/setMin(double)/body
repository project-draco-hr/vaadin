{
  this.min=min;
  try {
    if ((new Double(getValue().toString())).doubleValue() < min)     super.setValue(new Double(min));
  }
 catch (  ClassCastException e) {
    super.setValue(new Double(min));
  }
  requestRepaint();
}
