{
  int height=clearElement.getOffsetTop() - getElement().getOffsetTop();
  if (icon != null) {
    int iconHeight=icon.getOffsetHeight();
    ApplicationConnection.getConsole().log("Caption height: " + height + ", icon height: "+ iconHeight);
  }
  return height;
}
