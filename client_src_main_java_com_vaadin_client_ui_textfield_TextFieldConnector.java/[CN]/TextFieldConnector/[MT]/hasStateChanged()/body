{
  boolean textChanged=!getWidget().getValue().equals(getState().text);
  boolean cursorPosChanged=getWidget().getCursorPos() != getState().cursorPosition;
  return textChanged || cursorPosChanged;
}
