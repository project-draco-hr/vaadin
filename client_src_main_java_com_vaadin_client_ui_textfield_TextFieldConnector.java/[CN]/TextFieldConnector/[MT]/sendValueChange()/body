{
  if (!hasStateChanged()) {
    return;
  }
  lastSentCursorPosition=getWidget().getCursorPos();
  getRpcProxy(AbstractTextFieldServerRpc.class).setText(getWidget().getValue(),lastSentCursorPosition);
  getState().text=getWidget().getValue();
}
