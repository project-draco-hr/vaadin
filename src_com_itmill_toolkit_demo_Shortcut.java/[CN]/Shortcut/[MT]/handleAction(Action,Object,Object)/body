{
  main.addComponent(new Label("ShortcutAction fired" + action));
  if (target == a)   this.buttonAHandler();
  if (target == b)   this.buttonBHandler();
  if (target == c)   this.buttonCHandler();
  if (target == close)   this.close();
}
