{
  if (!Double.isNaN(deltaX) && deltaX != 0) {
    scrollerElem.setScrollLeft((int)(scrollerElem.getScrollLeft() + deltaX));
  }
  if (!Double.isNaN(deltaY) && deltaY != 0) {
    scrollerElem.setScrollTop((int)(scrollerElem.getScrollTop() + deltaY));
  }
  event.preventDefault();
}
