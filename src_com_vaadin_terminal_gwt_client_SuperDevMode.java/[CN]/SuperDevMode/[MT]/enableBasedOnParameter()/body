{
  String superDevModeParameter=Location.getParameter("superdevmode");
  if (superDevModeParameter != null) {
    if (!isSuperDevModeEnabledInModule()) {
      showError("SuperDevMode is not enabled for this module/widgetset.<br/>" + "Ensure that your module definition (.gwt.xml) contains <br/>" + "&lt;add-linker name=&quot;xsiframe&quot;/&gt;<br/>"+ "&lt;set-configuration-property name=&quot;devModeRedirectEnabled&quot; value=&quot;true&quot; /&gt;<br/>");
      return false;
    }
    return SuperDevMode.recompileIfNeeded(superDevModeParameter);
  }
  return false;
}
