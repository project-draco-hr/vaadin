{
  ComboBox pixels=new ComboBox("200px wide ComboBox with 300px wide suggestion popup",items);
  pixels.addStyleName("pixels");
  pixels.setWidth("200px");
  pixels.setPopupWidth("300px");
  addComponent(pixels);
}
