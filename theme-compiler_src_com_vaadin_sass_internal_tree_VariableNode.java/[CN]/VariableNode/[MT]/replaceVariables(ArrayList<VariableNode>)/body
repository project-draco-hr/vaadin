{
  for (  final VariableNode node : variables) {
    if (!equals(node)) {
      if (StringUtil.containsVariable(expr.printState(),node.getName())) {
        if (expr.getParameters() != null && StringUtil.containsVariable(expr.getParameters().printState(),node.getName())) {
          replaceValues(expr.getParameters(),node);
        }
 else         if (expr.getLexicalUnitType() == LexicalUnitImpl.SCSS_VARIABLE) {
          replaceValues(expr,node);
        }
      }
    }
  }
}
