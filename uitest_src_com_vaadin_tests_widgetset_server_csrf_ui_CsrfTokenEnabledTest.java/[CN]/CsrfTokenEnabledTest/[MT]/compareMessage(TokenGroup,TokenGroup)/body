{
  return tokenGroup1.clientToken.equals(tokenGroup2.clientToken) && tokenGroup1.clientToken.equals(tokenGroup1.tokenReceivedFromServer) && isUndefined(tokenGroup1.tokenSentToServer)&& tokenGroup2.clientToken.equals(tokenGroup2.tokenSentToServer)&& isUndefined(tokenGroup2.tokenReceivedFromServer);
}
