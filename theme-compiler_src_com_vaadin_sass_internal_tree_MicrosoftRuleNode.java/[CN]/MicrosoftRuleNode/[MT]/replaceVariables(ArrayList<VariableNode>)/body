{
  for (  final VariableNode var : variables) {
    if (StringUtil.containsVariable(value,var.getName())) {
      value=StringUtil.replaceVariable(value,var.getName(),var.getExpr().toString());
    }
  }
}
