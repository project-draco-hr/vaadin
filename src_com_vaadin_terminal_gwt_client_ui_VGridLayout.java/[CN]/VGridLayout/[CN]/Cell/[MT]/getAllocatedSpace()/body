{
  if (cc != null) {
    return new RenderSpace(getAvailableWidth() - cc.getCaptionWidthAfterComponent(),getAvailableHeight() - cc.getCaptionHeightAboveComponent());
  }
 else {
    return new RenderSpace(getAvailableWidth(),getAvailableHeight());
  }
}
