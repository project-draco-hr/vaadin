{
  assertTrue("Validator without ranges didn't accept value",cleanValidator.isValid((short)-15));
  assertTrue("Didn't accept valid value",minValidator.isValid((short)15));
  assertFalse("Accepted too small value",minValidator.isValid((short)9));
}
