{
  assertTrue("Validator without ranges didn't accept value",cleanValidator.isValid((short)1120));
  assertTrue("Didn't accept valid value",maxValidator.isValid((short)15));
  assertFalse("Accepted too large value",maxValidator.isValid((short)120));
}
