{
  if (connector.getParent() instanceof MayScrollChildren && (BrowserInfo.get().requiresOverflowAutoFix() || BrowserInfo.get().isIE9()) && !"absolute".equals(connector.getWidget().getElement().getStyle().getPosition())) {
    pendingOverflowFixes.add(connector);
  }
}
