{
  for (  Element element : measuredNonPaintableElements) {
    measuredAndUpdate(element,getMeasuredSize(element,null));
  }
  VConsole.log("Measured " + measuredNonPaintableElements.size() + " non paintable elements");
}
