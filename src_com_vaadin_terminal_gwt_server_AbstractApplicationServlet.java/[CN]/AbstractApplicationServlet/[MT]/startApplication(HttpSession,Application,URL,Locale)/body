{
  if (application == null) {
    throw new ServletException("Application is null and can't be started");
  }
  if (!application.isRunning()) {
    final WebApplicationContext context=WebApplicationContext.getApplicationContext(session);
    context.addApplication(application);
    application.setLocale(locale);
    application.start(applicationUrl,applicationProperties,context);
  }
}
