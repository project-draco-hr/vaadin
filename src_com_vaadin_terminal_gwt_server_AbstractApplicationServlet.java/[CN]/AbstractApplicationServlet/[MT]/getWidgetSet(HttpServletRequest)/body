{
  String widgetset=(String)request.getAttribute(REQUEST_WIDGETSET);
  if (widgetset == null) {
    widgetset=getApplicationOrSystemProperty(PARAMETER_WIDGETSET,DEFAULT_WIDGETSET);
  }
  return widgetset;
}
