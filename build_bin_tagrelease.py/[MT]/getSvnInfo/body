def getSvnInfo():
    if os.path.exists('.svn'):
        pin = os.popen('svn info', 'r')
    elif os.path.exists('.git'):
        pin = os.popen('git svn info', 'r')
    values = readMap(pin, '^([^:]+):\\s*(.+)$')
    pin.close()
    return values
