{
  super.setImmediate(immediate);
  for (Iterator<LegacyField<?>> i=fields.values().iterator(); i.hasNext(); ) {
    LegacyField<?> f=i.next();
    if (f instanceof AbstractComponent) {
      ((AbstractComponent)f).setImmediate(immediate);
    }
  }
}
