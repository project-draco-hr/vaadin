{
  String type=((JSONString)jsonArray.get(0)).stringValue();
  VaadinSerializer serializer=getSerializer(type);
  Object object=serializer.deserialize((JSONObject)jsonArray.get(1),idMapper);
  return object;
}
