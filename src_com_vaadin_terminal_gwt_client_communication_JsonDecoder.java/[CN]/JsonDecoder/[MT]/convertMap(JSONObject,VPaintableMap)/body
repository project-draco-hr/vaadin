{
  HashMap<String,Object> map=new HashMap<String,Object>();
  Iterator<String> it=jsonMap.keySet().iterator();
  while (it.hasNext()) {
    String key=it.next();
    map.put(key,convertVariableValue((JSONArray)jsonMap.get(key),idMapper));
  }
  return map;
}
