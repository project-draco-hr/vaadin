{
  float pos=0;
  if (position.indexOf("%") > 0) {
    pos=Float.valueOf(position.substring(0,position.length() - 1));
  }
 else {
    pos=Math.round(Float.parseFloat(position.substring(0,position.length() - 2)));
  }
  client.updateVariable(id,"position",pos,immediate);
}
