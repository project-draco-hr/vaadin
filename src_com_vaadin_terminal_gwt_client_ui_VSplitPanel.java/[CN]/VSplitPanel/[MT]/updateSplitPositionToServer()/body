{
  final String position=orientation == ORIENTATION_HORIZONTAL ? splitter.getStyle().getProperty("left") : splitter.getStyle().getProperty("top");
  final int pos=Integer.parseInt(position.substring(0,position.length() - 2));
  client.updateVariable(id,"position",pos,immediate);
}
