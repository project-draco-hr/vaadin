{
  if (!isClientChangeAllowed()) {
    return;
  }
  if (date != null && date.length() > 6) {
    try {
      Date d=df_date.parse(date);
      fireDateClick(d);
    }
 catch (    ParseException e) {
    }
  }
}
