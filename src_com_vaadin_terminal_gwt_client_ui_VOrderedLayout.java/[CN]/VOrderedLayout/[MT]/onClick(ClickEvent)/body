{
  Paintable childComponent=getChildComponent((Element)event.getNativeEvent().getEventTarget().cast());
  final MouseEventDetails details=new MouseEventDetails(event.getNativeEvent());
  Object[] parameters=new Object[]{details,childComponent};
  client.updateVariable(client.getPid(this),CLICK_EVENT_IDENTIFIER,parameters,true);
}
