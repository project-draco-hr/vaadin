{
  setText(headerText);
  DOM.setElementProperty(captionContainer,"className",CLASSNAME + "-footer-container");
  DOM.setStyleAttribute(captionContainer,"overflow","visible");
  DOM.appendChild(td,captionContainer);
  setElement(td);
}
