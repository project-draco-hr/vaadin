{
  for (  RequestHandler handler : new ArrayList<RequestHandler>(session.getRequestHandlers())) {
    if (handler.handleRequest(session,request,response)) {
      return true;
    }
  }
  return false;
}
