{
  if (null == buttonRpcProxy) {
    buttonRpcProxy=GWT.create(ButtonClientToServerRpc.class);
    ((InitializableClientToServerRpc)buttonRpcProxy).initRpc(paintableId,client);
  }
  return buttonRpcProxy;
}
