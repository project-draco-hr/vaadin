{
  if (paintableId == null || client == null) {
    return;
  }
  if (BrowserInfo.get().isSafari()) {
    VNativeButton.this.setFocus(true);
  }
  if (disableOnClick) {
    setEnabled(false);
    client.updateVariable(paintableId,"disabledOnClick",true,false);
  }
  MouseEventDetails details=new MouseEventDetails(event.getNativeEvent(),getElement());
  client.updateVariable(paintableId,"mousedetails",details.serialize(),false);
  client.updateVariable(paintableId,"state",true,true);
  clickPending=false;
}
