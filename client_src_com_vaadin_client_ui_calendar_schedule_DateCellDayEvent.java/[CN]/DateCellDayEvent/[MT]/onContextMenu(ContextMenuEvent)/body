{
  if (this.dateCell.weekgrid.getCalendar().getMouseEventListener() != null) {
    event.preventDefault();
    event.stopPropagation();
    this.dateCell.weekgrid.getCalendar().getMouseEventListener().contextMenu(event,this);
  }
}
