{
  if (orientation == ORIENTATION_HORIZONTAL) {
    containerExpansion.setWidth(containerExpansion.getWidth() + extra);
  }
 else {
    containerExpansion.setHeight(containerExpansion.getHeight() + extra);
  }
}
