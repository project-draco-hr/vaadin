{
  if (w == widget) {
    return;
  }
  if (w != null) {
    w.removeFromParent();
  }
  if (widget != null) {
    remove(widget);
  }
  widget=w;
  if (w != null) {
    widgetDIV.appendChild(widget.getElement());
    adopt(w);
    w.getElement().getStyle().setDisplay(Display.INLINE_BLOCK);
  }
}
