{
  return "Property value change should only update absolutely " + "needed cells. Tables have common datasource. The first is " + "editable, second one has data in disabled fields, the lastone "+ "is plain table that directly shows data. Use first table and "+ "combobox/sync button to send changed values to server and evaluate "+ "given uidl responses.";
}
