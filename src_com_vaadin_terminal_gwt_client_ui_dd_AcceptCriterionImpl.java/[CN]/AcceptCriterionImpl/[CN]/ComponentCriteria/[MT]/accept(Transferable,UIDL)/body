{
  try {
    Paintable component=transferable.getComponent();
    String requiredPid=configuration.getStringAttribute("component");
    String pid=((Widget)component).getElement().getPropertyString("tkPid");
    return pid.equals(requiredPid);
  }
 catch (  Exception e) {
  }
  return false;
}
