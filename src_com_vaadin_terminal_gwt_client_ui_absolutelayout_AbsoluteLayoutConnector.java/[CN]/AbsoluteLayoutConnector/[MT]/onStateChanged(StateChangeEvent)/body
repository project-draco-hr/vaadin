{
  super.onStateChanged(stateChangeEvent);
  clickEventHandler.handleEventHandlerRegistration();
  for (  ComponentConnector child : getChildren()) {
    getWrapper(child).setPosition(getState().getConnectorPosition(child));
  }
}
