{
  assertFalse("field should not be immediate by default",field.isImmediate());
  field.addValidator(validator);
  assertTrue("field should be immediate when it has a validator",field.isImmediate());
}
