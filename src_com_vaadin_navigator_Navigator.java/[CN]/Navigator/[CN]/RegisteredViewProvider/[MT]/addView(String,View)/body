{
  if (viewName == null || view == null || viewName.length() == 0) {
    throw new IllegalArgumentException("view and viewName must be non-null and not empty");
  }
  viewNameToView.put(viewName,view);
}
