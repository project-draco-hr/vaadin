{
  for (  final VariableNode node : variables) {
    if (StringUtil.containsVariable(args,node.getName())) {
      args=StringUtil.replaceVariable(args,node.getName(),node.getExpr().printState());
    }
  }
}
