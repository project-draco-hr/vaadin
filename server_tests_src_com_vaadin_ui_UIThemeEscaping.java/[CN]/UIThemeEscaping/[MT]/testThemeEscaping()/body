{
  UI ui=new UI(){
    @Override protected void init(    VaadinRequest request){
    }
  }
;
  ui.setTheme("a<??(_\"$");
  String theme=ui.getTheme();
  Assert.assertEquals("Dangerous characters should be removed from the theme name","a??_$",theme);
}
