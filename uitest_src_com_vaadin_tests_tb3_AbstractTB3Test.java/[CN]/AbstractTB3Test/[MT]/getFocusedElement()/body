{
  Object focusedElement=((JavascriptExecutor)getDriver()).executeScript("return document.activeElement");
  if (null != focusedElement) {
    return (WebElement)focusedElement;
  }
 else {
    return null;
  }
}
