{
  Collection<Object> childs=(Collection<Object>)treetable.getChildren(event.getItemId());
  if (childs == null) {
    return;
  }
  Object[] arr=childs.toArray();
  for (  Object obj : arr) {
    System.out.println("remove  " + obj.toString());
    treetable.removeItem(obj);
  }
}
