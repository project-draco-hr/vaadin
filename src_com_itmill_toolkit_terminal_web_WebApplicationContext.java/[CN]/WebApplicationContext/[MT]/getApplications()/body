{
  LinkedList applications=(LinkedList)session.getAttribute(WebAdapterServlet.SESSION_ATTR_APPS);
  return Collections.unmodifiableCollection(applications == null ? (new LinkedList()) : applications);
}
