{
  String s="";
  if (getState().getStyles() != null) {
    for (final Iterator<String> it=getState().getStyles().iterator(); it.hasNext(); ) {
      s+=it.next();
      if (it.hasNext()) {
        s+=" ";
      }
    }
  }
  return s;
}
