{
  String s="";
  if (getState().styles != null) {
    for (final Iterator<String> it=getState().styles.iterator(); it.hasNext(); ) {
      s+=it.next();
      if (it.hasNext()) {
        s+=" ";
      }
    }
  }
  return s;
}
