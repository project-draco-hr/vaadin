{
  setStyleName(CLASSNAME + "-suggestpopup");
  if (componentState.styles == null || componentState.styles.isEmpty()) {
    for (    String style : componentState.styles) {
      if (!"".equals(style)) {
        addStyleDependentName(style);
      }
    }
  }
}
