{
  if (!getWidget().vertical && isUndefinedHeight() && (hasRelativeHeight.size() > 0 || (hasVerticalAlignment.size() > 0 && hasVerticalAlignment.size() < getChildren().size()))) {
    return true;
  }
  return false;
}
