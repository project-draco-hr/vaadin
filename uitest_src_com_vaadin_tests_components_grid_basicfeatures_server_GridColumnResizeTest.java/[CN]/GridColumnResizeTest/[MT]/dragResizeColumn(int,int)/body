{
  GridCellElement headerCell=getGridElement().getHeaderCell(0,columnIndex);
  Dimension size=headerCell.getSize();
  new Actions(getDriver()).moveToElement(headerCell,size.getWidth() - 1,size.getHeight() / 2).clickAndHold().moveByOffset(offset,0).release().perform();
}
