{
  if (dragEvent.getTransferable() instanceof ComponentTransferable) {
    return ((ComponentTransferable)dragEvent.getTransferable()).getSourceComponent() == component;
  }
 else {
    return false;
  }
}
