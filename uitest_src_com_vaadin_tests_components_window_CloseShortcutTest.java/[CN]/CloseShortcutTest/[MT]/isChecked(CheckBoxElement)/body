{
  String checked=cb.findElement(By.tagName("input")).getAttribute("checked");
  if ("true".equals(checked)) {
    return true;
  }
 else   if (checked == null) {
    return false;
  }
  throw new IllegalStateException("Unexpected attribute value for 'checked': " + checked);
}
