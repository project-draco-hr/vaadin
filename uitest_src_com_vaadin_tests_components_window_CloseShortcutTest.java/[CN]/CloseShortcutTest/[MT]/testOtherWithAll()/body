{
  attemptOtherShortcut();
  if (!Browser.IE8.getDesiredCapabilities().equals(getDesiredCapabilities()) && !Browser.FIREFOX.getDesiredCapabilities().equals(getDesiredCapabilities()) && !Browser.CHROME.getDesiredCapabilities().equals(getDesiredCapabilities())) {
    ensureWindowClosed();
  }
}
