{
  if (exception != null) {
    warnings.addLast(exception);
    SourceLocator l=exception.getLocator();
    if (l != null) {
      rowToErrorMap.put(new Integer(l.getLineNumber()),exception);
      errorToRowMap.put(exception,new Integer(l.getLineNumber()));
    }
  }
}
