{
  try {
    paintTarget=new WebPaintTarget(variableMap,transformerType,webAdapterServlet,transformerType.getTheme());
  }
 catch (  PaintException e) {
    throw new IllegalArgumentException("Failed to instantiate new WebPaintTarget: " + e);
  }
  return paintTarget;
}
