{
  String design="<vaadin-tree-table>" + "  <table>" + "    <colgroup><col property-id=''></colgroup>"+ "    <tbody>" + hierarchy + "</tbody>"+ "  </table>"+ "</vaadin-tree-table>";
  try {
    read(design);
    Assert.fail("Malformed hierarchy should fail: " + hierarchy);
  }
 catch (  DesignException expected) {
  }
}
