{
  Widget editorWidget=event.getEditorWidget();
  if (editorWidget != null) {
    if (editorWidget.getElement().isOrHasChild(WidgetUtil.getFocusedElement())) {
      WidgetUtil.getFocusedElement().blur();
    }
  }
}
