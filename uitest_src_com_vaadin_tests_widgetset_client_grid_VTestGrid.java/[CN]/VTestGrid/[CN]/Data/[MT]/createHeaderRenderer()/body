{
  return new CellRenderer(){
    @Override public void renderCell(    Cell cell){
      int columnName=columns.get(cell.getColumn());
      cell.getElement().setInnerText("Header " + columnName);
    }
  }
;
}
