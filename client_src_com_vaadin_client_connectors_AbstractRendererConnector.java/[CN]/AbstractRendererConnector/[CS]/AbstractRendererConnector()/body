{
  if (presentationType == null) {
    throw new IllegalStateException("No presentation type found for " + WidgetUtil.getSimpleName(this) + ". This may be caused by some unspecified problem in widgetset compilation.");
  }
}
