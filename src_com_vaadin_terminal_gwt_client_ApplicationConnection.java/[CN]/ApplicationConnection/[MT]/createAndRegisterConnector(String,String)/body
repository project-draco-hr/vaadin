{
  ComponentConnector p=widgetSet.createWidget(connectorType,configuration);
  p.setConnection(this);
  p.setId(connectorId);
  p.init();
  connectorMap.registerConnector(connectorId,p);
  return p;
}
