{
  for (  String id : unregistryBag) {
    Widget component=(Widget)idToPaintableDetail.get(id).getComponent();
    if (!component.isAttached()) {
      idToPaintableDetail.remove(id);
    }
  }
  unregistryBag.clear();
}
