{
  if (null == titleOwner) {
    return null;
  }
  ComponentDetail pd=idToPaintableDetail.get(getPid(titleOwner));
  if (null != pd) {
    return pd.getTooltipInfo();
  }
 else {
    return null;
  }
}
