{
  List<WebElement> elems=getGridElement().findElements(By.className("v-grid-editor-row"));
  assertLessThanOrEqual("number of editors",elems.size(),1);
  return elems.isEmpty() ? null : elems.get(0);
}
