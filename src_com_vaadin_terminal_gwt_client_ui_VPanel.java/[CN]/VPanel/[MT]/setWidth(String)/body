{
  if (this.width.equals(width)) {
    return;
  }
  this.width=width;
  super.setWidth(width);
  if (!rendering) {
    runHacks(true);
    if (height.equals("")) {
      Util.updateRelativeChildrenAndSendSizeUpdateEvent(client,this,this);
    }
  }
}
