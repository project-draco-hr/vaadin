{
  VCaption caption=getWidgetForPaintable().captions.get(child);
  if (caption == null) {
    return 0;
  }
 else {
    MeasureManager.MeasuredSize measuredSize=getWidgetForPaintable().client.getMeasuredSize(child);
    return measuredSize.getDependencyWidth(caption.getElement());
  }
}
