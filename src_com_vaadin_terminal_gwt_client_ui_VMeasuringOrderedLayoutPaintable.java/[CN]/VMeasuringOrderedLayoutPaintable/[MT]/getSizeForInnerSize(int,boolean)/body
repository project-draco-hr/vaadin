{
  MeasuredSize measuredSize=getMeasuredSize();
  if (isVertical) {
    return size + measuredSize.getBorderHeight() + measuredSize.getPaddingHeight();
  }
 else {
    return size + measuredSize.getBorderWidth() + measuredSize.getPaddingWidth();
  }
}
