{
  MeasureManager.MeasuredSize measuredSize=getWidgetForPaintable().client.getMeasuredSize(paintable);
  if (isVertical) {
    return measuredSize.getHeight();
  }
 else {
    return measuredSize.getWidth();
  }
}
