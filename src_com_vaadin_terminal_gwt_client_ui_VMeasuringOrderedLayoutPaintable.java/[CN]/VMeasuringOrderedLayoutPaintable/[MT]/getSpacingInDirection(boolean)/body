{
  if (isVertical) {
    return getMeasuredSize().getDependencyOuterHeight(getWidgetForPaintable().spacingMeasureElement);
  }
 else {
    return getMeasuredSize().getDependencyOuterWidth(getWidgetForPaintable().spacingMeasureElement);
  }
}
