{
  if (isVertical) {
    return getLayoutManager().getOuterHeight(getWidgetForPaintable().spacingMeasureElement);
  }
 else {
    return getLayoutManager().getOuterWidth(getWidgetForPaintable().spacingMeasureElement);
  }
}
