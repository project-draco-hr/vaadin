{
  String pid=child.getId();
  if (getWidgetForPaintable().alignments.containsKey(pid)) {
    return new AlignmentInfo(getWidgetForPaintable().alignments.getInt(pid));
  }
 else {
    return AlignmentInfo.TOP_LEFT;
  }
}
