{
  if (isVertical) {
    return getLayoutManager().getInnerHeight(getWidgetForPaintable().getElement());
  }
 else {
    return getLayoutManager().getInnerWidth(getWidgetForPaintable().getElement());
  }
}
