{
  for (int i=0, j=0; i < super.getWidgetCount(); i++) {
    Widget w=getWidget(i);
    if (w instanceof AbsoluteWrapper) {
      if (child == w) {
        return j;
      }
 else {
        j++;
      }
    }
  }
  return -1;
}
