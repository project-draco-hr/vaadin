{
  if (notEqual(newValue,getValue()) || notEqual(newValue,lastKnownTextContent)) {
    localValueChanged=true;
    if (!repaintIsNotNeeded) {
      requestRepaint();
    }
  }
  super.setValue(newValue,repaintIsNotNeeded);
}
