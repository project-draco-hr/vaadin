{
  if (c != null) {
    tabs.addLast(c);
    tabCaptions.put(c,caption != null ? caption : "");
    if (icon != null) {
      tabIcons.put(c,icon);
    }
    if (selected == null) {
      selected=c;
      fireSelectedTabChange();
    }
    super.addComponent(c);
    requestRepaint();
  }
}
