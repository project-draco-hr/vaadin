{
  if (container instanceof GeneratedPropertyContainer) {
    super.initializeContainer(((GeneratedPropertyContainer)container).getWrappedContainer());
  }
 else {
    super.initializeContainer(container);
  }
}
