{
  final int interval=1000 * getConfiguration().getHeartbeatInterval();
  if (interval > 0) {
    new Timer(){
      @Override public void run(){
        sendHeartbeat();
      }
    }
.schedule(interval);
  }
}
