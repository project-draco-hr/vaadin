{
  Paintable openPaintable=openPaintables.peek();
  if (paintable != openPaintable) {
    throw new PaintException("Invalid UIDL: closing wrong paintable: '" + manager.getPaintableId(paintable) + "' expected: '"+ manager.getPaintableId(openPaintable)+ "'.");
  }
  openPaintables.pop();
  String openTag=openPaintableTags.pop();
  endTag(openTag);
}
