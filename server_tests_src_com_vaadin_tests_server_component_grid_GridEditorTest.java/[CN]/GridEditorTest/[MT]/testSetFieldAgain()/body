{
  TextField field=new TextField();
  grid.setEditorField(PROPERTY_NAME,field);
  field=new TextField();
  grid.setEditorField(PROPERTY_NAME,field);
  assertSame("new field should be used.",field,grid.getEditorField(PROPERTY_NAME));
}
