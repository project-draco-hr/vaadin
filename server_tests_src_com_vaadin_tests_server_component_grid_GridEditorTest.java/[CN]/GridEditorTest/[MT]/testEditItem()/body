{
  startEdit();
  assertEquals(ITEM_ID,grid.getEditedItemId());
  assertEquals(getEditedItem(),grid.getEditorFieldGroup().getItemDataSource());
  assertEquals(DEFAULT_NAME,grid.getEditorField(PROPERTY_NAME).getValue());
  assertEquals(String.valueOf(DEFAULT_AGE),grid.getEditorField(PROPERTY_AGE).getValue());
}
