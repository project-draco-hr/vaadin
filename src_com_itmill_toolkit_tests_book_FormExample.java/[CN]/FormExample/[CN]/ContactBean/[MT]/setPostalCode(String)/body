{
  try {
    if (postalCode != null) {
      this.postalCode=Integer.parseInt(postalCode);
    }
 else {
      this.postalCode=0;
    }
  }
 catch (  NumberFormatException e) {
    this.postalCode=0;
  }
}
