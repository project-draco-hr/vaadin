{
  if (invalidValueExceptions.size() == 1) {
    throw invalidValueExceptions.get(0);
  }
 else {
    InvalidValueException[] causes=invalidValueExceptions.toArray(new InvalidValueException[invalidValueExceptions.size()]);
    throw new InvalidValueException(null,causes);
  }
}
