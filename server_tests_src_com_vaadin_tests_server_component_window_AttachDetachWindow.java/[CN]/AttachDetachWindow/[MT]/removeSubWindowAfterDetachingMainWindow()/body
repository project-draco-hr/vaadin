{
  main.setSession(testApp);
  main.addWindow(sub);
  main.setSession(null);
  assertDetached(main);
  assertDetached(sub);
  main.removeWindow(sub);
  assertDetached(main);
  assertDetached(sub);
}
