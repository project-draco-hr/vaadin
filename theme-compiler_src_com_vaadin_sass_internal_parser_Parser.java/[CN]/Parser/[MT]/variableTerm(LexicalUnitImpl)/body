{
  LexicalUnitImpl result=null;
  String varName="";
  varName=variableName();
  result=LexicalUnitImpl.createVariable(token.beginLine,token.beginColumn,prev,varName);
{
    if (true) {
      return result;
    }
  }
  throw new Error("Missing return statement in function");
}
