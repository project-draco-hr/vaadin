{
  if (getLogger().isLoggable(Level.FINE)) {
    if (!dirtyConnectors.contains(connector)) {
      getLogger().fine(getDebugInfo(connector) + " " + "is now dirty");
    }
  }
  dirtyConnectors.add(connector);
}
