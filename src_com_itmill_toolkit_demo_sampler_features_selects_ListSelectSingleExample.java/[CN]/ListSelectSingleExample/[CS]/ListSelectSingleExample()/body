{
  setSpacing(true);
  ListSelect citySelect=new ListSelect("Please select a city",cities);
  citySelect.setRows(7);
  citySelect.setNullSelectionAllowed(false);
  citySelect.select("Berlin");
  citySelect.setImmediate(true);
  citySelect.addListener(this);
  addComponent(citySelect);
}
