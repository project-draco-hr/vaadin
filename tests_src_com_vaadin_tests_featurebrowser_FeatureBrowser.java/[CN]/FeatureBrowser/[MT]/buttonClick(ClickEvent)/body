{
  try {
    FeatureUtil.debug(getApplication().getUser().toString(),"buttonClick " + event.getButton().getClass().getSimpleName() + ", "+ event.getButton().getCaption()+ ", "+ event.getButton().getValue());
  }
 catch (  final Exception e) {
  }
}
