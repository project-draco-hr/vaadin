{
  WrappedRequest currentRequest=VaadinService.getCurrentRequest();
  try {
    WrappedPortletRequest request=WrappedPortletRequest.cast(currentRequest);
    if (request != null) {
      return request.getPortletRequest();
    }
 else {
      return null;
    }
  }
 catch (  ClassCastException e) {
    return null;
  }
}
