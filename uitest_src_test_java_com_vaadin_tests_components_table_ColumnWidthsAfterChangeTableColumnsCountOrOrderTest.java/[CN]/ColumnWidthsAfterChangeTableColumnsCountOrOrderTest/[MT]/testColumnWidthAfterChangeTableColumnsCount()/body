{
  openTestURL();
  getButtonChangeColumnCountAndWidth().click();
  waitForElementPresent(By.className("v-table"));
  assertEquals("The width of descr column should be " + NEW_COLUMN_WIDTH,NEW_COLUMN_WIDTH,getDescriptionColumnWidth());
}
