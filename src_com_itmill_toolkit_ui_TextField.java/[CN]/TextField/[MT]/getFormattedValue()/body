{
  Object value=getValue();
  if (this.format != null && value != null)   try {
    return this.format.format(value);
  }
 catch (  IllegalArgumentException ignored) {
  }
  if (value != null)   return value.toString();
  return null;
}
