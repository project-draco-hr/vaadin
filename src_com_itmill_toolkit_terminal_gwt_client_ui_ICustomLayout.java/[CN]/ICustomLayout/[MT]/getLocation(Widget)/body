{
  for (Iterator i=locationToWidget.keySet().iterator(); i.hasNext(); ) {
    String location=(String)i.next();
    if (locationToWidget.get(location) == w)     return location;
  }
  return null;
}
