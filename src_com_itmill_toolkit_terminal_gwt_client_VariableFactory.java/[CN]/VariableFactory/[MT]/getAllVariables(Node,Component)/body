{
  HashMap v=new HashMap();
  NodeList children=n.getChildNodes();
  for (int i=0; i < children.getLength(); i++) {
    Variable var=getVariable(children.item(i),owner);
    if (var != null)     v.put(var.getName(),var);
  }
  return v;
}
