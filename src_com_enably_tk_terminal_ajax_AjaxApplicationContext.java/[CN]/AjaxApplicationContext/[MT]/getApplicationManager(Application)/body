{
  ApplicationManager vm=(ApplicationManager)applicationToManagerMap.get(application);
  if (vm == null) {
    vm=new ApplicationManager(application);
    applicationToManagerMap.put(application,vm);
  }
  return vm;
}
