{
  if (connectorWrapper == null) {
    connectorWrapper=createConnectorWrapper(this,connector.getConnection(),nativeState,rpcMap,connector.getConnectorId(),rpcObjects);
  }
  return connectorWrapper;
}
