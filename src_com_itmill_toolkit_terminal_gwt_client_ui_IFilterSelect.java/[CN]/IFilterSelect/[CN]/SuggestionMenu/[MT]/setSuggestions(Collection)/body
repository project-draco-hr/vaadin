{
  this.clearItems();
  Iterator it=suggestions.iterator();
  while (it.hasNext()) {
    FilterSelectSuggestion s=(FilterSelectSuggestion)it.next();
    MenuItem mi=new MenuItem(s.getDisplayString(),true,s);
    this.addItem(mi);
    if (s == currentSuggestion)     selectItem(mi);
  }
}
