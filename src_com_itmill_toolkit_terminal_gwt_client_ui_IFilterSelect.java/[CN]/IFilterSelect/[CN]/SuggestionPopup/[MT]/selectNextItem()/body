{
  MenuItem cur=menu.getSelectedItem();
  int index=1 + menu.getItems().indexOf(cur);
  if (menu.getItems().size() > index)   menu.selectItem((MenuItem)menu.getItems().get(index));
 else   if (!clientSideFiltering && hasNextPage())   filterOptions(currentPage + 1);
}
