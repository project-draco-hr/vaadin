{
  int availableHeight=getOffsetHeight();
  int iconHeight=Util.getRequiredHeight(selectedItemIcon);
  DOM.setStyleAttribute(selectedItemIcon.getElement(),"marginTop",(availableHeight - iconHeight) / 2 + "px");
}
