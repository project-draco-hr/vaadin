{
  if (enabled) {
    if (!suggestionPopup.isJustClosed()) {
      filterOptions(-1,"");
      lastFilter="";
    }
    DOM.eventPreventDefault(DOM.eventGetCurrentEvent());
    tb.setFocus(true);
    tb.selectAll();
  }
}
