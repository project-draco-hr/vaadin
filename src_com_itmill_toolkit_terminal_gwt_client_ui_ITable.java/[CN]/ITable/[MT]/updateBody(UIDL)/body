{
  if (uidl == null)   return;
  Iterator it=uidl.getChildIterator();
  UIDL row=(UIDL)it.next();
  if (firstRendered == 0)   firstRendered=row.getIntAttribute("key");
  if (row.getIntAttribute("key") == lastRendered + 1) {
    while (it.hasNext())     appendRow((UIDL)it.next());
  }
}
