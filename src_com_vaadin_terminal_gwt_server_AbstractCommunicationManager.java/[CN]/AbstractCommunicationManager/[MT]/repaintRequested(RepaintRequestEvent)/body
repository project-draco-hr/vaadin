{
  if (paintPhase) {
    throw new IllegalStateException("Paintables shouldn't become dirty in paint phase!!");
  }
  final Paintable p=event.getPaintable();
  if (!dirtyPaintables.contains(p)) {
    dirtyPaintables.add(p);
  }
}
