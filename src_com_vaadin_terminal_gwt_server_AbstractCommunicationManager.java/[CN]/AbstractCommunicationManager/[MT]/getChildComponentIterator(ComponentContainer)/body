{
  if (cc instanceof Panel) {
    if (((Panel)cc).getContent() == null) {
      return new NullIterator<Component>();
    }
 else {
      return Collections.singleton((Component)((Panel)cc).getContent()).iterator();
    }
  }
  return cc.getComponentIterator();
}
