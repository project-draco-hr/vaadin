{
  final RpcTarget target=getRpcTarget(invocation.getConnectorId());
  if (null != target) {
    ServerRpcManager.applyInvocation(target,invocation);
  }
 else {
    throw new RuntimeException("No RPC target for paintable " + invocation.getConnectorId());
  }
}
