{
  try {
    BlockNodeHandler.traverse(this);
    replaceVariables(ScssStylesheet.getVariables());
  }
 catch (  Exception e) {
    Logger.getLogger(BlockNode.class.getName()).log(Level.SEVERE,null,e);
  }
}
