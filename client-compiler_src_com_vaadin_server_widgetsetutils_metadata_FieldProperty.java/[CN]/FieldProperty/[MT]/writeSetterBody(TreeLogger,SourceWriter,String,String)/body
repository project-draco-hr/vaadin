{
  boolean shouldUnbox=!"long".equals(field.getType().getSimpleSourceName());
  w.println("%s.@%s::%s = %s;",beanVariable,getBeanType().getQualifiedSourceName(),getName(),shouldUnbox ? unboxValue(valueVariable) : valueVariable);
}
