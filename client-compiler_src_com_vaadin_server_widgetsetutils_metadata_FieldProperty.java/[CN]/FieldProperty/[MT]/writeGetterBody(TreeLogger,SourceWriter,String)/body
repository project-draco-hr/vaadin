{
  boolean shouldBox=!"long".equals(field.getType().getSimpleSourceName());
  String value=String.format("%s.@%s::%s",beanVariable,getBeanType().getQualifiedSourceName(),getName());
  w.print("return ");
  w.print(shouldBox ? boxValue(value) : value);
  w.println(";");
}
