{
  if (testingToolsServerUri == null) {
    testingToolsServerUri="http" + (request.isSecure() ? "s" : "") + "://"+ request.getServerName()+ ":"+ request.getLocalPort()+ "/TestingTools/TestingToolsServer";
  }
  return testingToolsServerUri;
}
