{
  if (testingToolsServerUri == null) {
    testingToolsServerUri="http" + (request.isSecure() ? "s" : "") + "://"+ request.getServerName()+ ":"+ request.getLocalPort()+ "/ATF/ATFServer";
  }
  return testingToolsServerUri;
}
