{
  ApplicationManager mgr=(ApplicationManager)applicationToAjaxAppMgrMap.get(application);
  if (mgr == null) {
    mgr=new ApplicationManager(application,this);
    applicationToAjaxAppMgrMap.put(application,mgr);
    mgr.takeControl();
  }
  return mgr;
}
