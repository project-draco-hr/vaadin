{
  AbsoluteGridCell cell=(AbsoluteGridCell)paintableToCellMap.get(component);
  Caption c=cell.getCaption();
  if (c == null) {
    c=new Caption(component,client);
    cell.setCaption(c);
  }
  c.updateCaption(uidl);
}
